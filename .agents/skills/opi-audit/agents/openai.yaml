interface:
  display_name: "Opi Audit"
  short_description: "Seal and audit one Opi phase"
  default_prompt: "Use $opi-audit phase=<N> to run a sealed conformance audit at committed HEAD and emit immutable normalized findings."
policy:
  allow_implicit_invocation: false
