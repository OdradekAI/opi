interface:
  display_name: "Opi Document"
  short_description: "Audit implementation-backed English and Chinese docs"
  default_prompt: "Use $opi-document to independently audit every maintained current-product README against implementation evidence by default, or apply an explicitly requested targeted/version-bump scope, keeping English and Chinese mirrors truthful and synchronized."
policy:
  allow_implicit_invocation: false
