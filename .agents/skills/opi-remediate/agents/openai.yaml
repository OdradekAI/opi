interface:
  display_name: "Opi Remediate"
  short_description: "Plan or apply finding closure"
  default_prompt: "Use $opi-remediate mode=plan phase=<N> sources=<path,...> to verify immutable findings and prepare closure evidence."
policy:
  allow_implicit_invocation: false
