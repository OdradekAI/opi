[package]
name = "opi-eval"
description = "Unpublished Independent Companion for cross-agent evaluation experiments (provisional Phase 18 seam)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
clap = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tokio-util = { workspace = true }
toml = { workspace = true }

# Phase 18 external-process supervision (task 18.4): process groups (Unix)
# and Job Objects (Windows) for the crate-private ProcessSupervisor.
[target.'cfg(unix)'.dependencies]
libc = { workspace = true }

[target.'cfg(windows)'.dependencies]
windows-sys = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
