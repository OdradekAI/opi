# opi vs pi v0.84.1 (53fa77c) - objective differences

- **Measurement date:** 2026-08-11
- **opi:** `D:/Luiz/Odradek/opi`, version `0.7.3` (`Cargo.toml:13`; `CHANGELOG.md:9`), commit `b3a64fb20b050f782a6b7dcbdbf3af9218716b24`.
- **pi:** `D:/Luiz/Odradek/opi/.repo/pi-0.84.1`, package version `0.84.1` (`.repo/pi-0.84.1/packages/coding-agent/package.json:3`), tag `v0.84.1`, commit `53fa77ccd8a279eb87e92294ef3687b03ff80112`, remote `https://github.com/earendil-works/pi.git`.
- **Scope:** Full inward comparison, all 18 dimensions in `.claude/skills/opi-realign/references/dimensions.md`.
- **Method:** Both sides were read from source in three bounded measurement batches. Each batch was then adversarially checked by a different verifier against both source trees. Every retained delta has a verification outcome.
- **Target pin:** The supplied pi directory had no Git metadata. Its 1,354 files were SHA-256 compared with a clean checkout of the official `v0.84.1` tag; all file paths and contents matched.
- **Framing:** No phases, baselines, or roadmap framing.

## How to read

The **opi** and **pi** columns contain cited current-state facts. **Difference** states only the raw factual delta. Absence claims name the searched paths or patterns. **Verification outcome** is `confirmed`, `refined`, or `added`; no drift classification or recommendations are included.

## 1. Provider abstraction and dispatch

**opi:** `Provider` exposes an id, a synchronous model catalog, streaming, catalog replacement, and optional refresh. `ProviderRegistry` resolves model overrides before dynamic catalogs and built-ins; `ProviderCollection` resolves models, checks capabilities, gates authentication, and dispatches streams (`crates/opi-ai/src/provider.rs:22-60`; `crates/opi-ai/src/registry.rs:224-272`; `crates/opi-ai/src/provider_collection.rs:375-428`). `ApiMappedProvider` selects a route by each model's `wire_api` (`crates/opi-ai/src/api_mapped.rs:20-30`; `crates/opi-ai/src/api_mapped.rs:175-202`). The coding product constructs one active provider from `provider:model`; its harness collection exposes that provider through a catalog-only wrapper and registers extension providers separately (`crates/opi-coding-agent/src/provider_factory.rs:305-346`; `crates/opi-coding-agent/src/provider_factory.rs:1133-1147`; `crates/opi-coding-agent/src/provider_factory.rs:2120-2192`). Interactive model changes are restricted to the current provider (`crates/opi-coding-agent/src/harness.rs:1411-1417`; `crates/opi-coding-agent/src/harness.rs:1444-1462`).

**pi:** `Provider` includes authentication, current and dynamic catalogs, optional filtering, stream and simple-stream operations, and optional deferred fetch/cancel. `Models` owns the provider collection and performs lookup, authentication, refresh, stream, completion, and deferred dispatch (`.repo/pi-0.84.1/packages/ai/src/models.ts:97-148`; `.repo/pi-0.84.1/packages/ai/src/models.ts:151-222`; `.repo/pi-0.84.1/packages/ai/src/models.ts:667-731`). `createProvider` accepts one API implementation or a map keyed by `model.api` (`.repo/pi-0.84.1/packages/ai/src/models.ts:739-792`). The coding product's `ModelRuntime` retains the full provider collection and dispatches the selected model through its owning provider (`.repo/pi-0.84.1/packages/coding-agent/src/core/model-runtime.ts:130-169`; `.repo/pi-0.84.1/packages/coding-agent/src/core/model-runtime.ts:610-625`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| 1.1 Runtime dispatch topology | Constructs one active provider for the run; the active provider's collection entry is catalog-only, and product model changes stay within that provider (`crates/opi-coding-agent/src/provider_factory.rs:305-346`; `crates/opi-coding-agent/src/provider_factory.rs:1133-1147`; `crates/opi-coding-agent/src/harness.rs:1411-1462`). | Retains dispatchable providers for the full catalog and resolves the selected model's owning provider at dispatch (`.repo/pi-0.84.1/packages/coding-agent/src/core/model-runtime.ts:130-169`; `.repo/pi-0.84.1/packages/coding-agent/src/core/model-runtime.ts:610-625`). | One active product provider versus a retained multi-provider product runtime. | confirmed |
| 1.2 Provider boundary | `Provider` has no authentication or deferred methods; authentication gating is in `ProviderCollection` (`crates/opi-ai/src/provider.rs:22-60`; `crates/opi-ai/src/provider_collection.rs:375-428`). | `Provider` requires `auth` and may implement deferred fetch/cancel (`.repo/pi-0.84.1/packages/ai/src/models.ts:97-148`). | Authentication and deferred lifecycle occupy different abstraction boundaries. | confirmed |
| 1.3 Dynamic catalog replacement | Calls every registered provider in sorted sequence and replaces all dynamic catalogs only if every refresh succeeds (`crates/opi-ai/src/provider_collection.rs:431-505`). | Concurrently refreshes selected configured dynamic providers and returns per-provider results without rejecting the collection refresh (`.repo/pi-0.84.1/packages/ai/src/models.ts:172-177`; `.repo/pi-0.84.1/packages/ai/src/models.ts:487-557`). | Collection-wide transactional replacement versus selected per-provider refresh outcomes. | confirmed |

## 2. Authentication and credentials

**opi:** API-key and OAuth envelopes are stored in the OS keychain. Secret values redact in `Debug`, and a non-secret three-state probe reports present, absent, or unavailable status (`crates/opi-ai/src/credential.rs:39-94`; `crates/opi-ai/src/credential.rs:101-118`; `crates/opi-ai/src/credential.rs:217-238`; `crates/opi-coding-agent/src/doctor.rs:413-453`). API-key resolution is keychain-first with environment fallback after absence or backend unavailability. OAuth is keychain-only and refreshes with a five-minute skew, a mutation lock and double-check, a 30-second production timeout, and successful-write-only persistence (`crates/opi-coding-agent/src/credential_store.rs:1030-1103`; `crates/opi-coding-agent/src/credential_store.rs:1105-1197`). The production OAuth registry contains Anthropic, OpenAI Codex, and GitHub Copilot; interactive login/logout uses that registry (`crates/opi-coding-agent/src/oauth.rs:2386-2425`; `crates/opi-coding-agent/src/interactive_auth.rs:14-20`).

**pi:** The coding agent stores one type-tagged API-key or OAuth credential per provider in lock-serialized `auth.json`, created and written with mode `0600`; the storage interface supports read, list, delete, and serialized modification (`.repo/pi-0.84.1/packages/ai/src/auth/types.ts:13-43`; `.repo/pi-0.84.1/packages/ai/src/auth/types.ts:50-93`; `.repo/pi-0.84.1/packages/coding-agent/src/core/auth-storage.ts:1-10`; `.repo/pi-0.84.1/packages/coding-agent/src/core/auth-storage.ts:23-64`; `.repo/pi-0.84.1/packages/coding-agent/src/core/auth-storage.ts:95-113`). A stored credential owns its provider; ambient authentication is consulted only when no credential is stored. OAuth refresh uses a five-minute skew, cross-process `modify`, a double-check, and a 15-second timeout (`.repo/pi-0.84.1/packages/ai/src/auth/resolve.ts:44-109`; `.repo/pi-0.84.1/packages/ai/src/auth/resolve.ts:119-165`). Seven text OAuth loaders are present: Anthropic, OpenAI Codex, GitHub Copilot, OpenRouter, Kimi Coding, xAI, and Radius (`.repo/pi-0.84.1/packages/ai/src/auth/oauth/load.ts:14-21`; `.repo/pi-0.84.1/packages/ai/src/auth/oauth/load.ts:31-67`). CLI surfaces include auth checking and optional credential, API-key, or bearer-token output (`.repo/pi-0.84.1/packages/coding-agent/src/cli/auth-command.ts:4-24`; `.repo/pi-0.84.1/packages/coding-agent/src/cli/auth-check.ts:22-63`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| 2.1 Persistence backend | Stores credential envelopes in the OS keychain (`crates/opi-ai/src/credential.rs:39-70`). | Stores credentials in lock-serialized, mode-`0600` JSON (`.repo/pi-0.84.1/packages/coding-agent/src/core/auth-storage.ts:1-10`; `.repo/pi-0.84.1/packages/coding-agent/src/core/auth-storage.ts:23-64`). | Native keychain storage versus an application JSON credential file. | confirmed |
| 2.2 OAuth-bearing providers | Registers three production OAuth providers (`crates/opi-coding-agent/src/oauth.rs:2386-2425`). | Provides seven text-provider OAuth loaders (`.repo/pi-0.84.1/packages/ai/src/auth/oauth/load.ts:14-21`). | Three registered OAuth providers versus seven loader-backed providers. | confirmed |
| 2.3 Credential inspectability | Exposes redacted presence diagnostics and login/logout. Exact absence search `rg -n -i -e "auth check" -e "print-api-key" -e "print-bearer-token" -e "--credential" -e "--api-key" -e "--bearer-token" crates/opi-coding-agent/src crates/opi-ai/src` found no credential-output command (`crates/opi-ai/src/credential.rs:101-118`; `crates/opi-coding-agent/src/doctor.rs:413-453`). | `auth check` can optionally emit a credential, API key, or bearer token (`.repo/pi-0.84.1/packages/coding-agent/src/cli/auth-command.ts:4-24`; `.repo/pi-0.84.1/packages/coding-agent/src/cli/auth-check.ts:22-63`). | Redacted status-only inspection versus optional credential-bearing output. | confirmed |
| 2.4 Ambient fallback after stored state | Falls back to environment after store absence or backend unavailability, but not after a stored-type mismatch (`crates/opi-coding-agent/src/credential_store.rs:1066-1103`). | Consults ambient authentication only when no credential is stored; stored-type and refresh failures propagate (`.repo/pi-0.84.1/packages/ai/src/auth/resolve.ts:44-109`). | Different fallback behavior after unavailable storage; both preserve stored-credential ownership when stored state exists. | confirmed |

## 3. Provider catalog

**opi:** The fixed text-provider list contains 11 ids: Anthropic, OpenAI, OpenRouter, Mistral, OpenAI Responses, Gemini, Bedrock, Azure, Vertex, GitHub Copilot, and OpenAI Codex (`crates/opi-coding-agent/src/provider_factory.rs:353-366`). Collection metadata marks OpenAI, OpenRouter, and Mistral as OpenAI-compatible profiles (`crates/opi-coding-agent/src/provider_factory.rs:1960-1977`). Configuration also supports OpenAI-compatible profiles and custom mapped providers. Compatible profiles use the OpenAI Completions wire; custom mapped providers admit Anthropic Messages, OpenAI Completions, and OpenAI Responses (`crates/opi-coding-agent/src/config.rs:250-270`; `crates/opi-coding-agent/src/config.rs:1457-1486`; `crates/opi-coding-agent/src/provider_factory.rs:1035-1130`). GitHub Copilot maps those same three wires within one provider (`crates/opi-coding-agent/src/provider_factory.rs:1297-1378`).

**pi:** `builtinProviders()` returns 40 text-provider factories; the image catalog separately adds OpenRouter (`.repo/pi-0.84.1/packages/ai/src/providers/all.ts:88-145`). `Api` names ten wire APIs and remains open to additional string APIs (`.repo/pi-0.84.1/packages/ai/src/types.ts:17-33`). `createProvider` dispatches either a single implementation or a per-model API map (`.repo/pi-0.84.1/packages/ai/src/models.ts:739-792`). GitHub Copilot maps Anthropic Messages, OpenAI Completions, and OpenAI Responses in one provider (`.repo/pi-0.84.1/packages/ai/src/providers/github-copilot.ts:9-33`). Seven text-provider OAuth loaders are present (`.repo/pi-0.84.1/packages/ai/src/auth/oauth/load.ts:14-21`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| 3.1 Built-in breadth | Enumerates 11 built-in text-provider ids (`crates/opi-coding-agent/src/provider_factory.rs:353-366`). | Constructs 40 built-in text providers (`.repo/pi-0.84.1/packages/ai/src/providers/all.ts:88-131`). | Different fixed text-provider catalog sizes. | confirmed |
| 3.2 Configurable wire breadth | Restricts custom mapped providers to three wires and compatible profiles to OpenAI Completions (`crates/opi-coding-agent/src/config.rs:1457-1486`; `crates/opi-coding-agent/src/provider_factory.rs:1035-1069`). | Names ten known APIs, permits additional string APIs, and maps implementations by `model.api` (`.repo/pi-0.84.1/packages/ai/src/types.ts:17-33`; `.repo/pi-0.84.1/packages/ai/src/models.ts:739-792`). | Closed three-wire custom mapping versus an open API identifier and implementation map. | confirmed |
| 3.3 OAuth-bearing catalog breadth | Has three OAuth-bearing built-ins (`crates/opi-coding-agent/src/oauth.rs:2386-2425`). | Has seven text-provider OAuth loaders (`.repo/pi-0.84.1/packages/ai/src/auth/oauth/load.ts:14-21`). | Different OAuth-bearing provider counts. | confirmed |
| 3.4 Multiple APIs within one provider | GitHub Copilot maps three wires; custom configuration rejects wires outside its three admitted values (`crates/opi-coding-agent/src/provider_factory.rs:1297-1378`; `crates/opi-coding-agent/src/config.rs:1457-1486`). | GitHub Copilot maps the same three wires; the general API type additionally names Mistral Conversations, Azure OpenAI Responses, and pi-messages (`.repo/pi-0.84.1/packages/ai/src/providers/github-copilot.ts:9-33`; `.repo/pi-0.84.1/packages/ai/src/types.ts:17-33`). | Shared three-wire Copilot mapping with different general configurable API sets. | confirmed |

## 4. Stream and transport lifecycle

**opi:** The stream protocol emits start, text/thinking/tool-call start-delta-end events, and a done or error terminal event. Assistant messages retain response model/id, usage, stop reason, and error (`crates/opi-ai/src/stream.rs:314-383`; `crates/opi-ai/src/message.rs:27-38`). Requests carry cancellation, timeout, extra headers, cache retention, and session id; usage includes cache/reasoning subsets and cost calculation (`crates/opi-ai/src/provider.rs:67-114`; `crates/opi-ai/src/stream.rs:32-108`; `crates/opi-ai/src/stream.rs:300-311`). The agent retries typed retryable errors only before stream content is delivered, uses rate-limit delay with cancellable sleep, and selects cancellation against stream progress (`crates/opi-agent/src/agent_loop.rs:89-150`; `crates/opi-agent/src/agent_loop.rs:399-455`). Proxy handling supports explicit or environment HTTP(S) proxy values and `NO_PROXY` (`crates/opi-ai/src/http.rs:16-27`; `crates/opi-ai/src/http.rs:161-218`). Known unsupported image input and unmapped thinking levels fail request-capability preflight (`crates/opi-ai/src/provider.rs:234-307`).

**pi:** The event protocol has the same start and text/thinking/tool-call event shape and done/error terminals. Assistant messages additionally retain diagnostics, a deferred handle, and raw stop reason (`.repo/pi-0.84.1/packages/ai/src/types.ts:368-427`; `.repo/pi-0.84.1/packages/ai/src/types.ts:510-532`). Request options add custom fetch, provider-scoped environment values, payload/response callbacks, retry settings, and SSE/WebSocket selection to cancellation, timeout, headers, cache retention, and session id (`.repo/pi-0.84.1/packages/ai/src/types.ts:103-206`). Provider-request retry uses status/header classification, server delay or jittered exponential backoff, and abortable sleep; the coding agent separately retries retryable terminal assistant errors (`.repo/pi-0.84.1/packages/ai/src/utils/provider-retry.ts:22-124`; `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:2641-2649`; `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:2682-2735`). Proxy resolution honors request/process environment values and `NO_PROXY` and rejects non-HTTP(S) proxy schemes (`.repo/pi-0.84.1/packages/ai/src/utils/node-http-proxy.ts:37-111`). Unsupported images become text placeholders and unsupported thinking levels are clamped (`.repo/pi-0.84.1/packages/ai/src/api/transform-messages.ts:12-56`; `.repo/pi-0.84.1/packages/ai/src/models.ts:900-931`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| 4.1 Transport and request interception | Request state covers cancellation, timeout, headers, cache, and session affinity. Exact absence search `rg -n -i -e "websocket" -e "DeferredHandle" -e "fetchDeferred" -e "cancelDeferred" -e "onPayload" crates/opi-ai/src crates/opi-agent/src crates/opi-coding-agent/src` returned no matches (`crates/opi-ai/src/provider.rs:86-114`). | Exposes SSE/WebSocket selection, fetch injection, provider environment values, and payload/response callbacks (`.repo/pi-0.84.1/packages/ai/src/types.ts:103-206`). | Different per-request transport and interception surfaces. | confirmed |
| 4.2 Deferred lifecycle | Provider and stream terminal types have no deferred operations or deferred handle (`crates/opi-ai/src/provider.rs:22-60`; `crates/opi-ai/src/stream.rs:373-383`). | Models deferred intent and terminal handles and supports provider/collection fetch and cancel (`.repo/pi-0.84.1/packages/ai/src/types.ts:303-309`; `.repo/pi-0.84.1/packages/ai/src/types.ts:391-425`; `.repo/pi-0.84.1/packages/ai/src/models.ts:706-731`). | No deferred lifecycle versus modeled fetch/cancel continuation. | confirmed |
| 4.3 Retry layers | Retries provider stream errors in the agent loop only before the first delivered content (`crates/opi-agent/src/agent_loop.rs:124-150`; `crates/opi-agent/src/agent_loop.rs:399-455`). | Retries at the provider-request layer and again in the coding agent after retryable terminal assistant errors (`.repo/pi-0.84.1/packages/ai/src/utils/provider-retry.ts:75-124`; `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:2641-2649`; `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:2682-2735`). | One pre-delivery stream retry layer versus request-adapter and terminal-message layers. | confirmed |
| 4.4 Capability coercion | Rejects known unsupported image input and unmapped thinking levels before the network call (`crates/opi-ai/src/provider.rs:234-307`; `crates/opi-agent/src/agent_loop.rs:89-110`). | Replaces unsupported images with text and selects a supported thinking level (`.repo/pi-0.84.1/packages/ai/src/api/transform-messages.ts:12-56`; `.repo/pi-0.84.1/packages/ai/src/models.ts:900-931`). | Preflight failure versus request coercion. | confirmed |
| 4.5 Cache default at the agent call site | Sets `CacheRetention::None`, which preserves provider defaults (`crates/opi-agent/src/agent_loop.rs:89-105`; `crates/opi-ai/src/provider.rs:67-83`). | Resolves an omitted retention to `short` unless the environment requests `long` (`.repo/pi-0.84.1/packages/ai/src/api/anthropic-messages.ts:49-56`; `.repo/pi-0.84.1/packages/ai/src/api/openai-completions.ts:190-197`; `.repo/pi-0.84.1/packages/ai/src/api/openai-responses.ts:57-64`; `.repo/pi-0.84.1/packages/ai/src/api/bedrock-converse-stream.ts:697-704`). | Provider-default retention hint versus an explicit short default in provider adapters. | confirmed |

## 5. Agent loop and harness

**opi:** The low-level loop transforms and converts context, streams a provider, batches tool calls sequentially when any tool requires it, invokes before/after hooks, checks turn termination, prepares the next turn, and polls steering/follow-up queues (`crates/opi-agent/src/agent_loop.rs:37-105`; `crates/opi-agent/src/agent_loop.rs:146-220`; `crates/opi-agent/src/agent_loop.rs:351-397`; `crates/opi-agent/src/agent_loop.rs:559-604`). `AgentHooks` has six methods. Stateful `Agent` owns provider, tools, model, config, hooks, cancellation, messages, subscribers, and steering/follow-up queues before calling the loop (`crates/opi-agent/src/hooks.rs:56-105`; `crates/opi-agent/src/agent.rs:90-107`; `crates/opi-agent/src/agent.rs:390-414`). Generic `AgentHarness` owns phase guards, ordered pending writes, save points, snapshots, and abort/flush, but has no prompt, continue, or step method (`crates/opi-agent/src/harness.rs:3-22`; `crates/opi-agent/src/harness.rs:113-173`; `crates/opi-agent/src/harness.rs:333-525`). The product path is `CodingHarness.prompt` to `Agent.prompt` (`crates/opi-coding-agent/src/harness.rs:414-432`; `crates/opi-coding-agent/src/harness.rs:1953-1975`).

**pi:** `runAgentLoop` and `runAgentLoopContinue` share a loop that injects steering, streams, executes tools, prepares next-turn state, checks stop, and drains follow-ups; stateful `Agent` calls those functions (`.repo/pi-0.84.1/packages/agent/src/agent-loop.ts:95-143`; `.repo/pi-0.84.1/packages/agent/src/agent-loop.ts:153-274`; `.repo/pi-0.84.1/packages/agent/src/agent.ts:409-433`). Generic `AgentHarness` declares prompt, skill, compaction, navigation, resume, abort, queue, manual-drive, model/thinking/tool, and session APIs. Turn/action methods reject with `HarnessNotImplemented`; creation rejects restoration when the durable session contains at least one record, while model/thinking/active-tool getters and setters are implemented (`.repo/pi-0.84.1/packages/agent/src/harness/agent-harness.ts:271-303`; `.repo/pi-0.84.1/packages/agent/src/harness/agent-harness.ts:347-441`). The coding product creates `Agent` and `AgentSession`; `AgentSession` calls `agent.prompt` and `agent.continue` (`.repo/pi-0.84.1/packages/coding-agent/src/core/sdk.ts:292-389`; `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:1063-1069`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| 5.1 Generic harness implementation extent | Implements phase transitions, ordered pending writes, save points, and abort/flush, with no turn-driving method (`crates/opi-agent/src/harness.rs:3-22`; `crates/opi-agent/src/harness.rs:113-173`; `crates/opi-agent/src/harness.rs:333-525`). | Declares a broader lane/turn/navigation surface; turn/action methods reject as unimplemented, and creation rejects restoration when at least one session record exists (`.repo/pi-0.84.1/packages/agent/src/harness/agent-harness.ts:271-303`; `.repo/pi-0.84.1/packages/agent/src/harness/agent-harness.ts:347-420`). | Implemented persistence/lifecycle seam without turn driving versus a broader declared surface whose turn path and record-bearing restoration reject. | refined |
| 5.2 Product routing | Product prompting routes from `CodingHarness` to stateful `Agent` (`crates/opi-coding-agent/src/harness.rs:414-432`; `crates/opi-coding-agent/src/harness.rs:1953-1975`). | Product prompting routes from `AgentSession` to stateful `Agent` (`.repo/pi-0.84.1/packages/coding-agent/src/core/sdk.ts:292-389`; `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:1063-1069`). | Neither product turn path uses its generic harness. | confirmed |
| 5.3 Loop callback surface | One six-method trait contains transform, conversion, before/after tool, stop, and next-turn preparation (`crates/opi-agent/src/hooks.rs:56-105`). | Loop config contains those callbacks plus steering/follow-up polling and mutable next-turn context/model/thinking snapshots (`.repo/pi-0.84.1/packages/agent/src/types.ts:149-292`; `.repo/pi-0.84.1/packages/agent/src/agent-loop.ts:226-267`). | Pi exposes queue pollers and next-turn state replacement in addition to the shared callback categories. | confirmed |
| 5.4 Next-turn callback ordering | Evaluates `should_stop_after_turn` after `turn_end` and returns before `prepare_next_turn` (`crates/opi-agent/src/agent_loop.rs:351-397`; `crates/opi-agent/src/agent_loop.rs:559-568`). | Invokes `prepareNextTurn`, applies context/model/thinking updates, then invokes `shouldStopAfterTurn` with the updated context (`.repo/pi-0.84.1/packages/agent/src/agent-loop.ts:224-257`). | Stop-before-prepare ordering versus prepare-before-stop ordering. | added |

## 6. Session model

**opi:** One append-only JSONL v1 format is used by generic and product session seams. Its header carries cwd and optional parent session; entries are message, compaction, durable leaf pointer, extension state, session info, model/thinking change, label, and branch summary (`crates/opi-agent/src/session.rs:1-14`; `crates/opi-agent/src/session.rs:49-83`; `crates/opi-agent/src/session.rs:88-208`). The reader requires version 1, skips and counts unknown or corrupt entries, and treats a non-newline-terminated final line as truncated (`crates/opi-agent/src/session.rs:400-505`). Forking copies entries on the reconstructed active branch, records the source id in `parent_session`, and writes one fresh leaf pointer (`crates/opi-coding-agent/src/session_cli.rs:196-267`). Compaction records first-kept id and token counts; reconstruction follows the stored tree and active pointer (`crates/opi-agent/src/session.rs:100-119`; `crates/opi-agent/src/session_context.rs:1-35`). Generic storage includes append/count `HarnessSession`, append/load/count `SessionRepo`, and `SessionFacade` for ordered writes, recovery-aware loads, and active-tip reads (`crates/opi-agent/src/harness.rs:293-329`; `crates/opi-agent/src/harness.rs:614-699`; `crates/opi-agent/src/harness.rs:729-915`).

**pi:** The coding product uses append-only `SessionManager` v3 with message, thinking/model change, compaction, branch summary, custom state, custom message, label, and session-info entries. Migration v1 to v2 adds tree ids/parents, and v2 to v3 renames the old hook-message role (`.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:30-153`; `.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:230-295`). Active context walks parent ids from a selected or current leaf and projects message, custom-message, branch-summary, and compaction entries (`.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:334-359`; `.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:379-407`). The product leaf pointer is runtime state; the next append forms a branch. `forkFrom` copies every non-header source entry and records the source path as parent (`.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:1195-1200`; `.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:1354-1374`; `.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:1579-1627`). Separately, pi-agent defines generic JSONL v4 storage and repository interfaces with lanes, active-tool changes, operation/tool/queue/deferred-write/usage records, queries, list/delete/fork, and JSONL plus in-memory implementations (`.repo/pi-0.84.1/packages/agent/src/harness/session/jsonl/types.ts:46-55`; `.repo/pi-0.84.1/packages/agent/src/harness/session/types.ts:14-74`; `.repo/pi-0.84.1/packages/agent/src/harness/session/types.ts:80-212`; `.repo/pi-0.84.1/packages/agent/src/harness/session/types.ts:279-352`; `.repo/pi-0.84.1/packages/agent/src/harness/session/memory.ts:25-36`; `.repo/pi-0.84.1/packages/agent/src/harness/session/memory.ts:148-187`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| 6.1 Format topology and versions | Uses one v1 JSONL format through its generic and product seams (`crates/opi-agent/src/session.rs:49-83`; `crates/opi-agent/src/harness.rs:293-329`; `crates/opi-agent/src/harness.rs:614-687`). | Uses product `SessionManager` v3 and separately defines generic JSONL v4 (`.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:30-38`; `.repo/pi-0.84.1/packages/agent/src/harness/session/jsonl/types.ts:46-55`). | One shared v1 format versus distinct product-v3 and generic-v4 formats. | confirmed |
| 6.2 Entry and record coverage | Has durable `leaf` and `extension_state` entries and no dedicated `custom_message` entry (`crates/opi-agent/src/session.rs:33-38`; `crates/opi-agent/src/session.rs:100-128`; `crates/opi-agent/src/session.rs:193-208`). | Product v3 has distinct `custom` and `custom_message` entries and usage/details fields; generic v4 also records active tools, operations, tool starts, queues, deferred writes, usage, and lane pointers (`.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:69-153`; `.repo/pi-0.84.1/packages/agent/src/harness/session/types.ts:39-74`; `.repo/pi-0.84.1/packages/agent/src/harness/session/types.ts:80-212`; `.repo/pi-0.84.1/packages/agent/src/harness/session/types.ts:262-272`). | Different durable entry sets; pi generic v4 also separates entries from operational lane records. | confirmed |
| 6.3 Migration policy | Requires header version 1 and handles additive unknown tags by skipping and counting them (`crates/opi-agent/src/session.rs:423-505`). | Treats a missing version as v1 and migrates v1 to v2 to v3 (`.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:230-295`; `.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:895-921`). | Exact-version plus additive-tag handling versus explicit version migrations. | confirmed |
| 6.4 Leaves, branches, and forks | Persists leaf-pointer entries and forks only the reconstructed active branch (`crates/opi-agent/src/session.rs:112-119`; `crates/opi-coding-agent/src/session_cli.rs:196-267`). | Moves the product leaf pointer in memory and `forkFrom` copies the complete source tree (`.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:1195-1200`; `.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:1354-1374`; `.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:1579-1627`). | Durable active-pointer records and active-branch fork versus runtime pointer state and whole-tree fork. | confirmed |
| 6.5 Storage abstraction | Provides append/count `HarnessSession`, append/load/count `SessionRepo`, and a v1-JSONL `SessionFacade` with ordered writes, recovery-aware load, and active-tip access (`crates/opi-agent/src/harness.rs:293-329`; `crates/opi-agent/src/harness.rs:614-699`; `crates/opi-agent/src/harness.rs:729-915`). | Generic v4 `SessionStorage` and `SessionRepo` provide lanes, entry/record queries, facts/stats, list/delete/fork, and JSONL plus memory implementations; the product separately uses v3 (`.repo/pi-0.84.1/packages/agent/src/harness/session/types.ts:279-352`; `.repo/pi-0.84.1/packages/agent/src/harness/session/memory.ts:25-36`; `.repo/pi-0.84.1/packages/agent/src/harness/session/memory.ts:148-187`; `.repo/pi-0.84.1/packages/agent/src/harness/session/jsonl/storage.ts:48-76`; `.repo/pi-0.84.1/packages/agent/src/harness/session/jsonl/repo.ts:36-109`; `.repo/pi-0.84.1/packages/coding-agent/src/core/sdk.ts:292-389`). | Opi's load/append/active-tip facade versus pi's broader lane, query, lifecycle, and backend repository interfaces. | refined |
| 6.6 Tail and malformed-line recovery | Skips any non-newline-terminated final data line and reports truncated, corrupt, and unknown counts (`crates/opi-agent/src/session.rs:400-505`). | Retains a valid final JSON entry without a trailing newline and silently skips malformed lines (`.repo/pi-0.84.1/packages/coding-agent/src/core/session-manager.ts:503-555`). | Different treatment and reporting of final and malformed lines. | added |

## 7. Extension and plugin surface

**opi:** The compiled `Extension` trait exposes tools, providers, model overrides, loop hooks, commands, events, and serialized state. `ExtensionRegistry` accepts registrations before loop use and rejects registration after its extension vector is shared (`crates/opi-agent/src/extension.rs:175-218`; `crates/opi-agent/src/extension.rs:220-310`; `crates/opi-agent/src/extension.rs:332-377`). The product can start installed `process-jsonl` packages at startup and bridge them into that registry, but the process adapter's advertised subset is tools, commands, hooks/events, state, and model overrides rather than compiled-extension provider registration (`crates/opi-coding-agent/src/adapter_extension.rs:209-237`; `crates/opi-coding-agent/src/adapter_host.rs:100-110`; `crates/opi-coding-agent/src/adapter_extension.rs:783-899`).

- No extension UI/message-renderer, shortcut, flag, widget, header, or footer injection point was found: searched `crates/opi-agent/src`, `crates/opi-coding-agent/src`, and `crates/opi-tui/src` for `registerMessageRenderer|registerEntryRenderer|registerShortcut|registerFlag|setWidget|setFooter|setHeader|registerToolRenderer|custom_tool_renderer`.
- Subagent orchestration is outside core. The package directory contains metadata and documentation; the in-process child-agent implementation is an integration-test example (`examples/sub-agent/README.md:3-14`; `examples/sub-agent/README.md:31-42`; `crates/opi-coding-agent/tests/sub_agent_example.rs:1-28`).

**pi:** Pi imports TypeScript extension modules through Jiti and executes their factories in process (`.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/loader.ts:436-463`; `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/loader.ts:490-511`). Its API covers lifecycle/session events, tools, commands, shortcuts, flags, message/entry/Markdown and per-tool renderers, direct session actions, providers, model changes, and interactive UI controls (`.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:96-265`; `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:489-500`; `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:1198-1390`). Tool registration refreshes the bound tool list immediately; provider registration is queued during initial loading and applies immediately after runtime binding (`.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/loader.ts:249-271`; `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:1364-1375`).

- Its subagent is a runnable example extension that starts separate `pi` processes (`.repo/pi-0.84.1/packages/coding-agent/examples/extensions/subagent/README.md:1-12`; `.repo/pi-0.84.1/packages/coding-agent/examples/extensions/subagent/index.ts:448-472`). No core subagent source was found by searching `.repo/pi-0.84.1/packages/coding-agent/src` for `subagent|sub-agent`.
- No out-of-process extension adapter was found: searched `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions`, `core/resource-loader.ts`, and `core/package-manager.ts` for `process-jsonl|extension-jsonl|out-of-process|adapter protocol`.

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Extension API breadth | The compiled trait covers tools, providers, model overrides, loop hooks, commands/events, and state; the process adapter exposes the subset tools, commands, hooks/events, state, and model overrides (`crates/opi-agent/src/extension.rs:175-310`; `crates/opi-coding-agent/src/adapter_extension.rs:209-237`). | Exposes those categories plus shortcuts, flags, Markdown/message/entry and custom-tool renderers, direct session actions, and interactive UI controls (`.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:96-265`; `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:489-500`; `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:1198-1390`). | The compiled Opi trait and its process adapter have different breadth; Pi's in-process API additionally includes registration, rendering, session-action, and UI categories. | refined |
| Loading and registration timing | Registers compiled extensions and startup process adapters before the loop into a registry that rejects registration once its wrappers share the extension vector (`crates/opi-agent/src/extension.rs:332-377`; `crates/opi-coding-agent/src/adapter_extension.rs:783-899`). | Executes TypeScript modules/factories in process; `registerTool` refreshes immediately, while provider registrations queue during initial load and apply immediately after runtime binding (`.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/loader.ts:249-271`; `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/loader.ts:436-511`; `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:1364-1375`). | Pre-loop registry population and locking versus an in-process runtime with immediate tool refresh and phase-dependent provider registration. | refined |
| Process boundary | Defines a package `process-jsonl` subprocess adapter using `opi-extension-jsonl-v1` (`crates/opi-coding-agent/src/adapter_extension.rs:783-803`). | No corresponding adapter exists under the recorded extension/resource/package-manager search. | Opi has a versioned subprocess extension boundary; Pi's measured extension loader is in-process. | confirmed |
| Subagent placement and mechanism | Provides package metadata/documentation plus an integration-test example that constructs fresh in-process `Agent` instances (`examples/sub-agent/README.md:31-42`; `crates/opi-coding-agent/tests/sub_agent_example.rs:23-28`). | Provides a runnable example TypeScript extension that starts separate `pi` processes (`.repo/pi-0.84.1/packages/coding-agent/examples/extensions/subagent/README.md:7-12`; `.repo/pi-0.84.1/packages/coding-agent/examples/extensions/subagent/index.ts:448-472`). | Both place subagent orchestration outside core, with an in-process Rust test example versus a runnable multi-process TypeScript example. | refined |

## 8. Built-in tools

**opi:** Opi defines `read`, `write`, `edit`, `bash`, `grep`, `find`, `ls`, and `glob`. Interactive mode defaults to the coding set; non-interactive/RPC defaults to read-only navigation and requires explicit mutating opt-in for `write`, `edit`, or `bash` (`crates/opi-coding-agent/src/policy.rs:6-24`; `crates/opi-coding-agent/src/policy.rs:39-75`). File and shell behavior is replaceable through `FileOperations` and `BashOperations` (`crates/opi-coding-agent/src/tool/operations.rs:301-359`).

- `write`, `edit`, and `bash` are sequential tools, and any batch containing a sequential call runs wholly sequentially (`crates/opi-coding-agent/src/tool/write.rs:225-226`; `crates/opi-coding-agent/src/tool/edit.rs:352-353`; `crates/opi-coding-agent/src/tool/bash.rs:294-295`; `crates/opi-agent/src/agent_loop.rs:187-195`).
- File tools use canonical root/symlink policy. Interactive `read` may leave the workspace while non-interactive `read` is confined; navigation uses internal walkers and result/work/byte caps (`crates/opi-coding-agent/src/tool/mod.rs:37-113`; `crates/opi-coding-agent/src/harness.rs:2553-2562`; `crates/opi-coding-agent/src/tool/mod.rs:201-242`).

**pi:** Pi defines `read`, `bash`, `edit`, `write`, `grep`, `find`, and `ls`; its product SDK defaults to the four-tool coding set and exposes allow/exclude/disable controls (`.repo/pi-0.84.1/packages/coding-agent/src/core/tools/index.ts:81-153`; `.repo/pi-0.84.1/packages/coding-agent/src/core/sdk.ts:245-251`; `.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:118-131`). No separate mutation opt-in was found by searching `.repo/pi-0.84.1/packages/coding-agent/src` for `allow-mutating|allowMutating|mutation opt-in`. Individual tool constructors accept replaceable operations (`.repo/pi-0.84.1/packages/coding-agent/src/core/tools/read.ts:208-214`; `.repo/pi-0.84.1/packages/coding-agent/src/core/tools/bash.ts:59-102`).

- Tool batches default to parallel. Edit/write calls are serialized only when their resolved real-path key matches; calls on different keys and unmarked `bash` calls can overlap (`.repo/pi-0.84.1/packages/agent/src/types.ts:259-268`; `.repo/pi-0.84.1/packages/agent/src/agent-loop.ts:411-425`; `.repo/pi-0.84.1/packages/coding-agent/src/core/tools/file-mutation-queue.ts:16-60`; `.repo/pi-0.84.1/packages/coding-agent/src/core/tools/edit.ts:300-320`; `.repo/pi-0.84.1/packages/coding-agent/src/core/tools/write.ts:195-220`).
- Paths preserve absolute input and otherwise resolve from cwd. Read/navigation output is capped, and missing `rg`/`fd` binaries can be provisioned (`.repo/pi-0.84.1/packages/coding-agent/src/core/tools/path-utils.ts:40`; `.repo/pi-0.84.1/packages/coding-agent/src/core/tools/read.ts:208-217`; `.repo/pi-0.84.1/packages/coding-agent/src/core/tools/ls.ts:100-108`; `.repo/pi-0.84.1/packages/coding-agent/src/core/tools/find.ts:123-131`; `.repo/pi-0.84.1/packages/coding-agent/src/core/tools/grep.ts:128-136`; `.repo/pi-0.84.1/packages/coding-agent/src/utils/tools-manager.ts:242-326`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Built-in set | Defines eight tools, including `glob` (`crates/opi-coding-agent/src/policy.rs:18-24`). | Defines seven tools without a distinct `glob` (`.repo/pi-0.84.1/packages/coding-agent/src/core/tools/index.ts:81-153`). | Opi has a distinct `glob` built-in in addition to Pi's seven-tool set. | confirmed |
| Mode and mutation policy | Uses a read-only non-interactive/RPC default and gates mutating tools (`crates/opi-coding-agent/src/policy.rs:39-75`). | Defaults the SDK to `read,bash,edit,write` and offers selection/removal flags; no separate mutation gate exists under the recorded search (`.repo/pi-0.84.1/packages/coding-agent/src/core/sdk.ts:245-251`; `.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:118-131`). | Mode-specific read-only defaults plus mutating opt-in versus a shared coding default with selection controls. | confirmed |
| Mutation scheduling | Marks write/edit/bash sequential, and any batch containing one runs wholly sequentially (`crates/opi-agent/src/agent_loop.rs:187-195`; `crates/opi-coding-agent/src/tool/write.rs:225-226`; `crates/opi-coding-agent/src/tool/edit.rs:352-353`; `crates/opi-coding-agent/src/tool/bash.rs:294-295`). | Defaults batches to parallel and queues edit/write only by matching resolved real-path key; `bash` is not marked sequential (`.repo/pi-0.84.1/packages/agent/src/types.ts:259-268`; `.repo/pi-0.84.1/packages/coding-agent/src/core/tools/file-mutation-queue.ts:16-60`). | Whole-batch serialization around any mutation versus per-file edit/write serialization within an otherwise parallel batch. | refined |
| Path and binary handling | Applies canonical workspace/symlink policy, allows interactive external reads while confining non-interactive reads, uses internal walkers, and bounds navigation work/results/bytes (`crates/opi-coding-agent/src/tool/mod.rs:37-113`; `crates/opi-coding-agent/src/harness.rs:2553-2562`; `crates/opi-coding-agent/src/tool/mod.rs:201-242`). | Preserves absolute paths without an equivalent workspace-root gate, caps read/navigation output, and can provision missing `rg`/`fd` (`.repo/pi-0.84.1/packages/coding-agent/src/core/tools/path-utils.ts:40`; `.repo/pi-0.84.1/packages/coding-agent/src/core/tools/read.ts:208-217`; `.repo/pi-0.84.1/packages/coding-agent/src/utils/tools-manager.ts:242-326`). | Different root-boundary policies and binary strategies; both cap result/output size, while Opi defines shared visited-entry and cumulative-read-byte bounds. | refined |

## 9. Terminal UI

**opi:** Opi uses ratatui/crossterm and exports Rust modules for chat, Markdown, editor, diff, image, picker, tree, and keybindings; the coding product owns the terminal event/render loop and model/session/branch/tree pickers (`crates/opi-tui/Cargo.toml:16-17`; `crates/opi-tui/src/lib.rs:1-41`; `crates/opi-coding-agent/src/interactive.rs:1`; `crates/opi-coding-agent/src/picker.rs:14`). Image rendering names Kitty, iTerm2, Sixel, and fallback, but detection selects Kitty/iTerm2/fallback and Sixel emits an empty string (`crates/opi-tui/src/terminal_image.rs:7-14`; `crates/opi-tui/src/terminal_image.rs:61-87`; `crates/opi-tui/src/terminal_image.rs:123-129`). Themes are layered resources discovered on demand; configurable editor keybindings cover submit, abort, and newline (`crates/opi-coding-agent/src/theme_discovery.rs:1-29`; `crates/opi-tui/src/keybindings.rs:1-42`; `crates/opi-tui/src/keybindings.rs:162-200`).

**pi:** Pi's TUI uses a line-rendering `Component` interface and a `TUI` that schedules rendering and supports dynamic children and overlays. Its public catalog exports layout containers, input/editor, Markdown, scroll, image, selectors/settings, loaders, text, and terminal-screen implementations (`.repo/pi-0.84.1/packages/tui/src/tui.ts:20-47`; `.repo/pi-0.84.1/packages/tui/src/tui.ts:291-318`; `.repo/pi-0.84.1/packages/tui/src/tui.ts:545-580`; `.repo/pi-0.84.1/packages/tui/src/index.ts:1-41`). Interactive mode provides model/session/theme/tree selectors (`.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:131`; `.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:691`).

- Image protocol selection is Kitty or iTerm2 (`.repo/pi-0.84.1/packages/tui/src/terminal-image.ts:6-18`; `.repo/pi-0.84.1/packages/tui/src/terminal-image.ts:87-110`). Built-in/custom JSON themes can be registered and watched for live reload (`.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/theme/theme.ts:450-501`; `.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/theme/theme.ts:856-930`).
- Extensions can register shortcuts and message/entry/Markdown renderers and can inject widgets, header/footer, editor/autocomplete, and overlays (`.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:96-265`; `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:1259-1295`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Renderer and components | Exposes a fixed ratatui widget set composed by the coding-agent render loop (`crates/opi-tui/src/lib.rs:1-41`; `crates/opi-coding-agent/src/interactive.rs:1`). | Exposes a line-rendering `Component`/`TUI` interface with dynamic children/overlays and reusable layout, input, scroll, settings, and screen components (`.repo/pi-0.84.1/packages/tui/src/tui.ts:20-47`; `.repo/pi-0.84.1/packages/tui/src/tui.ts:291-318`; `.repo/pi-0.84.1/packages/tui/src/index.ts:1-41`). | Product-owned ratatui composition versus a public line-component/TUI catalog with dynamic composition operations. | refined |
| Image protocols | Implements Kitty and iTerm2; also names Sixel, but the detector does not select it and its renderer emits no data (`crates/opi-tui/src/terminal_image.rs:61-87`; `crates/opi-tui/src/terminal_image.rs:123-129`). | Implements Kitty and iTerm2 selection (`.repo/pi-0.84.1/packages/tui/src/terminal-image.ts:6-18`; `.repo/pi-0.84.1/packages/tui/src/terminal-image.ts:87-110`). | Both implement Kitty/iTerm2; Opi additionally contains an inactive Sixel variant. | confirmed |
| Themes and keybindings | Discovers layered TOML themes on demand and configures submit, abort, and newline (`crates/opi-coding-agent/src/theme_discovery.rs:1-29`; `crates/opi-tui/src/keybindings.rs:1-42`; `crates/opi-tui/src/keybindings.rs:162-200`). | Registers and watches built-in/custom JSON themes and exposes the TUI key layer plus extension shortcut registration (`.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/theme/theme.ts:450-501`; `.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/theme/theme.ts:856-930`; `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:1259-1269`). | On-demand layered TOML themes and three configurable editor actions versus watched JSON themes and extension shortcuts. | confirmed |
| Extension injection | No UI/render injection API exists under the recorded Opi search. | Exposes widgets, header/footer, editor/autocomplete, overlays, shortcuts, and message/entry/Markdown renderers (`.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:96-265`; `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:1259-1295`). | Pi exposes extension UI/render injection points; Opi's measured extension surfaces do not. | confirmed |

## 10. Skills and prompt templates

**opi:** Opi parses `SKILL.md` frontmatter, including `disable-model-invocation`, and its registry can load bodies on demand. Discovery merges layered roots but scans only a root containing `SKILL.md` or its immediate child directories (`crates/opi-coding-agent/src/skill.rs:1-48`; `crates/opi-coding-agent/src/skill.rs:93-145`; `crates/opi-coding-agent/src/skill.rs:261-288`; `crates/opi-coding-agent/src/skill.rs:320-371`). `FRAGMENT.md` resources declare named/default arguments and support substitution through `FragmentRegistry` (`crates/opi-coding-agent/src/prompt_fragment.rs:1-45`; `crates/opi-coding-agent/src/prompt_fragment.rs:500-630`).

- The harness converts discovered skills/fragments to name/description metadata and appends that metadata to the product system prompt and RPC resource catalog. This conversion drops `disable-model-invocation` (`crates/opi-coding-agent/src/harness.rs:508-568`; `crates/opi-coding-agent/src/harness.rs:1231-1271`; `crates/opi-coding-agent/src/harness.rs:2776-2805`).
- No product caller of skill body loading or fragment expansion was found: searched `crates/opi-coding-agent/src` outside `skill.rs` and `prompt_fragment.rs` for `SkillRegistry|FragmentRegistry|expand_fragment_body|load_body(`.

**pi:** Pi parses skills from user, project, and explicit roots and recursively descends subdirectories, subject to ignore rules, until it finds `SKILL.md` roots (`.repo/pi-0.84.1/packages/coding-agent/src/core/skills.ts:155-170`; `.repo/pi-0.84.1/packages/coding-agent/src/core/skills.ts:173-265`; `.repo/pi-0.84.1/packages/coding-agent/src/core/skills.ts:277-317`). It filters `disable-model-invocation` skills from the system-prompt XML while retaining them for explicit `/skill:name` expansion (`.repo/pi-0.84.1/packages/coding-agent/src/core/skills.ts:327-360`; `.repo/pi-0.84.1/packages/coding-agent/src/core/system-prompt.ts:40-67`; `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:1304-1333`). Prompt-template Markdown uses shell-like tokenization plus `$1`, `$@`, `${@:N}`, and `${@:N:L}` substitutions, and `AgentSession` expands both skill and template commands (`.repo/pi-0.84.1/packages/coding-agent/src/core/prompt-templates.ts:20-101`; `.repo/pi-0.84.1/packages/coding-agent/src/core/prompt-templates.ts:177-210`; `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:1107-1164`; `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:2340-2363`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Template contract | `FRAGMENT.md` declares named/default arguments and substitutes them through `FragmentRegistry` (`crates/opi-coding-agent/src/prompt_fragment.rs:1-45`; `crates/opi-coding-agent/src/prompt_fragment.rs:500-630`). | Prompt-template Markdown substitutes shell-tokenized positional, all, and sliced arguments (`.repo/pi-0.84.1/packages/coding-agent/src/core/prompt-templates.ts:20-101`). | Named manifest arguments versus positional/all/sliced template arguments. | confirmed |
| System-prompt and explicit invocation wiring | Injects discovered skill/fragment names and descriptions into the product system prompt, but no product caller of body loading or fragment expansion exists under the recorded search (`crates/opi-coding-agent/src/harness.rs:536-568`; `crates/opi-coding-agent/src/harness.rs:1260-1271`; `crates/opi-coding-agent/src/harness.rs:2776-2805`). | Emits model-invocable skill metadata with file locations and expands `/skill:name` plus prompt-template commands in `AgentSession` (`.repo/pi-0.84.1/packages/coding-agent/src/core/skills.ts:327-360`; `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:1107-1164`; `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:1304-1333`). | Metadata-only product wiring versus system-prompt locations plus explicit runtime expansion. | refined |
| Placement | Keeps discovery and expansion APIs in the coding-agent product layer; discovery metadata reaches the prompt/RPC catalog, while body/expansion registries have no product caller (`crates/opi-coding-agent/src/skill.rs:1`; `crates/opi-coding-agent/src/prompt_fragment.rs:1`; `crates/opi-coding-agent/src/harness.rs:2346-2353`). | Keeps loaders and expanders in the coding-agent product layer and calls them from system-prompt construction and `AgentSession` (`.repo/pi-0.84.1/packages/coding-agent/src/core/system-prompt.ts:40-67`; `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:1107-1164`). | Both are product-layer facilities; only Pi directly calls body/template expansion from the measured session runtime. | refined |
| `disable-model-invocation` handling | Parses the flag, but product metadata conversion drops it and emits every discovered skill name/description (`crates/opi-coding-agent/src/skill.rs:93-145`; `crates/opi-coding-agent/src/harness.rs:2776-2785`). | Excludes flagged skills from the system prompt while retaining explicit `/skill:name` invocation (`.repo/pi-0.84.1/packages/coding-agent/src/core/skills.ts:305-360`; `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:1304-1333`). | The flag is parsed but not honored in Opi's product prompt metadata; Pi separates model visibility from explicit invocation. | added |
| Skill discovery depth | Scans a layer root containing `SKILL.md` or its immediate child directories (`crates/opi-coding-agent/src/skill.rs:320-371`). | Recursively descends subdirectories, subject to ignore rules, to find `SKILL.md` roots (`.repo/pi-0.84.1/packages/coding-agent/src/core/skills.ts:155-170`; `.repo/pi-0.84.1/packages/coding-agent/src/core/skills.ts:173-265`). | Root/immediate-child scanning versus recursive discovery. | added |

## 11. CLI modes

**opi:** Opi dispatches RPC first; otherwise `--non-interactive`, `--json`, or a nonempty positional prompt selects non-interactive execution, and no prompt selects the interactive TUI (`crates/opi-coding-agent/src/main.rs:130-184`). Its CLI exposes model listing, shell-completion generation, session list/resume/fork/delete, session export, top-level doctor, and package commands (`crates/opi-coding-agent/src/cli.rs:208-257`; `crates/opi-coding-agent/src/cli.rs:284-363`). Export supports Markdown or JSON plus tool-output, thinking, and redaction controls (`crates/opi-coding-agent/src/cli.rs:48-73`; `crates/opi-coding-agent/src/cli.rs:224-253`).

**pi:** Pi names output modes `text`, `json`, and `rpc`; app selection combines `--mode`, `--print`, and stdin/stdout TTY state (`.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:11-55`; `.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:80-122`; `.repo/pi-0.84.1/packages/coding-agent/src/main.ts:118-129`). Its CLI exposes model listing, continue/resume selection, session path/id, fork, session directory, ephemeral mode, and HTML export (`.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:87-116`; `.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:142-173`; `.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:260-291`; `.repo/pi-0.84.1/packages/coding-agent/src/main.ts:626-637`). Doctor, completion generation, and session list/delete flags were not found by searching `.repo/pi-0.84.1/packages/coding-agent/src` for `generate-completion|shell completion|list-sessions|delete-session|doctor`.

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Mode selection | Uses separate `--non-interactive`, `--json`, and `--rpc` switches, with positional prompts also selecting non-interactive execution (`crates/opi-coding-agent/src/main.rs:130-184`; `crates/opi-coding-agent/src/cli.rs:120-206`). | Uses `--mode text\|json\|rpc`, `--print`, and TTY-derived interactive selection (`.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:80-83`; `.repo/pi-0.84.1/packages/coding-agent/src/main.ts:118-129`). | Separate mode switches and prompt-derived selection versus one output-mode option plus print/TTY selection. | confirmed |
| Local operational commands | Exposes top-level doctor and shell-completion generation (`crates/opi-coding-agent/src/cli.rs:284-297`). | No corresponding command exists under the recorded coding-agent source search. | Opi exposes both local operational commands; Pi's measured CLI does not. | confirmed |
| Session commands | Exposes list, resume by id, fork, and delete (`crates/opi-coding-agent/src/cli.rs:208-222`; `crates/opi-coding-agent/src/main.rs:84-112`). | Exposes continue/resume selection, session path/id, fork, session directory, and ephemeral mode, but no list/delete flag under the recorded search (`.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:87-116`; `.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:262-268`). | Different session-management command sets; list/delete flags occur only in Opi. | confirmed |
| Export | Exports Markdown or JSON with thinking, tool-output, and redaction switches (`crates/opi-coding-agent/src/cli.rs:48-73`; `crates/opi-coding-agent/src/cli.rs:224-253`). | The CLI export flag writes HTML (`.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:288`; `.repo/pi-0.84.1/packages/coding-agent/src/main.ts:626-637`). | Different export formats and CLI disclosure controls. | confirmed |

## 12. Packaging and update

**opi:** `opi package` provides add, remove, list, doctor, enable, and disable. Package declarations can reference local paths or Git, and lock entries record the resolved Git commit when applicable, manifest SHA-256, and validated executable contribution material (`crates/opi-coding-agent/src/cli.rs:320-363`; `crates/opi-coding-agent/src/package_store.rs:75-150`; `crates/opi-coding-agent/src/package_store.rs:287-313`). Runtime resolution disables a package when its lock is missing or its manifest hash differs (`crates/opi-coding-agent/src/package_resolver.rs:365-395`; `crates/opi-coding-agent/src/package_resolver.rs:442-512`). Executable contributions additionally require machine-owned per-package trusted/enabled state and interactive confirmation (`crates/opi-coding-agent/src/package_activation.rs:88-109`; `crates/opi-coding-agent/src/package_activation.rs:195-230`; `crates/opi-coding-agent/src/package_cli.rs:451`).

- No package update/self-update variant was found: searched `crates/opi-coding-agent/src` for `PackageCommand::Update|SelfUpdate|self_update|self-update`. No gallery/marketplace surface was found there by searching for `gallery|marketplace`.

**pi:** Pi's top-level package CLI implements install, remove/uninstall, update, and list. Update can target self, all packages, one package, or model catalogs (`.repo/pi-0.84.1/packages/coding-agent/src/package-manager-cli.ts:33-35`; `.repo/pi-0.84.1/packages/coding-agent/src/package-manager-cli.ts:81-88`; `.repo/pi-0.84.1/packages/coding-agent/src/package-manager-cli.ts:150-179`). Sources include npm, Git/HTTPS/SSH, and local paths (`.repo/pi-0.84.1/packages/coding-agent/src/package-manager-cli.ts:111-128`; `.repo/pi-0.84.1/packages/coding-agent/src/core/package-manager.ts:1419-1443`). Self-update resolves a release plan and runs the installation command (`.repo/pi-0.84.1/packages/coding-agent/src/package-manager-cli.ts:476-505`; `.repo/pi-0.84.1/packages/coding-agent/src/package-manager-cli.ts:820-878`).

- Project-scoped package storage requires project trust (`.repo/pi-0.84.1/packages/coding-agent/src/core/package-manager.ts:978-1010`; `.repo/pi-0.84.1/packages/coding-agent/src/core/package-manager.ts:1714-1717`). No explicit manifest/executable hash or per-package trust record was found: searched `package-manager-cli.ts` and `core/package-manager.ts` for `manifest_sha256|manifestSha256|manifest hash|executable_sha256|executableSha256|executable hash|package-trust|package trust|per-package trust|checksum|integrity`; the only SHA-256 use there names a temporary directory (`.repo/pi-0.84.1/packages/coding-agent/src/core/package-manager.ts:2089-2095`).
- No gallery/marketplace surface was found by searching `.repo/pi-0.84.1/packages/coding-agent/src` for `gallery|marketplace`; npm is a package source instead (`.repo/pi-0.84.1/packages/coding-agent/src/package-manager-cli.ts:123`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Package command surface | Provides add/remove/list/doctor plus trust enable/disable; no update command exists under the recorded search (`crates/opi-coding-agent/src/cli.rs:320-363`). | Provides install/remove/update/list and update targets for self, packages, and models; no package doctor exists under the recorded search (`.repo/pi-0.84.1/packages/coding-agent/src/package-manager-cli.ts:33-35`; `.repo/pi-0.84.1/packages/coding-agent/src/package-manager-cli.ts:150-179`). | Different package verbs; Opi has doctor and activation controls, while Pi has package/model/self update targets. | confirmed |
| Sources and distribution | Accepts local and Git sources; no gallery/marketplace occurs under the recorded source search (`crates/opi-coding-agent/src/package_store.rs:75-150`). | Accepts npm, Git/HTTPS/SSH, and local sources; no gallery/marketplace occurs under the recorded source search (`.repo/pi-0.84.1/packages/coding-agent/src/package-manager-cli.ts:111-128`). | Pi additionally treats npm as a public package source; neither tree exposes a separate gallery/marketplace surface. | confirmed |
| Self-update | No self-update symbol or package update variant exists under the recorded search. | Resolves and executes self-update through the package CLI (`.repo/pi-0.84.1/packages/coding-agent/src/package-manager-cli.ts:476-505`; `.repo/pi-0.84.1/packages/coding-agent/src/package-manager-cli.ts:820-878`). | Pi includes self-update; Opi does not under the recorded source search. | confirmed |
| Trust and integrity | Locks Git identity/manifest/executable material, disables missing or drifted locks, and separately requires trusted/enabled state for executable contributions (`crates/opi-coding-agent/src/package_store.rs:287-313`; `crates/opi-coding-agent/src/package_resolver.rs:365-395`; `crates/opi-coding-agent/src/package_activation.rs:88-109`). | Gates project package storage on project trust and delegates npm/Git installation/update; no explicit manifest/executable lock or per-package trust record exists under the recorded search (`.repo/pi-0.84.1/packages/coding-agent/src/core/package-manager.ts:978-1010`; `.repo/pi-0.84.1/packages/coding-agent/src/core/package-manager.ts:1714-1717`; `.repo/pi-0.84.1/packages/coding-agent/src/core/package-manager.ts:1781-1829`). | Per-package lock and executable activation state versus project-scope trust plus package-manager/Git installation state. | confirmed |

## 13. Export and share

**opi:** `--export-session` writes a local Markdown document by default or JSON, with active-branch or full-tree scope and configurable redaction/tool/thinking inclusion (`crates/opi-coding-agent/src/cli.rs:48`, `crates/opi-coding-agent/src/cli.rs:224`, `crates/opi-coding-agent/src/session_cli.rs:408`). Active-branch export reconstructs current context; full-tree export collects every message entry in file order, including sibling branches, without active-branch compaction/branch-summary processing (`crates/opi-coding-agent/src/session_cli.rs:415`, `crates/opi-coding-agent/src/session_cli.rs:433`).

- Export rendering applies summary, verbose, or no redaction and independently includes/excludes thinking and tool results (`crates/opi-coding-agent/src/session_cli.rs:452`, `crates/opi-coding-agent/src/session_cli.rs:530`, `crates/opi-coding-agent/src/session_cli.rs:550`).
- No share/publish surface was found: searched `crates/opi-coding-agent/src` for `handleShareCommand`, `getShareViewerUrl`, `Share URL:`, `gist create`, `--public=false`, and `publish_session`.

**pi:** CLI `--export <file>` writes HTML; interactive `/export` writes current-branch JSONL when the destination ends in `.jsonl` and otherwise writes HTML (`.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:288`, `.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:5766`). HTML uses all entries and the current leaf; interactive export can also embed the live system prompt, tool definitions, and pre-rendered custom tools, while CLI file export leaves the live prompt/tool fields unset (`.repo/pi-0.84.1/packages/coding-agent/src/core/export-html/index.ts:251`, `.repo/pi-0.84.1/packages/coding-agent/src/core/export-html/index.ts:263`, `.repo/pi-0.84.1/packages/coding-agent/src/core/export-html/index.ts:298`).

- Current-branch JSONL re-chains only `getBranch()` entries into a linear parent sequence (`.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:3267`).
- `/share` exports HTML, creates a non-public gist through `gh`, and displays the configured viewer URL (`.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:5855`, `.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:5907`, `.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:5939`).
- No redaction or tool/thinking omission option was found: searched `.repo/pi-0.84.1/packages/coding-agent/src/core/export-html/index.ts` for `redact`, `exclude thinking`, and `exclude tool` variants.

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Output formats | Local Markdown or normalized JSON (`crates/opi-coding-agent/src/session_cli.rs:423`). | CLI HTML; interactive HTML or branch JSONL (`.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:288`, `.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:5770`). | The exported formats differ; Pi's JSONL is a re-chained session branch rather than Opi's normalized JSON document. | confirmed |
| Branch/tree scope | Selects reconstructed active context or all message entries in file order (`crates/opi-coding-agent/src/session_cli.rs:415`, `crates/opi-coding-agent/src/session_cli.rs:433`). | HTML contains all entries plus the leaf ID; JSONL contains the current branch (`.repo/pi-0.84.1/packages/coding-agent/src/core/export-html/index.ts:251`, `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session.ts:3267`). | Opi exposes an explicit branch/full-tree switch; Pi ties scope to format. | confirmed |
| Redaction and omission | Supports summary/verbose/none redaction and independent thinking/tool-result inclusion (`crates/opi-coding-agent/src/session_cli.rs:452`, `crates/opi-coding-agent/src/session_cli.rs:530`, `crates/opi-coding-agent/src/session_cli.rs:550`). | HTML exporter exposes no corresponding option under the recorded absence search. | Opi has export-time disclosure controls; Pi HTML serializes without those switches. | confirmed |
| Interactive export and publication | Provides the local CLI export; no measured share/publish implementation under the recorded absence search. | Provides interactive `/export` and `/share`; `/share` publishes a non-public gist and displays a viewer URL (`.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:5766`, `.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:5907`, `.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:5939`). | Pi includes interactive export and remote gist/viewer publication; Opi's measured export is local and CLI-driven. | confirmed |
| Interactive HTML payload | CLI export renders a session header plus reconstructed/collected agent messages (`crates/opi-coding-agent/src/session_cli.rs:595`, `crates/opi-coding-agent/src/session_cli.rs:603`, `crates/opi-coding-agent/src/session_cli.rs:614`). | Interactive HTML embeds all entries, leaf ID, current system prompt, tool definitions, and pre-rendered custom tools; CLI file export embeds entries/leaf but sets system prompt and tools to `undefined` (`.repo/pi-0.84.1/packages/coding-agent/src/core/export-html/index.ts:251`, `.repo/pi-0.84.1/packages/coding-agent/src/core/export-html/index.ts:263`, `.repo/pi-0.84.1/packages/coding-agent/src/core/export-html/index.ts:298`). | Pi's interactive HTML carries live prompt/tool/rendering state that Opi's export document does not; Pi's CLI-only HTML path does not carry those live fields. | added |

## 14. Permissions and sandbox

**opi:** Tool policy defines separate coding and read-only defaults and requires explicit mutating opt-in for headless mutation (`crates/opi-coding-agent/src/policy.rs:18`, `crates/opi-coding-agent/src/policy.rs:39`, `crates/opi-coding-agent/src/policy.rs:64`). Project trust resolves CLI override, registered resolver votes, stored trust, global default, then UI; the resolver registry seals before project resources load, and headless unresolved asks map to untrusted (`crates/opi-coding-agent/src/project_trust.rs:458`, `crates/opi-coding-agent/src/project_trust.rs:601`, `crates/opi-coding-agent/src/project_trust.rs:648`).

- External `command.execute` authorization is separate from tool enablement: local defaults allow, external adapters default ask, deny hides the capability from the model, and headless ask without a broker fails closed (`crates/opi-coding-agent/src/execution/permission.rs:1`, `crates/opi-coding-agent/src/execution/permission.rs:40`, `crates/opi-coding-agent/src/execution/permission.rs:152`).
- The standalone `opi-sandbox` crate requests workspace/temp-scoped writes and default network denial; Linux reports Landlock/seccomp, macOS reports Seatbelt, and unsupported platforms do not fall back to unrestricted execution (`crates/opi-sandbox/src/policy.rs:27`, `crates/opi-sandbox/src/platform/mod.rs:49`, `crates/opi-sandbox/src/platform/mod.rs:67`).
- A selected external command adapter is revalidated per call; activation/protocol failure becomes a tool failure without local fallback (`crates/opi-coding-agent/src/execution/runtime.rs:561`, `crates/opi-coding-agent/src/execution/runtime.rs:595`).

**pi:** Project trust checks CLI approval, pre-trust extension handlers, stored trust, global default, then UI; a headless ask returns untrusted (`.repo/pi-0.84.1/packages/coding-agent/src/core/project-trust.ts:46`, `.repo/pi-0.84.1/packages/coding-agent/src/core/project-trust.ts:54`, `.repo/pi-0.84.1/packages/coding-agent/src/core/project-trust.ts:72`, `.repo/pi-0.84.1/packages/coding-agent/src/core/project-trust.ts:86`). The extension result can return yes/no/undecided and optional persistence (`.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:519`, `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:526`).

- Built-ins support selection filters, and the `tool_call` extension event can block execution; the included confirmation behavior is an example extension (`.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:118`, `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:899`, `.repo/pi-0.84.1/packages/coding-agent/examples/extensions/permission-gate.ts:13`).
- Pi assigns confinement to an external container, VM, or sandbox and explicitly states that the coding agent has no sandbox (`.repo/pi-0.84.1/SECURITY.md:6`, `.repo/pi-0.84.1/SECURITY.md:50`).
- Built-in bash operations directly spawn the configured shell and allow an operations override; they do not implement a selected backend/fallback policy (`.repo/pi-0.84.1/packages/coding-agent/src/core/tools/bash.ts:87`, `.repo/pi-0.84.1/packages/coding-agent/src/core/tools/bash.ts:102`, `.repo/pi-0.84.1/packages/coding-agent/src/core/tools/bash.ts:325`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Mode-specific mutation policy | Uses coding/read-only defaults and enforces headless mutating opt-in (`crates/opi-coding-agent/src/policy.rs:18`, `crates/opi-coding-agent/src/policy.rs:39`, `crates/opi-coding-agent/src/policy.rs:64`). | Defaults the SDK to `read`, `bash`, `edit`, and `write` and exposes selection filters (`.repo/pi-0.84.1/packages/coding-agent/src/core/sdk.ts:245`, `.repo/pi-0.84.1/packages/coding-agent/src/cli/args.ts:118`); no mutation opt-in flag was found under `.repo/pi-0.84.1/packages/coding-agent/src`. | Opi has mode-specific defaults plus a separate headless mutation gate; Pi has inclusion/exclusion/disable controls without that gate. | confirmed |
| Project-trust extension/resolver seam | Accepts user-global/embedder-registered `ProjectTrustResolver` votes; the standard CLI registry starts empty, resolver results are votes without a persistence flag, and the registry seals before project resources load (`crates/opi-coding-agent/src/project_trust.rs:208`, `crates/opi-coding-agent/src/project_trust.rs:218`, `crates/opi-coding-agent/src/project_trust.rs:258`, `crates/opi-coding-agent/src/project_trust.rs:284`). | Invokes already loaded pre-trust extension handlers; a handler returns yes/no/undecided plus optional `remember`, which writes the trust store (`.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:519`, `.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:526`, `.repo/pi-0.84.1/packages/coding-agent/src/core/project-trust.ts:54`, `.repo/pi-0.84.1/packages/coding-agent/src/core/project-trust.ts:63`). | Both gate project resources and allow a pre-trust policy hook; Pi exposes a persisted extension decision, while Opi exposes an embedder resolver vote and separately seals out project-loaded resolvers. | refined |
| Command permission gate | External-adapter allow/ask/deny is distinct from tool enablement and fails unresolved headless asks (`crates/opi-coding-agent/src/execution/permission.rs:1`, `crates/opi-coding-agent/src/execution/permission.rs:152`). | Core extensions can block tool calls; per-command confirmation is provided as an example (`.repo/pi-0.84.1/packages/coding-agent/src/core/extensions/types.ts:899`, `.repo/pi-0.84.1/packages/coding-agent/examples/extensions/permission-gate.ts:13`). | Opi has a core user-owned capability gate for external command adapters; Pi expresses per-command blocking/confirmation through extensions. | confirmed |
| Native restriction | Ships a standalone adapter crate with explicit supported and unsupported postures (`crates/opi-sandbox/src/platform/mod.rs:49`, `crates/opi-sandbox/src/platform/mod.rs:67`). | Delegates confinement to an external environment and declares no coding-agent sandbox (`.repo/pi-0.84.1/SECURITY.md:6`, `.repo/pi-0.84.1/SECURITY.md:50`). | Opi includes a separately installed native-restriction backend; Pi does not include a sandbox backend. | confirmed |
| Selected-backend failure | Revalidates the selected external adapter and has no local fallback on activation/protocol failure (`crates/opi-coding-agent/src/execution/runtime.rs:561`, `crates/opi-coding-agent/src/execution/runtime.rs:595`). | Built-in bash directly uses local operations or a caller-supplied operations object (`.repo/pi-0.84.1/packages/coding-agent/src/core/tools/bash.ts:87`, `.repo/pi-0.84.1/packages/coding-agent/src/core/tools/bash.ts:325`). | Opi defines fail-closed selected-backend behavior; Pi's built-in execution path has no corresponding backend selection/fallback layer. | confirmed |

## 15. MCP

**opi:** No MCP client, server, protocol, or transport was found in product runtime source: searched `crates/opi-agent/src` and `crates/opi-coding-agent/src` for `MCP` and `model context protocol` (excluding the unrelated OAuth scope string). The repository contains a metadata-only `examples/mcp-adapter` package and a test-local fixture `Extension` implementation with no live network transport (`examples/mcp-adapter/package.toml:1`, `examples/mcp-adapter/README.md:7`, `crates/opi-coding-agent/tests/mcp_adapter_example.rs:18`, `crates/opi-coding-agent/tests/mcp_adapter_example.rs:271`).

**pi:** Pi documents that coding-agent has no built-in MCP and directs consumers to an extension (`.repo/pi-0.84.1/packages/coding-agent/README.md:498`). No MCP-named file exists under `.repo/pi-0.84.1/packages`; source occurrences are incidental bridge/test text rather than an implementation (`.repo/pi-0.84.1/packages/coding-agent/src/utils/tool-result-images.ts:15`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Core MCP runtime | No MCP client/server/protocol/transport under the recorded product-source search. | Documents no built-in MCP (`.repo/pi-0.84.1/packages/coding-agent/README.md:498`) and has no MCP implementation under the recorded package search. | Both measured product runtimes contain no MCP implementation. | confirmed |
| MCP repository example | Contains a metadata-only `examples/mcp-adapter` package describing fixture MCP-style commands, plus a test-local `Extension` implementation; it has no live transport/network client (`examples/mcp-adapter/package.toml:1`, `examples/mcp-adapter/extensions/mcp-adapter/extension.toml:1`, `examples/mcp-adapter/README.md:7`, `crates/opi-coding-agent/tests/mcp_adapter_example.rs:18`, `crates/opi-coding-agent/tests/mcp_adapter_example.rs:271`). | Contains no MCP-named package/example and directs consumers to build an extension (`.repo/pi-0.84.1/packages/coding-agent/README.md:498`; absent: searched `.repo/pi-0.84.1/packages` for MCP-named files). | Opi retains manifest/documentation and test-fixture evidence for an MCP-shaped extension; neither tree ships a live MCP adapter. | refined |

## 16. Image generation

**opi:** Opi represents user/tool image content as URL, base64, or byte sources and loads size-checked PNG/JPEG/GIF/WebP attachments into conversation input (`crates/opi-ai/src/message.rs:53`, `crates/opi-ai/src/message.rs:78`, `crates/opi-ai/src/message.rs:94`, `crates/opi-coding-agent/src/image.rs:1`). No generation API/provider/model surface was found: searched `crates/opi-ai/src` and `crates/opi-coding-agent/src` for `generateImages`, `generate_images`, `ImagesModel`, `ImagesProvider`, and `image_generation`.

**pi:** `pi-ai` defines a one-shot `generateImages` call and image-provider/model collections with provider/model lookup, refresh, auth resolution, and dispatch (`.repo/pi-0.84.1/packages/ai/src/images.ts:14`, `.repo/pi-0.84.1/packages/ai/src/images-models.ts:12`, `.repo/pi-0.84.1/packages/ai/src/images-models.ts:49`, `.repo/pi-0.84.1/packages/ai/src/images-models.ts:183`). The built-in collection registers OpenRouter image generation (`.repo/pi-0.84.1/packages/ai/src/providers/all.ts:143`).

- OpenRouter generation accepts text or image-conditioned input, supports cancellation/timeout/retry and payload/response hooks, records response ID/usage, decodes data-URL image output, and returns error/aborted state in the result (`.repo/pi-0.84.1/packages/ai/src/api/openrouter-images.ts:40`, `.repo/pi-0.84.1/packages/ai/src/api/openrouter-images.ts:61`, `.repo/pi-0.84.1/packages/ai/src/api/openrouter-images.ts:65`, `.repo/pi-0.84.1/packages/ai/src/api/openrouter-images.ts:84`, `.repo/pi-0.84.1/packages/ai/src/api/openrouter-images.ts:96`, `.repo/pi-0.84.1/packages/ai/src/api/openrouter-images.ts:110`, `.repo/pi-0.84.1/packages/ai/src/api/openrouter-images.ts:136`).
- The coding-agent product has no generation wiring: searched `.repo/pi-0.84.1/packages/coding-agent/src` for `generateImages`, `builtinImagesModels`, and `ImagesModel`.

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Generation library surface | Exposes image content/attachment types but no generation API under the recorded search (`crates/opi-ai/src/message.rs:53`, `crates/opi-coding-agent/src/image.rs:1`). | Exposes `generateImages`, image provider/model contracts, collection, auth, catalog refresh, lookup, and dispatch (`.repo/pi-0.84.1/packages/ai/src/images.ts:14`, `.repo/pi-0.84.1/packages/ai/src/images-models.ts:12`, `.repo/pi-0.84.1/packages/ai/src/images-models.ts:49`). | Pi has an embeddable image-generation library surface; Opi's image surface is conversation/tool content. | confirmed |
| OpenRouter generation provider | Has no registered generation provider under the recorded search. | Registers OpenRouter with cancellation, retry, hooks, response ID, usage, output decoding, and structured failure state (`.repo/pi-0.84.1/packages/ai/src/providers/all.ts:143`, `.repo/pi-0.84.1/packages/ai/src/api/openrouter-images.ts:40`, `.repo/pi-0.84.1/packages/ai/src/api/openrouter-images.ts:65`, `.repo/pi-0.84.1/packages/ai/src/api/openrouter-images.ts:84`, `.repo/pi-0.84.1/packages/ai/src/api/openrouter-images.ts:96`, `.repo/pi-0.84.1/packages/ai/src/api/openrouter-images.ts:110`). | Pi includes a concrete generation provider; Opi does not. | confirmed |
| Product wiring | Has neither library nor coding-agent generation wiring under the recorded Opi search. | Generation is exported by `pi-ai`, but no coding-agent call site exists under the recorded Pi product search. | Pi's generation capability is library-only in the measured tree; Opi has no generation layer. | confirmed |
| Generation input contract | Image values are chat/tool message content and local attachments; there is no generation context (`crates/opi-ai/src/message.rs:78`, `crates/opi-ai/src/message.rs:94`, `crates/opi-coding-agent/src/image.rs:1`). | `ImagesContext.input` accepts text or base64 image content, and the OpenRouter generator maps image inputs to data-URL `image_url` parts (`.repo/pi-0.84.1/packages/ai/src/types.ts:354`, `.repo/pi-0.84.1/packages/ai/src/types.ts:450`, `.repo/pi-0.84.1/packages/ai/src/types.ts:453`, `.repo/pi-0.84.1/packages/ai/src/api/openrouter-images.ts:136`, `.repo/pi-0.84.1/packages/ai/src/api/openrouter-images.ts:144`). | Pi's generation call supports text-only and image-conditioned inputs; Opi's measured image input belongs only to conversation/tool content. | added |

## 17. Diagnostics and observability

**opi:** Opi uses one cross-subsystem diagnostic record with severity, stable code/source, message, details, and action; product doctor/RPC/NDJSON paths convert it to a redacted public payload (`crates/opi-agent/src/diagnostic.rs:70`, `crates/opi-agent/src/diagnostic.rs:95`, `crates/opi-agent/src/diagnostic.rs:153`, `crates/opi-coding-agent/src/doctor.rs:306`, `crates/opi-coding-agent/src/rpc.rs:758`). The internal `Diagnostic` itself remains public and serializable (`crates/opi-agent/src/diagnostic.rs:77`).

- `opi doctor` has six local scopes (config, provider, package, session, TUI, RPC) and emits human or NDJSON output with summary-redacted payloads (`crates/opi-coding-agent/src/doctor.rs:59`, `crates/opi-coding-agent/src/doctor.rs:218`, `crates/opi-coding-agent/src/doctor.rs:302`).
- Opt-in trace records carry schema version 1, redact details before writing, and are retrievable through an RPC trace envelope (`crates/opi-agent/src/trace.rs:49`, `crates/opi-agent/src/trace.rs:157`, `crates/opi-agent/src/trace.rs:241`, `crates/opi-coding-agent/src/rpc.rs:940`).
- No TUI debug-log command was found: searched `crates/opi-coding-agent/src` for `/debug`, `debug JSONL`, and `all agent messages` variants.

**pi:** Coding-agent startup/resource diagnostics use smaller severity/message/path or collision shapes and are rendered during startup (`.repo/pi-0.84.1/packages/coding-agent/src/core/diagnostics.ts:1`, `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session-services.ts:18`, `.repo/pi-0.84.1/packages/coding-agent/src/main.ts:95`). No doctor command was found: searched `.repo/pi-0.84.1/packages/coding-agent/src` for `--doctor`, `case "doctor"`, `command: "doctor"`, and `type: "doctor"`.

- Pi defines a vendor-neutral telemetry context and typed AI/harness span helpers, and `AgentHarnessOptions` accepts an optional context (`.repo/pi-0.84.1/packages/telemetry/src/index.ts:1`, `.repo/pi-0.84.1/packages/agent/src/harness/telemetry.ts:138`, `.repo/pi-0.84.1/packages/agent/src/harness/telemetry.ts:602`, `.repo/pi-0.84.1/packages/agent/src/harness/agent-harness.ts:243`). No calls to `startAiSpan` or `startHarnessSpan` were found outside the helper file under `.repo/pi-0.84.1/packages/agent/src` and `.repo/pi-0.84.1/packages/coding-agent/src`.
- Interactive `/debug` writes current rendered terminal lines and serializes `this.session.messages` without an explicit redaction pass (`.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:6222`, `.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:6233`, `.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:6240`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Diagnostic model and public payloads | Uses one cross-subsystem `Diagnostic` with severity/code/source/message/details/action; doctor, RPC, and NDJSON paths explicitly call `redacted_payload`, while the internal record remains publicly serializable (`crates/opi-agent/src/diagnostic.rs:70`, `crates/opi-agent/src/diagnostic.rs:77`, `crates/opi-agent/src/diagnostic.rs:95`, `crates/opi-agent/src/diagnostic.rs:153`, `crates/opi-coding-agent/src/doctor.rs:306`, `crates/opi-coding-agent/src/rpc.rs:758`). | Coding-agent startup/resource diagnostics use smaller severity/message/path or collision shapes and render through startup handling (`.repo/pi-0.84.1/packages/coding-agent/src/core/diagnostics.ts:1`, `.repo/pi-0.84.1/packages/coding-agent/src/core/agent-session-services.ts:18`, `.repo/pi-0.84.1/packages/coding-agent/src/main.ts:95`). | Opi centralizes stable codes, sources, details, actions, and explicit redacted public-payload conversion; Pi's coding-agent diagnostics do not share that record contract. | refined |
| Doctor | Defines six local scopes and human/NDJSON output (`crates/opi-coding-agent/src/doctor.rs:59`, `crates/opi-coding-agent/src/doctor.rs:218`, `crates/opi-coding-agent/src/doctor.rs:302`). | No doctor command under the recorded product-source search. | Opi exposes a local operator health command; Pi does not expose a measured equivalent. | confirmed |
| Trace and telemetry hooks | Emits versioned redacted local trace records and exposes RPC retrieval (`crates/opi-agent/src/trace.rs:49`, `crates/opi-agent/src/trace.rs:157`, `crates/opi-coding-agent/src/rpc.rs:940`). | Defines typed span helpers and an optional telemetry context, but has no measured production helper call sites (`.repo/pi-0.84.1/packages/agent/src/harness/telemetry.ts:138`, `.repo/pi-0.84.1/packages/agent/src/harness/telemetry.ts:602`, `.repo/pi-0.84.1/packages/agent/src/harness/agent-harness.ts:243`). | Opi's trace is integrated into measured runtime/RPC paths; Pi's measured source exposes telemetry schemas/context helpers without starting those spans. | confirmed |
| Debug artifact versus trace | Has no `/debug` command; its separate trace collector records lifecycle/provider/tool/diagnostic observations and redacts structured details before the sink and RPC retrieval (`crates/opi-agent/src/trace.rs:60`, `crates/opi-agent/src/trace.rs:157`, `crates/opi-agent/src/trace.rs:241`, `crates/opi-coding-agent/src/rpc.rs:940`; absent: searched `crates/opi-coding-agent/src` for `/debug`, `debug JSONL`, and `all agent messages`). | `/debug` writes current rendered terminal lines and serializes `this.session.messages` without an explicit redaction pass (`.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:6222`, `.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:6233`, `.repo/pi-0.84.1/packages/coding-agent/src/modes/interactive/interactive-mode.ts:6240`). | Pi exposes an unredacted TUI/message debugging artifact; Opi exposes a distinct redacted structured trace artifact and no TUI debug-log command. | refined |

## 18. Wire protocols and embedding

**opi:** Opi emits an NDJSON schema-version-2 header, uses shared SDK/RPC schema version 3 over strict LF-delimited JSON, and emits trace schema version 1 (`crates/opi-coding-agent/src/runner.rs:29`, `crates/opi-coding-agent/src/runner.rs:276`, `crates/opi-agent/src/sdk.rs:37`, `crates/opi-coding-agent/src/rpc.rs:10`, `crates/opi-agent/src/trace.rs:49`). Execution adapters use the separate closed `command-execution-jsonl-v1` one-shot protocol (`crates/opi-protocol/src/execution/v1/mod.rs:1`, `crates/opi-protocol/src/execution/v1/mod.rs:95`).

- Embedders can drive the public stateful `Agent`, subscribe/abort/steer/follow up, construct a `CodingHarnessBuilder`, or use a schema-v3 `StreamingProxy` over caller-supplied `BufRead + Write` (`crates/opi-agent/src/agent.rs:67`, `crates/opi-agent/src/agent.rs:245`, `crates/opi-coding-agent/src/harness.rs:424`, `crates/opi-agent/src/streaming_proxy.rs:1`, `crates/opi-agent/src/streaming_proxy.rs:131`).
- No remote-session client/server contract was found: searched `Cargo.toml` and `crates` for `PiClient`, `PiServer`, `SessionService`, and `ByteTransport`. No browser/Wasm target declaration was found: searched root and crate `Cargo.toml` files for `wasm-bindgen`, `web-sys`, `wasm32`, and `cdylib`.

**pi:** Coding-agent retains process-spawning LF-framed JSONL RPC with typed commands/responses/events and no schema/version field (`.repo/pi-0.84.1/packages/coding-agent/src/modes/rpc/rpc-types.ts:1`, `.repo/pi-0.84.1/packages/coding-agent/src/modes/rpc/jsonl.ts:1`, `.repo/pi-0.84.1/packages/coding-agent/src/modes/rpc/rpc-client.ts:1`; absent: searched that RPC directory for `schema_version`, `schemaVersion`, and `PROTOCOL_VERSION`). Separately, protocol version 1 defines strict request/response/event/session-snapshot schemas over four-byte-length-prefixed CBOR with bounded incremental framing (`.repo/pi-0.84.1/packages/protocol/src/schemas.ts:3`, `.repo/pi-0.84.1/packages/protocol/src/schemas.ts:7`, `.repo/pi-0.84.1/packages/protocol/src/framing.ts:1`, `.repo/pi-0.84.1/packages/protocol/src/framing.ts:6`, `.repo/pi-0.84.1/packages/protocol/src/framing.ts:57`).

- `PiClient` is transport-neutral, correlates requests, supports multiple session attachments, and implements shared/exclusive leases; `PiServer` composes listener transports around an application-supplied session service (`.repo/pi-0.84.1/packages/client/src/client.ts:51`, `.repo/pi-0.84.1/packages/client/src/client.ts:138`, `.repo/pi-0.84.1/packages/client/src/client.ts:383`, `.repo/pi-0.84.1/packages/server/src/server.ts:39`, `.repo/pi-0.84.1/packages/server/src/types.ts:55`).
- The root client has no Node-specific imports and accepts a consumer-supplied WebSocket or other ordered byte transport, but the client/protocol/server packages contain no dedicated browser/WebSocket target file (`.repo/pi-0.84.1/packages/client/README.md:3`, `.repo/pi-0.84.1/packages/client/README.md:8`, `.repo/pi-0.84.1/packages/client/README.md:26`; absent: searched those package filenames for `browser`, `websocket`, and a `web` path segment).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Version identities | Declares NDJSON 2, SDK/RPC 3, trace 1, and `command-execution-jsonl-v1` (`crates/opi-coding-agent/src/runner.rs:29`, `crates/opi-agent/src/sdk.rs:37`, `crates/opi-agent/src/trace.rs:49`, `crates/opi-protocol/src/execution/v1/mod.rs:1`). | Coding-agent JSONL RPC has no version field under the recorded search; the remote protocol declares version 1 (`.repo/pi-0.84.1/packages/protocol/src/schemas.ts:3`). | Opi versions each measured JSON/JSONL surface; Pi's legacy coding-agent RPC is unversioned while its separate remote-session protocol is versioned. | confirmed |
| Remote-session and proxy embedding | Exposes in-process `Agent`/`CodingHarness`, stdio RPC, and a transport-agnostic schema-v3 JSONL `StreamingProxy` over caller-supplied `BufRead + Write`; no `PiClient`, `PiServer`, `SessionService`, or `ByteTransport` counterpart occurs under `Cargo.toml` or `crates` (`crates/opi-agent/src/agent.rs:67`, `crates/opi-coding-agent/src/harness.rs:424`, `crates/opi-coding-agent/src/rpc.rs:1`, `crates/opi-agent/src/streaming_proxy.rs:1`, `crates/opi-agent/src/streaming_proxy.rs:131`). | Provides dedicated strict protocol, transport-neutral `PiClient`, `PiServer`, request correlation, authoritative snapshots, multi-session attachment, and shared/exclusive leases (`.repo/pi-0.84.1/packages/client/src/client.ts:51`, `.repo/pi-0.84.1/packages/client/src/client.ts:138`, `.repo/pi-0.84.1/packages/client/src/client.ts:383`, `.repo/pi-0.84.1/packages/server/src/server.ts:39`, `.repo/pi-0.84.1/packages/server/src/types.ts:55`). | Both expose transport-neutral primitives; Opi's proxy forwards JSONL commands/events through caller I/O, while Pi's packages implement a remote-session client/server state and ownership protocol. | refined |
| Framing and encoding | User RPC and execution-adapter wires use LF-delimited JSON (`crates/opi-coding-agent/src/rpc.rs:16`, `crates/opi-protocol/src/execution/v1/mod.rs:1`). | Remote protocol uses strict CBOR with a four-byte length and configurable frame bound (`.repo/pi-0.84.1/packages/protocol/src/schemas.ts:7`, `.repo/pi-0.84.1/packages/protocol/src/framing.ts:1`, `.repo/pi-0.84.1/packages/protocol/src/framing.ts:6`, `.repo/pi-0.84.1/packages/protocol/src/framing.ts:27`). | The measured remote/user-facing encodings and framing differ. | confirmed |
| Browser/web target | Has no browser/Wasm target declaration under the recorded manifest search. | Root client is runtime-neutral and permits a consumer transport, but bundles no dedicated browser/WebSocket target file (`.repo/pi-0.84.1/packages/client/README.md:3`, `.repo/pi-0.84.1/packages/client/README.md:8`, `.repo/pi-0.84.1/packages/client/README.md:26`). | Neither tree bundles a dedicated browser/web target; Pi's client can be paired with a consumer WebSocket transport, while Opi has no browser/Wasm target declaration. | confirmed |

## Appendix B: method and verification

- **Dimension coverage:** 18 of 18.
- **Independent verification:** Dimensions 1-6, 7-12, and 13-18 were each verified by a different reader from their measurer; no dimension used a single-pass fallback.
- **Outcome tally:** 54 `confirmed`, 15 `refined`, 6 `added`, 0 `refuted`; 75 retained deltas total.
- **Refuted-delta notes:** None.
- **Unavailable evidence:** None after pin recovery. The supplied pi tree lacked `.git`, so its exact identity and official remote were established by a content-identical checkout of tag `v0.84.1` at `53fa77ccd8a279eb87e92294ef3687b03ff80112`.
- **Layer B:** Not requested and not included.
