# opi vs pi v0.84.3 - objective differences

- Measurement date: 2026-08-25.
- opi root: `D:\Luiz\Odradek\opi`; version `0.8.1` (`Cargo.toml:13`); commit `8944d5ad1fb23cc6a3c4e70078981e2ca46eec0b`.
- pi root: `D:\Luiz\Odradek\opi\.repo\pi-0.84.3`; version `0.84.3` (`.repo/pi-0.84.3/packages/coding-agent/package.json:3`); tag `v0.84.3`; commit `4e58f324fae8ebfa98a3d45181fb248072a2afac`.
- Verified pi remote: `https://github.com/earendil-works/pi.git`. `git ls-remote` resolved `v0.84.3` to the recorded commit. The supplied snapshot has no nested Git metadata, so its files were byte-compared with a `git archive` from a fresh checkout of that commit; no tracked-file differences were found.
- Scope: full inward comparison, all 18 dimensions in `opi-realign`.
- Method: both sides were measured from current source in three bounded batches; every retained delta was checked by a different worker against source and relevant changelogs. All dimensions received independent verification.
- Framing: Layer A has no phases, baselines, or roadmap framing; Layer H separately records pinned target-design evidence and maturity.

## How to read

The project paragraphs and project columns are cited current facts. `Difference` records only the raw difference between the measured trees. An absence names the source paths searched. Layer H records pinned target intent and observed maturity, not shipped behavior or an Opi requirement. `Verification outcome` records the independent second-pass result; no drift classification or recommendation appendix was requested.

## 1. Provider abstraction and dispatch

**opi:** `ProviderCollection::prepare_call` resolves the canonical route, validates the request, resolves authentication once, and freezes provider, request, authentication, and route before attempts start; `Provider::stream_prepared` consumes that prepared state (`crates/opi-ai/src/provider_collection.rs:400-478`, `crates/opi-ai/src/provider_collection.rs:684-702`, `crates/opi-ai/src/provider.rs:29-44`).

**pi:** Providers declare authentication and streaming behavior, while `Models.applyAuth()` resolves and applies authentication before the lazy stream delegates to `provider.stream()`; model records carry both provider and API identities (`.repo/pi-0.84.3/packages/ai/src/models.ts:89-154`, `.repo/pi-0.84.3/packages/ai/src/models.ts:636-679`, `.repo/pi-0.84.3/packages/ai/src/types.ts:820-826`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Call preparation | The collection freezes route, request, and resolved authentication before attempts (`crates/opi-ai/src/provider_collection.rs:400-478`). | `Models` applies authentication per request before provider delegation (`.repo/pi-0.84.3/packages/ai/src/models.ts:636-679`). | Preparation is collection-owned and frozen across attempts in opi; pi resolves authentication in `Models` before provider streaming. | refined |
| Route identity | Selection is canonical `provider:model`, and a prepared route records provider, model, and wire API (`crates/opi-ai/src/provider_collection.rs:400-425`, `crates/opi-ai/src/provider_collection.rs:466-475`). | A model records `provider` and `api`; the provider factory can select an adapter by `model.api` (`.repo/pi-0.84.3/packages/ai/src/types.ts:820-826`, `.repo/pi-0.84.3/packages/ai/src/models.ts:756-791`). | opi resolves one canonical route before execution; pi can select among API adapters within a provider. | confirmed |
| Refresh publication | All provider results are gathered before dynamic catalogs are replaced as a collection (`crates/opi-ai/src/provider_collection.rs:552-630`). | Generation, cancellation, publication, and errors are maintained per selected provider (`.repo/pi-0.84.3/packages/ai/src/models.ts:320-445`). | Publication is collection-wide in opi and provider-local in pi. | confirmed |

## 2. Authentication and credentials

**opi:** Credentials are stored in the OS keychain, with mutations coordinated by an `fs4` lock; doctor exposes credential probe state without the credential value (`crates/opi-coding-agent/src/credential_store.rs:3-18`, `crates/opi-coding-agent/src/doctor.rs:413-453`).

**pi:** `AuthStorage` persists `auth.json`, uses `proper-lockfile`, and defaults writes to mode `0600`; `pi auth check` can optionally emit the resolved credential (`.repo/pi-0.84.3/packages/coding-agent/src/core/auth-storage.ts:2-25`, `.repo/pi-0.84.3/packages/coding-agent/src/core/auth-storage.ts:52-76`, `.repo/pi-0.84.3/packages/coding-agent/src/cli/auth-command.ts:19-45`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Persistence | OS keychain plus a cross-process credential lock (`crates/opi-coding-agent/src/credential_store.rs:3-18`). | Mode-restricted `auth.json` plus a file lock (`.repo/pi-0.84.3/packages/coding-agent/src/core/auth-storage.ts:2-25`, `.repo/pi-0.84.3/packages/coding-agent/src/core/auth-storage.ts:52-76`). | The durable secret backend is the OS keychain in opi and a local JSON file in pi. | confirmed |
| API-key ambient fallback | The configured environment variable is used only when the keychain entry is absent or the backend is narrowly classified unavailable; wrong-kind and other store errors are returned (`crates/opi-coding-agent/src/credential_store.rs:1071-1074`, `crates/opi-coding-agent/src/credential_store.rs:1107-1131`). | Ambient authentication is consulted only when no credential is stored; stored wrong-kind and failed OAuth paths do not fall through (`.repo/pi-0.84.3/packages/ai/src/auth/resolve.ts:44-48`, `.repo/pi-0.84.3/packages/ai/src/auth/resolve.ts:87-109`). | The fallback predicates differ: selected keychain absence/unavailability in opi and absence of any stored credential in pi. | refined |
| Bundled OAuth flows | Anthropic, OpenAI Codex, and GitHub Copilot: 3 flows (`crates/opi-coding-agent/src/oauth.rs:2392-2427`). | Anthropic, OpenAI Codex, GitHub Copilot, OpenRouter, Kimi Coding, xAI, and Radius: 7 loaders (`.repo/pi-0.84.3/packages/ai/src/auth/oauth/load.ts:26-66`). | The bundled sets contain 3 and 7 flows respectively. | confirmed |
| Credential inspection | Doctor reports presence/probe state without the secret (`crates/opi-coding-agent/src/doctor.rs:413-453`). | `pi auth check --credentials` emits the resolved credential (`.repo/pi-0.84.3/packages/coding-agent/src/cli/auth-command.ts:19-45`, `.repo/pi-0.84.3/packages/coding-agent/src/main.ts:177-196`). | pi has an opt-in credential-emitting CLI path; opi's cited inspection path is redacted. | confirmed |

## 3. Provider catalog

**opi:** Product assembly registers 11 fixed provider IDs, and the closed `WireApi` enum contains 8 variants (`crates/opi-coding-agent/src/provider_factory.rs:517-530`, `crates/opi-ai/src/model_info.rs:76-110`).

**pi:** `builtinProviders()` constructs 40 providers; `KnownApi` contains 10 identifiers while `Api` also accepts custom strings (`.repo/pi-0.84.3/packages/ai/src/providers/all.ts:89-131`, `.repo/pi-0.84.3/packages/ai/src/types.ts:17-29`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Built-in providers | 11 fixed provider IDs (`crates/opi-coding-agent/src/provider_factory.rs:517-530`). | 40 provider definitions (`.repo/pi-0.84.3/packages/ai/src/providers/all.ts:89-131`). | The enumerated built-in catalogs differ by 29 provider definitions. | confirmed |
| API vocabulary | Closed 8-variant `WireApi` enum (`crates/opi-ai/src/model_info.rs:76-110`). | 10 known API identifiers plus arbitrary custom strings (`.repo/pi-0.84.3/packages/ai/src/types.ts:17-29`). | opi rejects identifiers outside its enum; pi's public API type admits custom identifiers. | confirmed |
| Product refresh trigger | The provider contract records no coding-agent production refresh trigger; invocation is absent: searched `crates/opi-ai/src`, `crates/opi-agent/src`, and `crates/opi-coding-agent/src` for `ProviderCollection::refresh` and `.refresh().await` call sites (`crates/opi-ai/src/provider.rs:17-21`). | `ModelRuntime.create()` invokes refresh unless `refreshOnCreate` is false (`.repo/pi-0.84.3/packages/coding-agent/src/core/model-runtime.ts:190-216`). | pi has a product creation path that invokes refresh; no corresponding opi product call site was found in the stated scope. | confirmed |

## 4. Stream and transport lifecycle

**opi:** Streams normalize start, text, thinking, tool-call, done, and error events; the agent loop prepares one logical call before retrying sequential attempts from the same frozen call (`crates/opi-ai/src/stream.rs:325-438`, `crates/opi-agent/src/agent_loop.rs:29-39`, `crates/opi-agent/src/agent_loop.rs:270-378`).

**pi:** The normalized stream includes a deferred terminal reason and durable handle; retries occur in adapter request establishment and in coding-session continuation after retryable assistant errors (`.repo/pi-0.84.3/packages/ai/src/types.ts:405-419`, `.repo/pi-0.84.3/packages/ai/src/types.ts:535-551`, `.repo/pi-0.84.3/packages/ai/src/utils/provider-retry.ts:98-123`, `.repo/pi-0.84.3/packages/coding-agent/src/core/agent-session.ts:2808-2860`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Deferred completion | Deferred response primitives are absent: searched `crates/opi-ai/src`, `crates/opi-agent/src`, and `crates/opi-coding-agent/src` for `DeferredHandle`, `fetchDeferred`, and `cancelDeferred`; `StopReason` has no deferred variant (`crates/opi-ai/src/stream.rs:13-36`). | `deferred` is a terminal done reason with authenticated fetch/cancel operations (`.repo/pi-0.84.3/packages/ai/src/types.ts:405-419`, `.repo/pi-0.84.3/packages/ai/src/models.ts:706-731`). | Only pi exposes deferred completion in its normalized model API. | confirmed |
| Retry boundaries | One prepared logical call is reopened for sequential attempts (`crates/opi-agent/src/agent_loop.rs:270-378`, `crates/opi-ai/src/provider_collection.rs:781-821`). | Retries occur around fresh adapter requests and in coding-session continuation (`.repo/pi-0.84.3/packages/ai/src/utils/provider-retry.ts:98-123`, `.repo/pi-0.84.3/packages/coding-agent/src/core/agent-session.ts:2808-2860`). | opi's cited retry boundary is the frozen logical call; pi has adapter and product-session retry boundaries. | confirmed |
| Provider-neutral transport selector | Concrete SSE adapters exist (`crates/opi-ai/src/anthropic.rs:1-54`, `crates/opi-ai/src/gemini.rs:1-33`, `crates/opi-ai/src/openai_chat.rs:1-45`); a public text-generation selector with `websocket`, `websocket-cached`, and `auto` is absent: searched `crates/opi-ai/src`, `crates/opi-agent/src`, and `crates/opi-coding-agent/src`. | The request vocabulary exposes `sse`, `websocket`, `websocket-cached`, and `auto` (`.repo/pi-0.84.3/packages/ai/src/types.ts:110`). | Both include SSE behavior, but only pi exposes the cited provider-neutral transport selector. | refined |

## 5. Agent loop and harness

**opi:** Public `agent_loop` is paired with a stateful `Agent`; `SessionFacade` and the product `CodingHarness` provide separate session and product wrappers (`crates/opi-agent/src/agent_loop.rs:29-58`, `crates/opi-agent/src/agent.rs:539-563`, `crates/opi-agent/src/harness.rs:217-255`, `crates/opi-coding-agent/src/harness.rs:662`).

**pi:** Public `agentLoop`/`agentLoopContinue` are paired with a stateful `Agent`; `AgentHarness` exposes implemented state/configuration accessors while several run, navigation, lane, and watch operations return `HarnessNotImplemented` (`.repo/pi-0.84.3/packages/agent/src/agent-loop.ts:27-64`, `.repo/pi-0.84.3/packages/agent/src/agent.ts:167-181`, `.repo/pi-0.84.3/packages/agent/src/harness/agent-harness.ts:305-503`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Core turn surface | Public loop function plus stateful wrapper (`crates/opi-agent/src/agent_loop.rs:29-58`, `crates/opi-agent/src/agent.rs:539-563`). | Public loop functions plus stateful wrapper (`.repo/pi-0.84.3/packages/agent/src/agent-loop.ts:27-64`, `.repo/pi-0.84.3/packages/agent/src/agent.ts:167-181`). | Both separate a loop function from an evented stateful wrapper. | confirmed |
| Named `AgentHarness` | A public `AgentHarness` type is absent: searched `crates/opi-agent/src` and `crates/opi-coding-agent/src` for a public struct, trait, or type; opi exposes `SessionFacade` and `CodingHarness` (`crates/opi-agent/src/harness.rs:217-255`, `crates/opi-coding-agent/src/harness.rs:662`). | State/configuration accessors are implemented; prompt/run/navigation/lane/watch operations return `HarnessNotImplemented` (`.repo/pi-0.84.3/packages/agent/src/harness/agent-harness.ts:305-503`). | The named public type exists only in pi, and its operational surface is mixed rather than uniformly unavailable. | refined |
| Product construction | Interactive, runner, and RPC paths build `CodingHarness` (`crates/opi-coding-agent/src/main.rs:1243`, `crates/opi-coding-agent/src/runner.rs:271`, `crates/opi-coding-agent/src/rpc.rs:348`). | The SDK path constructs `Agent`; the session-server path constructs `AgentHarness` (`.repo/pi-0.84.3/packages/coding-agent/src/core/sdk.ts:173`, `.repo/pi-0.84.3/packages/coding-agent/src/server/create-harness.ts:153`). | opi's cited product entry paths converge on one harness type; pi retains separate SDK and server-harness constructions. | confirmed |

## 6. Session model

**opi:** New durable JSONL writers require v2; v1 remains readable but cannot be reopened for writing. Entries form a `parent_id` graph, and the latest leaf entry selects the active branch (`crates/opi-agent/src/session.rs:1-21`, `crates/opi-agent/src/session.rs:46-55`, `crates/opi-agent/src/session.rs:143-174`, `crates/opi-agent/src/session_branch.rs:30-43`). A session-search contract is absent: searched `crates/opi-agent/src` and `crates/opi-coding-agent/src` for `SessionSearch`, a session search index, and session-entry search APIs.

**pi:** Agent-core JSONL uses v4 with named lanes and repository/storage contracts, while the coding product's persisted `SessionManager` uses v3 and one current leaf (`.repo/pi-0.84.3/packages/agent/src/harness/session/jsonl/codec.ts:89-99`, `.repo/pi-0.84.3/packages/agent/src/harness/session/types.ts:290-372`, `.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:30`, `.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:847-866`). Agent core also exports an asynchronous session-search contract and a scanning implementation (`.repo/pi-0.84.3/packages/agent/src/index.ts:141`, `.repo/pi-0.84.3/packages/agent/src/search/index.ts:14-31`, `.repo/pi-0.84.3/packages/agent/src/search/scanning.ts:135-176`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Active formats | New writers require v2; v1 is readable but not writable (`crates/opi-agent/src/session.rs:1-21`, `crates/opi-agent/src/session.rs:46-55`, `crates/opi-agent/src/session.rs:447-488`). | Agent-core JSONL uses v4; coding-agent `SessionManager` uses v3 (`.repo/pi-0.84.3/packages/agent/src/harness/session/jsonl/repo.ts:209-216`, `.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:30`). | pi contains distinct core-v4 and coding-product-v3 paths; opi's product writes the core v2 format. | confirmed |
| Legacy migration | The source remains unmodified; resume/fork creates a parented v2 child with the reconstructed active branch (`crates/opi-coding-agent/src/session_cli.rs:208-216`, `crates/opi-coding-agent/src/session_cli.rs:272-313`, `crates/opi-coding-agent/src/harness.rs:2302-2306`). | Persisted sessions migrate v1 to v2 to v3 in memory and rewrite the same file during load; the rewrite is skipped without persistence (`.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:230-295`, `.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:895-922`). | opi preserves legacy source bytes and writes a child; pi's persisted product path upgrades the source file. | refined |
| Repository contract | `SessionRepo` exposes append, load, and message count (`crates/opi-agent/src/harness.rs:142-162`). | Lifecycle operations span `SessionRepo`; lanes, records, queries, facts, and statistics span `SessionStorage`/`SessionTree`, with JSONL and memory repositories (`.repo/pi-0.84.3/packages/agent/src/harness/session/types.ts:290-372`, `.repo/pi-0.84.3/packages/agent/src/harness/session/jsonl/repo.ts:109-155`, `.repo/pi-0.84.3/packages/agent/src/harness/session/memory.ts:148-179`). | The cited pi contracts cover more lifecycle, lane, query, and alternate-storage operations. | refined |
| Branch representation | A `parent_id` graph plus the latest leaf entry selects the active branch (`crates/opi-agent/src/session.rs:143-174`, `crates/opi-agent/src/session_branch.rs:30-43`). | Agent-core v4 exposes named lane pointers; coding-agent v3 separately maintains one current leaf (`.repo/pi-0.84.3/packages/agent/src/harness/session/types.ts:273-296`, `.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:847-866`). | opi uses one active parent chain; pi exposes named lanes in v4 while retaining a current-leaf product path in v3. | confirmed |
| Session search API | Absent: searched `crates/opi-agent/src` and `crates/opi-coding-agent/src` for `SessionSearch`, session search indexes, and session-entry search APIs. | The package root exports `SessionSearch`, whose hits have stable session/entry identity; `createScanningSessionSearch` is implemented and covered by focused memory, filter, abort, and JSONL tests (`.repo/pi-0.84.3/packages/agent/src/index.ts:141`, `.repo/pi-0.84.3/packages/agent/src/search/index.ts:14-31`, `.repo/pi-0.84.3/packages/agent/src/search/scanning.ts:135-176`, `.repo/pi-0.84.3/packages/agent/test/harness/session/search.test.ts:57-124`). | Only pi exposes the measured session-search contract and scanning implementation. | added |

## 7. Extension and plugin surface

**opi:** The core extension contract contributes tools, providers, model overrides, hooks, events, commands, and state; executable adapters use a manifest-selected child-process JSONL protocol (`crates/opi-agent/src/extension.rs:164-302`, `crates/opi-coding-agent/src/adapter_protocol.rs:1-4`, `crates/opi-coding-agent/src/adapter_protocol.rs:44`). The coding product wires several contribution types but explicitly excludes extension tools from its tool registry (`crates/opi-coding-agent/src/harness.rs:1490-1514`, `crates/opi-coding-agent/src/harness.rs:1603-1609`).

**pi:** Extension modules are loaded in-host with `jiti`; their APIs register tools, commands, shortcuts, flags, renderers, providers, widgets, header/footer, and editor components, and extension tools enter the live product registry (`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/loader.ts:490-518`, `.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/types.ts:171-262`, `.repo/pi-0.84.3/packages/coding-agent/src/core/agent-session.ts:2588-2654`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Extension tools in the product | Tools are a core contribution type but are excluded from coding-product registration (`crates/opi-agent/src/extension.rs:164-302`, `crates/opi-coding-agent/src/harness.rs:1603-1609`). | Extension tools enter the live product registry (`.repo/pi-0.84.3/packages/coding-agent/src/core/agent-session.ts:2588-2654`). | opi declares extension tools below the product and excludes them at product assembly; pi composes them into the product registry. | refined |
| UI, CLI, and renderer registration | Custom commands exist; CLI flags, shortcuts, widgets, header/footer/editor components, and message-renderer registration are absent: searched `crates/opi-agent/src/extension.rs`, `crates/opi-coding-agent/src/adapter_extension.rs`, `crates/opi-coding-agent/src/adapter_protocol.rs`, `crates/opi-coding-agent/src/resource.rs`, and `crates/opi-coding-agent/src/runtime_packages.rs`. | The extension API registers the cited command, shortcut, flag, renderer, and UI categories (`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/types.ts:171-262`, `.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/types.ts:1281-1336`). | pi exposes the cited UI/CLI/render categories; opi exposes commands but not the other categories in the stated scope. | refined |
| Registration timing | Registration requires unique ownership of the registry list and fails after sharing (`crates/opi-agent/src/extension.rs:308-379`). | Factory registrations commit only after successful initialization, and loaded providers can later be registered or unregistered (`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/loader.ts:256-275`, `.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/runner.ts:354-410`). | opi fixes registration before sharing; pi combines transactional factory initialization with live provider mutation. | confirmed |
| Execution boundary | Child-process stdin/stdout JSONL (`crates/opi-coding-agent/src/adapter_host.rs:1-15`, `crates/opi-coding-agent/src/adapter_host.rs:183-223`). | In-host JavaScript/TypeScript factory loaded with `jiti` (`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/loader.ts:490-518`). | The extension execution boundaries are out-of-process JSONL and in-host module loading respectively. | confirmed |

## 8. Built-in tools

**opi:** The eight built-ins are `read`, `write`, `edit`, `bash`, `grep`, `find`, `ls`, and `glob`; run-mode policy gives noninteractive mutation a class-based opt-in gate (`crates/opi-coding-agent/src/policy.rs:18-24`, `crates/opi-coding-agent/src/policy.rs:33-75`).

**pi:** The eight built-ins are `read`, `bash`, `powershell`, `edit`, `write`, `grep`, `find`, and `ls`; selection composes settings, CLI allow/deny filters, and SDK inputs (`.repo/pi-0.84.3/packages/coding-agent/src/core/tools/index.ts:93-105`, `.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:32-35`, `.repo/pi-0.84.3/packages/coding-agent/src/core/sdk.ts:59-73`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Built-in membership | Eight tools, including `glob` (`crates/opi-coding-agent/src/policy.rs:18-24`). | Eight tools, including `powershell` (`.repo/pi-0.84.3/packages/coding-agent/src/core/tools/index.ts:93-105`). | `glob` is unique to the cited opi set; `powershell` is unique to the cited pi set. | confirmed |
| Default selection | Noninteractive mutation requires class-based opt-in in addition to allowlisting (`crates/opi-coding-agent/src/policy.rs:33-75`, `crates/opi-coding-agent/src/cli.rs:93-112`). | Tool selection uses settings, CLI allow/deny filters, and SDK inputs (`.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:32-35`, `.repo/pi-0.84.3/packages/coding-agent/src/core/sdk.ts:59-73`). | opi adds a run-mode mutation gate; pi applies general name-based filters. | refined |
| File-mutation queue | absent: searched `crates/opi-coding-agent/src/tool`, `crates/opi-agent/src/tool.rs`, and `crates/opi-agent/src/agent_loop.rs`. | Same-real-path writes and edits serialize while different paths remain parallel (`.repo/pi-0.84.3/packages/coding-agent/src/core/tools/file-mutation-queue.ts:4-60`). | Only pi contains the cited per-file serialization mechanism. | confirmed |
| Read-only default set | Five tools: `read`, `grep`, `find`, `ls`, and `glob` (`crates/opi-coding-agent/src/policy.rs:18-24`). | Four tools: `read`, `grep`, `find`, and `ls` (`.repo/pi-0.84.3/packages/coding-agent/src/core/tools/index.ts:164-180`). | opi's cited read-only set additionally includes `glob`. | added |

## 9. Terminal UI

**opi:** `opi-tui` is a synchronous Ratatui widget library exporting product-oriented message, editor, status, tool-call, permission, and trust views; configurable bindings are fixed to submit, abort, and newline (`crates/opi-tui/src/lib.rs:1-40`, `crates/opi-tui/src/keybindings.rs:36-42`).

**pi:** The TUI package exports generic components plus component, overlay, and layout-root APIs; its action maps support declaration merging and a registry (`.repo/pi-0.84.3/packages/tui/src/index.ts:12-61`, `.repo/pi-0.84.3/packages/tui/src/index.ts:116-138`, `.repo/pi-0.84.3/packages/tui/src/keybindings.ts:3-69`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Component surface | Product-oriented Ratatui widgets (`crates/opi-tui/src/lib.rs:1-40`). | Generic components plus component, overlay, and layout-root APIs (`.repo/pi-0.84.3/packages/tui/src/index.ts:12-61`, `.repo/pi-0.84.3/packages/tui/src/tui.ts:84-188`). | The cited exports emphasize product views in opi and generic component/layout contracts in pi. | refined |
| Keybindings | Fixed submit, abort, and newline fields (`crates/opi-tui/src/keybindings.rs:36-42`, `crates/opi-tui/src/keybindings.rs:162-202`). | Declaration-mergeable action maps managed by a registry (`.repo/pi-0.84.3/packages/tui/src/keybindings.ts:3-69`, `.repo/pi-0.84.3/packages/tui/src/keybindings.ts:231-307`). | Fixed three-field configuration versus an extensible named-action registry. | confirmed |
| Terminal images | Exposes Kitty, iTerm2, Sixel, and fallback; detection selects Kitty/iTerm2/fallback, and the Sixel encoder is empty (`crates/opi-tui/src/terminal_image.rs:7-14`, `crates/opi-tui/src/terminal_image.rs:61-129`). | Supports Kitty and iTerm2; Kitty is chunked and iTerm2 uses one OSC payload (`.repo/pi-0.84.3/packages/tui/src/terminal-image.ts:6`, `.repo/pi-0.84.3/packages/tui/src/terminal-image.ts:176-220`, `.repo/pi-0.84.3/packages/tui/src/terminal-image.ts:243-269`). | The protocol vocabularies and encoder behavior differ; only pi's Kitty path is chunked. | refined |
| Extension UI injection | absent: searched `crates/opi-agent/src`, `crates/opi-tui/src`, and `crates/opi-coding-agent/src` for extension widget, header, footer, editor, and renderer registration. | Extensions can install widgets, header/footer/editor components, and message renderers (`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/types.ts:171-262`, `.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/types.ts:1325-1336`). | Only pi exposes the cited extension UI injection APIs. | confirmed |

## 10. Skills and prompt templates

**opi:** `SKILL.md` discovery examines a layer root and immediate child directories with a simple frontmatter parser; prompt fragments are `FRAGMENT.md` directories with declared named `{{argument}}` expansion (`crates/opi-coding-agent/src/skill.rs:108-210`, `crates/opi-coding-agent/src/skill.rs:320-372`, `crates/opi-coding-agent/src/prompt_fragment.rs:1-45`).

**pi:** Skill loading is recursive, ignore-aware, and YAML-frontmatter based; skills enter the system prompt, and slash invocations expand skill or prompt-template content before submission (`.repo/pi-0.84.3/packages/agent/src/harness/skills.ts:50-175`, `.repo/pi-0.84.3/packages/coding-agent/src/core/system-prompt.ts:63-66`, `.repo/pi-0.84.3/packages/coding-agent/src/core/agent-session.ts:1120-1174`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Runtime registry use | The harness converts discovered skills/fragments to resource metadata; runtime registry construction, body formatting, and invocation expansion are absent: searched `crates/opi-coding-agent/src/harness.rs`, `crates/opi-coding-agent/src/skill.rs`, `crates/opi-coding-agent/src/prompt_fragment.rs`, and product-wide references to their registry APIs (`crates/opi-coding-agent/src/harness.rs:4163-4192`). | Skills enter the system prompt and slash invocations expand before submission (`.repo/pi-0.84.3/packages/coding-agent/src/core/system-prompt.ts:63-66`, `.repo/pi-0.84.3/packages/coding-agent/src/core/agent-session.ts:1120-1174`). | opi's cited product path records metadata; pi's cited product paths emit and expand content. | confirmed |
| Discovery and parsing | Root/immediate-child scan with a simple frontmatter parser (`crates/opi-coding-agent/src/skill.rs:108-210`, `crates/opi-coding-agent/src/skill.rs:320-372`). | Recursive, ignore-aware YAML-frontmatter loading (`.repo/pi-0.84.3/packages/agent/src/harness/skills.ts:50-175`, `.repo/pi-0.84.3/packages/coding-agent/src/core/skills.ts:168-274`). | One-directory scalar parsing versus recursive YAML parsing with ignore handling. | confirmed |
| Template substitution | Declared named `{{argument}}` values (`crates/opi-coding-agent/src/prompt_fragment.rs:1-45`, `crates/opi-coding-agent/src/prompt_fragment.rs:493-540`). | `$1`, `$@`, `$ARGUMENTS`, slices, and defaults (`.repo/pi-0.84.3/packages/coding-agent/src/core/prompt-templates.ts:58-70`, `.repo/pi-0.84.3/packages/coding-agent/src/core/prompt-templates.ts:266-281`). | Named manifest arguments versus positional/all-argument substitution. | confirmed |
| Code placement | Skill and fragment machinery resides in `opi-coding-agent` (`crates/opi-coding-agent/src/skill.rs:1-22`, `crates/opi-coding-agent/src/prompt_fragment.rs:1-22`). | Reusable agent-harness skill support is integrated separately into the coding product (`.repo/pi-0.84.3/packages/agent/src/harness/skills.ts:38-175`, `.repo/pi-0.84.3/packages/coding-agent/src/core/agent-session.ts:1120-1174`). | Product-crate placement versus agent-harness placement plus product integration. | confirmed |
| Template resource layout | Directories containing `FRAGMENT.md` are discovered (`crates/opi-coding-agent/src/prompt_fragment.rs:9-22`, `crates/opi-coding-agent/src/prompt_fragment.rs:404-455`). | Individual `.md` prompt-template files are discovered nonrecursively (`.repo/pi-0.84.3/packages/coding-agent/src/core/prompt-templates.ts:9-14`, `.repo/pi-0.84.3/packages/coding-agent/src/core/prompt-templates.ts:136-176`). | The template packaging unit is a directory in opi and a Markdown file in pi. | added |

## 11. CLI modes

**opi:** The CLI exposes default interactive mode plus noninteractive, NDJSON, and stdin/stdout JSONL RPC modes, along with models, completions, doctor, and session subcommands (`crates/opi-coding-agent/src/cli.rs:87-206`, `crates/opi-coding-agent/src/cli.rs:208-257`, `crates/opi-coding-agent/src/cli.rs:297-318`).

**pi:** The CLI exposes default interactive mode plus print mode and text, JSON, or RPC output modes; session inputs cover continue, resume selection, explicit session, fork, no-session, and custom session directory (`.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:11-58`, `.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:121-165`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Runtime modes | Interactive, noninteractive, NDJSON, and RPC JSONL (`crates/opi-coding-agent/src/cli.rs:87-206`). | Interactive plus print mode with text, JSON, and RPC output (`.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:11-58`, `.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:157-165`). | Both expose interactive, noninteractive, JSON-event, and RPC categories with different flag structures. | confirmed |
| Doctor and completions | Top-level doctor and completion generation (`crates/opi-coding-agent/src/cli.rs:255-257`, `crates/opi-coding-agent/src/cli.rs:297-318`). | absent: searched `.repo/pi-0.84.3/packages/coding-agent/src/cli.ts`, `.repo/pi-0.84.3/packages/coding-agent/src/cli`, `.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts`, `.repo/pi-0.84.3/packages/coding-agent/src/core/package-manager.ts`, and `.repo/pi-0.84.3/packages/coding-agent/src/bun/cli.ts`. | Only opi exposes both cited operator commands. | confirmed |
| Session CLI | Resume/fork plus list, delete, and Markdown/JSON export (`crates/opi-coding-agent/src/cli.rs:208-253`). | Resume/fork plus continue/latest, selector behavior, no-session, custom session directory, and HTML export (`.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:11-58`, `.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:283-313`). | The shared resume/fork surface is extended by different session-selection, lifecycle, and export operations. | refined |
| RPC command union | The canonical union has 12 variants, including extension command, trace, and quit (`crates/opi-agent/src/sdk.rs:48-166`). | `RpcCommand` has 32 variants, including state queries, model cycling, queue modes, Bash, session-tree queries, and command catalogs (`.repo/pi-0.84.3/packages/coding-agent/src/modes/rpc/rpc-types.ts:20-73`). | The measured command families and union sizes differ. | refined |
| RPC prompt media | `prompt` accepts text only (`crates/opi-agent/src/sdk.rs:59-64`). | RPC prompt accepts text plus image attachments (`.repo/pi-0.84.3/packages/coding-agent/src/modes/rpc/rpc-types.ts:20-25`). | Only pi's local RPC prompt carries image attachments. | added |

## 12. Packaging and update

**opi:** Package commands add, remove, list, diagnose, enable, and disable local/Git packages whose manifests can contribute adapters, extensions, skills, fragments, and themes; lock and activation records bind source and executable material (`crates/opi-coding-agent/src/cli.rs:320-363`, `crates/opi-coding-agent/src/package_store.rs:75-150`, `crates/opi-coding-agent/src/package_store.rs:287-314`).

**pi:** Package commands install, remove, update, and list npm/Git/local packages, with a separate configuration TUI; manifests contribute extensions, skills, prompts, and themes (`.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:46`, `.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:267-290`, `.repo/pi-0.84.3/packages/coding-agent/src/core/package-manager.ts:80-84`, `.repo/pi-0.84.3/packages/coding-agent/src/core/settings-manager.ts:80-88`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Package commands | Add, remove, list, doctor, enable, and disable; enable/disable controls executable-contribution trust and activation (`crates/opi-coding-agent/src/cli.rs:320-363`, `crates/opi-coding-agent/src/package_activation.rs:517-589`). | Install, remove, update, and list, plus a separate resource-configuration TUI (`.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:46`, `.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:267-290`). | opi exposes diagnostic and executable activation commands; pi exposes update and separate resource configuration. | refined |
| Update and self-update | absent: searched `crates/opi-coding-agent/src/cli.rs`, `crates/opi-coding-agent/src/package_cli.rs`, `crates/opi-coding-agent/src/package_store.rs`, and `crates/opi-coding-agent/src/package_resolver.rs`. | Update targets cover self, extension/package sources, models, and combined updates (`.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:336-357`, `.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:1006-1051`). | Only pi exposes the cited update targets. | confirmed |
| Sources and resources | Local/Git sources with adapters, extensions, skills, fragments, and themes (`crates/opi-coding-agent/src/package_store.rs:75-150`, `crates/opi-coding-agent/src/package_discovery.rs:193-232`). | npm/Git/local sources with extensions, skills, prompts, and themes (`.repo/pi-0.84.3/packages/coding-agent/src/core/package-manager.ts:80-84`, `.repo/pi-0.84.3/packages/coding-agent/src/core/settings-manager.ts:80-88`). | opi includes executable adapter declarations and fragments; pi includes npm distribution and prompts. | confirmed |
| Integrity and trust | Locks record source, Git commit, manifest hash, and executable material; enable and activation revalidate and invalidate trust on drift; Git cache replacement is staged with rollback (`crates/opi-coding-agent/src/package_store.rs:287-347`, `crates/opi-coding-agent/src/package_activation.rs:517-589`, `crates/opi-coding-agent/src/package_activation.rs:618-745`). | Project trust gates project-local loading and writes; installer-managed self-update stages artifacts, runs `npm ci --ignore-scripts`, smoke-tests the version, and activates it (`.repo/pi-0.84.3/packages/coding-agent/src/core/project-trust.ts:24-95`, `.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:81-123`, `.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:171-220`). | opi's cited controls bind contribution activation and Git-cache replacement; pi's cited controls bind project resources and managed self-update. | refined |

## 13. Export and share

**opi:** Session export writes Markdown or JSON and selects the active branch or all message entries across the tree; independent flags omit tool-result output or assistant thinking and select summary, verbose, or no redaction (`crates/opi-coding-agent/src/cli.rs:48-55`, `crates/opi-coding-agent/src/session_cli.rs:481-550`).

**pi:** JSONL export serializes the current branch as a linear parent chain, while HTML export serializes all entries with the current leaf; `/share` uploads an organization-visible Radius artifact or creates a private GitHub gist (`.repo/pi-0.84.3/packages/coding-agent/src/core/session-export.ts:6-40`, `.repo/pi-0.84.3/packages/coding-agent/src/core/export-html/index.ts:251-270`, `.repo/pi-0.84.3/packages/coding-agent/src/modes/interactive/session-share.ts:91-196`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Export format and tree scope | Markdown or JSON; active branch or all message entries across branches in file order (`crates/opi-coding-agent/src/session_cli.rs:402-409`, `crates/opi-coding-agent/src/session_cli.rs:481-515`). | JSONL current branch or HTML all entries plus current leaf (`.repo/pi-0.84.3/packages/coding-agent/src/core/session-export.ts:6-40`, `.repo/pi-0.84.3/packages/coding-agent/src/core/export-html/index.ts:251-270`). | The formats differ, and opi's full-tree output contains message entries rather than pi's complete entry set. | refined |
| Disclosure controls | Independent tool-output/thinking exclusions and summary, verbose, or no redaction (`crates/opi-coding-agent/src/cli.rs:242-253`, `crates/opi-coding-agent/src/session_cli.rs:518-550`). | Corresponding parameters are absent: searched `.repo/pi-0.84.3/packages/coding-agent/src/core/session-export.ts`, `.repo/pi-0.84.3/packages/coding-agent/src/core/export-html/index.ts`, and `.repo/pi-0.84.3/packages/coding-agent/src/modes/interactive/session-share.ts`; share serialization directly appends system-prompt and active-tool metadata (`.repo/pi-0.84.3/packages/coding-agent/src/modes/interactive/session-share.ts:24-42`). | Only opi exposes the cited export-time exclusion and redaction controls. | confirmed |
| Publication | A session upload/share handler is absent: searched `crates/opi-coding-agent/src` and `crates/opi-agent/src` for share, Radius, gist creation, upload, and publish implementations. | `/share` uploads Radius NDJSON when configured, otherwise invokes `gh gist create --public=false`; a selected Radius failure does not fall through to gist (`.repo/pi-0.84.3/packages/coding-agent/src/modes/interactive/session-share.ts:91-196`). | Only pi contains an in-product session publication path. | refined |

## 14. Permissions and sandbox

**opi:** Every registered-tool execution crosses a mandatory authorizer boundary, and a missing authorizer yields a controlled denial; project-trust resolvers aggregate with deny dominance (`crates/opi-agent/src/authority.rs:1-11`, `crates/opi-agent/src/agent_loop.rs:1785-1818`, `crates/opi-coding-agent/src/project_trust.rs:460-495`). The `opi-sandbox` CLI refuses unsupported confinement, while the library also exposes an explicit unrestricted `NoRestriction` seam (`crates/opi-sandbox/src/cli.rs:507-518`, `crates/opi-sandbox/src/lib.rs:20-33`).

**pi:** Validated tool calls execute unless an optional before-tool hook blocks; the product binds that hook to extension handlers when present. Project-trust extensions use the first decisive result, and sandboxing is delivered as an example extension (`.repo/pi-0.84.3/packages/agent/src/agent-loop.ts:615-700`, `.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/runner.ts:203-222`, `.repo/pi-0.84.3/packages/coding-agent/examples/extensions/sandbox/index.ts:201-225`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Tool authorization | A missing authorizer denies before execution (`crates/opi-agent/src/authority.rs:1-11`, `crates/opi-agent/src/agent_loop.rs:1785-1818`). | Calls execute unless an optional before-tool hook blocks; the confirmation gate is an example extension (`.repo/pi-0.84.3/packages/agent/src/agent-loop.ts:615-700`, `.repo/pi-0.84.3/packages/coding-agent/examples/extensions/permission-gate.ts:1-33`). | Mandatory runtime authorization in opi versus optional extension interception in pi. | confirmed |
| Project-trust arbitration | `Deny` is terminal; `Trust` remains provisional until later resolvers run (`crates/opi-coding-agent/src/project_trust.rs:460-495`). | The first handler returning yes/no wins; undecided falls through (`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/runner.ts:203-222`). | The project-trust resolver rules are deny-dominant aggregation and first decisive result respectively. | refined |
| Sandbox posture | The human CLI exits 125 when native confinement is unsupported; the library also exposes explicit unrestricted `NoRestriction` (`crates/opi-sandbox/src/platform/mod.rs:1-11`, `crates/opi-sandbox/src/cli.rs:507-518`, `crates/opi-sandbox/src/lib.rs:20-33`). | The sandbox example delegates to local Bash whenever disabled or uninitialized, including unsupported platforms and initialization failures (`.repo/pi-0.84.3/packages/coding-agent/examples/extensions/sandbox/index.ts:201-225`, `.repo/pi-0.84.3/packages/coding-agent/examples/extensions/sandbox/index.ts:234-284`). | opi ships a standalone restriction product with separate confined and explicit unrestricted paths; pi ships an optional example that falls back to local execution. | refined |

## 15. MCP

**opi:** No MCP protocol client, server, or runtime is present in core/product sources; an in-tree example models MCP-style commands over fixture data, with the Rust adapter implemented in an integration test (`examples/mcp-adapter/README.md:7-28`, `crates/opi-coding-agent/tests/mcp_adapter_example.rs:16-20`).

**pi:** The coding-agent describes extensions as the MCP integration route, but no MCP protocol client, server, runtime, or example implementation is present in the measured packages (`.repo/pi-0.84.3/packages/coding-agent/README.md:394`, `.repo/pi-0.84.3/packages/coding-agent/README.md:498`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Runtime implementation | absent: searched `crates/opi-ai/src`, `crates/opi-agent/src`, `crates/opi-coding-agent/src`, `crates/opi-protocol/src`, and `Cargo.toml` for MCP protocol/runtime/dependency symbols. | absent: searched `.repo/pi-0.84.3/packages/ai/src`, `.repo/pi-0.84.3/packages/agent/src`, `.repo/pi-0.84.3/packages/coding-agent/src`, `.repo/pi-0.84.3/packages/protocol/src`, `.repo/pi-0.84.3/packages/client/src`, `.repo/pi-0.84.3/packages/server/src`, and their manifests. | Neither measured core/product tree contains an MCP runtime implementation. | confirmed |
| In-tree adapter material | Fixture-only MCP-style manifests, with adapter code in an integration test (`examples/mcp-adapter/README.md:7-28`, `examples/mcp-adapter/README.md:40-42`, `crates/opi-coding-agent/tests/mcp_adapter_example.rs:273-305`). | absent: searched `.repo/pi-0.84.3/packages/ai/examples`, `.repo/pi-0.84.3/packages/agent/examples`, and `.repo/pi-0.84.3/packages/coding-agent/examples` for MCP examples. | Only opi contains the cited in-tree fixture adapter material. | refined |

## 16. Image generation

**opi:** The product supports user image input and generic tool-result image content, but a dedicated image-generation model, provider, request, or route API is absent in the measured source (`crates/opi-coding-agent/src/image.rs:1-8`, `crates/opi-coding-agent/src/image.rs:22-57`, `crates/opi-ai/src/message.rs:91-127`).

**pi:** `pi-ai` exposes `generateImages()`, `ImagesModels`, provider-resolved authentication, a built-in OpenRouter image provider, and a generated image-model catalog (`.repo/pi-0.84.3/packages/ai/src/images.ts:14-20`, `.repo/pi-0.84.3/packages/ai/src/images-models.ts:45-88`, `.repo/pi-0.84.3/packages/ai/src/providers/openrouter-images.ts:7-21`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Library generation API | User image input and generic tool-result images exist; dedicated generation APIs are absent: searched `crates/opi-ai/src`, `crates/opi-agent/src`, `crates/opi-coding-agent/src`, and `Cargo.toml` for image-generation models, providers, requests, and routes (`crates/opi-coding-agent/src/image.rs:1-8`, `crates/opi-ai/src/message.rs:91-127`). | `generateImages()` plus `ImagesProvider` and `ImagesModels` (`.repo/pi-0.84.3/packages/ai/src/images.ts:14-20`, `.repo/pi-0.84.3/packages/ai/src/images-models.ts:8-88`). | Only pi exposes a dedicated library generation API. | refined |
| Built-in generation provider | absent: searched `crates/opi-ai/src` for image-generation provider and model-catalog APIs. | OpenRouter adapter plus generated catalog (`.repo/pi-0.84.3/packages/ai/src/providers/openrouter-images.ts:7-21`, `.repo/pi-0.84.3/packages/ai/src/image-models.generated.ts:6-22`). | Only pi contains a built-in image-generation provider. | confirmed |
| Coding-product command/tool | absent: searched `crates/opi-coding-agent/src` for image-generation command/tool registration. | absent: searched `.repo/pi-0.84.3/packages/coding-agent/src` for `generateImages`, `ImagesModels`, `ImagesProvider`, and generation command/tool registration. | Neither coding product registers a built-in image-generation command or tool. | confirmed |
| Extension access to generation library | absent: searched `crates/opi-ai/src`, `crates/opi-agent/src`, and `crates/opi-coding-agent/src` for an image-generation library or bundled generation-provider module exposed to extensions. | The extension loader exposes bundled `pi-ai/compat` and `pi-ai/providers/all` modules (`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/loader.ts:59-72`). | pi extensions can consume the generation library even though the product has no built-in generation command/tool. | added |

## 17. Diagnostics and observability

**opi:** A shared structured diagnostic carries stable codes and explicit public redaction; `opi doctor` is local/network-free across six scopes, and opt-in trace capture writes per-run evidence JSONL plus a manifest (`crates/opi-agent/src/diagnostic.rs:70-112`, `crates/opi-coding-agent/src/doctor.rs:3-17`, `crates/opi-coding-agent/src/evidence.rs:52-64`).

**pi:** Diagnostics cover argument parsing, service creation, settings, and resources. The telemetry package defines callback spans, typed schemas, and no-op/in-memory adapters; measured runtime source has no durable telemetry exporter (`.repo/pi-0.84.3/packages/coding-agent/src/core/diagnostics.ts:1-15`, `.repo/pi-0.84.3/packages/telemetry/src/index.ts:14-24`, `.repo/pi-0.84.3/packages/telemetry/src/memory.ts:188-218`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| Operator health command | `opi doctor` covers config, provider, package, session, TUI, and RPC without network calls (`crates/opi-coding-agent/src/doctor.rs:3-17`, `crates/opi-coding-agent/src/doctor.rs:59-84`). | A consolidated doctor command is absent: searched `.repo/pi-0.84.3/packages/coding-agent/src/main.ts`, `.repo/pi-0.84.3/packages/coding-agent/src/cli`, and `.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts`. | Only opi contains a consolidated health command. | confirmed |
| Diagnostic contract | Severity, stable code, source, message, details, action, and a fully redacted public payload (`crates/opi-agent/src/diagnostic.rs:70-112`, `crates/opi-agent/src/diagnostic.rs:144-163`). | Argument, service, settings, and resource diagnostics use narrower runtime/resource records; a shared diagnostic redaction method is absent in the cited files (`.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:53-58`, `.repo/pi-0.84.3/packages/coding-agent/src/core/diagnostics.ts:1-15`). | The diagnostic schemas and public-boundary treatment differ. | refined |
| Durable evidence versus telemetry spans | Opt-in file sink appends `evidence.jsonl` and durably publishes `manifest.json` per run (`crates/opi-coding-agent/src/evidence.rs:52-64`, `crates/opi-coding-agent/src/evidence.rs:166-215`, `crates/opi-coding-agent/src/evidence.rs:300-391`). | Callback spans, typed schemas, and no-op/in-memory adapters; runtime call sites for span helpers and durable/file/OTLP exporters are absent: searched `.repo/pi-0.84.3/packages/telemetry/src`, `.repo/pi-0.84.3/packages/agent/src`, and `.repo/pi-0.84.3/packages/coding-agent/src` (`.repo/pi-0.84.3/packages/telemetry/src/index.ts:14-24`, `.repo/pi-0.84.3/packages/telemetry/src/memory.ts:188-218`). | opi ships durable run evidence; pi ships telemetry contracts and reference adapters without a measured durable exporter. | refined |
| Install/update version ping | absent: searched `crates/opi-coding-agent/src` and `crates/opi-agent/src` for install/update telemetry, version pings, and version-ping call sites. | Interactive mode sends an opt-out install/update version ping after fresh install or detected updates; offline mode and disabled install telemetry skip it (`.repo/pi-0.84.3/packages/coding-agent/src/modes/interactive/interactive-mode.ts:1263-1300`, `.repo/pi-0.84.3/packages/coding-agent/src/core/telemetry.ts:8-13`). | Only pi contains the measured product version ping. | added |

## 18. Wire protocols and embedding

**opi:** `CodingHarness` is the in-process product wrapper; a separate schema-v3 stdin/stdout JSONL RPC controls the coding agent, while `opi-protocol` defines a one-execution command-backend JSONL wire rather than a remote session service (`crates/opi-coding-agent/src/harness.rs:655-703`, `crates/opi-coding-agent/src/rpc.rs:1-36`, `crates/opi-protocol/src/lib.rs:1-17`).

**pi:** `createAgentSession()` is the in-process product API, the coding product has a separate local JSONL RPC, and experimental protocol/client/server packages define a length-prefixed CBOR multi-session stack with a byte-transport client abstraction (`.repo/pi-0.84.3/packages/coding-agent/src/core/sdk.ts:173-188`, `.repo/pi-0.84.3/packages/coding-agent/src/modes/rpc/rpc-types.ts:1-73`, `.repo/pi-0.84.3/packages/protocol/src/schemas.ts:260-325`, `.repo/pi-0.84.3/packages/client/src/transport.ts:1-18`).

| Item | opi | pi | Difference | Verification outcome |
|---|---|---|---|---|
| In-process embedding | `CodingHarness` exposes a builder and prompt operations (`crates/opi-coding-agent/src/harness.rs:655-703`, `crates/opi-coding-agent/src/harness.rs:1214-1224`, `crates/opi-coding-agent/src/harness.rs:2828-2869`). | `createAgentSession()` constructs an `AgentSession` (`.repo/pi-0.84.3/packages/coding-agent/src/core/sdk.ts:39-75`, `.repo/pi-0.84.3/packages/coding-agent/src/core/sdk.ts:173-188`). | Both embed through product-level session APIs with different types. | confirmed |
| Local agent RPC | Schema-v3 stdin/stdout JSONL includes continue, extension command, trace, and quit (`crates/opi-coding-agent/src/rpc.rs:1-36`, `crates/opi-agent/src/sdk.rs:37-130`). | Stdin/stdout JSONL includes image-bearing prompt, new session, model cycling, Bash, HTML export, branch/tree, and transcript queries (`.repo/pi-0.84.3/packages/coding-agent/src/modes/rpc/rpc-types.ts:1-73`). | Both products expose local JSONL control surfaces with different command unions. | confirmed |
| Remote multi-session wire | A remote product-session client/server is absent: searched `crates/opi-agent/src`, `crates/opi-coding-agent/src`, and `crates/opi-protocol/src`; `opi-protocol` instead owns one-shot command execution (`crates/opi-protocol/src/lib.rs:1-17`, `crates/opi-protocol/src/execution/v1/mod.rs:1-23`). | Protocol v1 uses validated length-prefixed CBOR, session-addressed commands, and a server that manages multiple live sessions (`.repo/pi-0.84.3/packages/protocol/src/schemas.ts:260-325`, `.repo/pi-0.84.3/packages/protocol/src/codec.ts:60-85`, `.repo/pi-0.84.3/packages/server/src/sessions.ts:38-115`). | Only pi contains the measured remote multi-session product stack. | refined |
| Client transport implementation | A remote-session byte-transport seam is absent: searched `crates/opi-agent/src`, `crates/opi-coding-agent/src`, and `crates/opi-protocol/src` for `ByteTransport`, CBOR, WebSocket, and session socket listeners. | `ByteTransport` is abstract; the shipped implementation is a Node Unix-domain socket transport. A WebSocket/browser implementation is absent: searched `.repo/pi-0.84.3/packages/protocol/src`, `.repo/pi-0.84.3/packages/client/src`, and `.repo/pi-0.84.3/packages/server/src` (`.repo/pi-0.84.3/packages/client/src/transport.ts:1-18`, `.repo/pi-0.84.3/packages/client/src/unix.ts:1-23`). | pi exposes a runtime-neutral byte seam and one Unix-socket adapter, not a shipped WebSocket/browser client; opi exposes neither in the stated scope. | refined |
| Third command union | Opi's measured command unions are the local coding-agent RPC and one-shot execution protocol (`crates/opi-agent/src/sdk.rs:37-130`, `crates/opi-protocol/src/execution/v1/mod.rs:1-23`). | The remote protocol has a separate 9-command union: list, create, attach, detach, prompt, steer, abort, set model, and set thinking (`.repo/pi-0.84.3/packages/protocol/src/schemas.ts:291-325`). | pi's remote protocol adds a third, narrower control surface distinct from both products' local RPC unions. | added |

## Layer H: target design horizon

The pinned pi tree contains `packages/agent/docs/harness.md`, titled `AgentHarness — implementation specification`. The document states that only its `must` / `must not` clauses are normative and that other text is explanatory (`.repo/pi-0.84.3/packages/agent/docs/harness.md:1`, `.repo/pi-0.84.3/packages/agent/docs/harness.md:221`). It also contains `packages/agent/docs/search.md`, whose final paragraph names a follow-up indexing seam without declaring that proposal normative (`.repo/pi-0.84.3/packages/agent/docs/search.md:270-276`). The rows below preserve those authority boundaries. They do not treat the complete design as shipped pi behavior or as an Opi requirement.

### H5-H6. Agent harness and durable session design

| Design item | pi target design | Design authority/status | pi current evidence | Observed maturity | opi current evidence | Objective horizon difference | Verification outcome |
|---|---|---|---|---|---|---|---|
| Durable session system model | A session combines an immutable entry tree, mutable facts, named lanes, and an append-only usage ledger; each lane owns its leaf, configuration, queues, and at most one operation (`.repo/pi-0.84.3/packages/agent/docs/harness.md:88-105`). | Explanatory system model; only explicit `must` / `must not` clauses are self-declared normative (`.repo/pi-0.84.3/packages/agent/docs/harness.md:221`). | Agent-core v4 exposes lane/storage/repo contracts and implemented lane/operation delegation with JSONL-focused tests, while the coding product still persists its separate v3 current-leaf model (`.repo/pi-0.84.3/packages/agent/src/harness/session/types.ts:273-372`, `.repo/pi-0.84.3/packages/agent/src/harness/session/session.ts:190-219`, `.repo/pi-0.84.3/packages/agent/test/harness/session/jsonl.test.ts:273-322`, `.repo/pi-0.84.3/packages/agent/CHANGELOG.md:30-40`, `.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:30`, `.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:847-866`). | `partial` | Opi currently exposes a parent-entry graph, one selected active branch, `SessionRepo`, and `SessionFacade`; named lane declarations are absent in searches of `crates/opi-agent/src` and `crates/opi-coding-agent/src` (`crates/opi-agent/src/session.rs:143-174`, `crates/opi-agent/src/session_branch.rs:30-43`, `crates/opi-agent/src/harness.rs:142-255`). | pi's target converges tree, facts, named lanes, usage, and durable operations into one session model; the cited Opi model currently exposes a parent graph, repository, and facade without that complete named-lane contract. | refined |
| Harness responsibility and durable operations | The harness is intended to drive lane prompts, model/tool steps, queues, compaction, navigation, and resume; it owns registries, hooks, events, configuration, and total durable operation state (`.repo/pi-0.84.3/packages/agent/docs/harness.md:101-110`). | Explanatory target model under the document's clause-level authority rule (`.repo/pi-0.84.3/packages/agent/docs/harness.md:221`). | The v0.84.3 changelog calls the public v2 surface a compile-complete scaffold, and focused tests show configuration accessors working while prompt, compaction, navigation, resume, queue, lane, watch, hook, and event paths reject with `HarnessNotImplemented` (`.repo/pi-0.84.3/packages/agent/CHANGELOG.md:39`, `.repo/pi-0.84.3/packages/agent/test/harness/agent-harness-scaffold.test.ts:84-187`). | `scaffold` | Opi distributes the current loop, durable-session facade, and product policy across `Agent`, `SessionFacade`, and `CodingHarness`; a public type named `AgentHarness` is absent in the searched agent and coding-agent source (`crates/opi-agent/src/agent.rs:539-563`, `crates/opi-agent/src/harness.rs:217-255`, `crates/opi-coding-agent/src/harness.rs:655-703`). | pi documents one future harness boundary for responsibilities that Opi currently distributes across runtime, session, and product wrappers. The current pi scaffold does not yet establish behavioral parity for that target. | confirmed |
| Build sequence and transition from the scaffold | The implementation specification separates storage/search work from a sequential runtime track R1-R12; R1 permits public operations to remain unimplemented, while R12 removes remaining scaffold code (`.repo/pi-0.84.3/packages/agent/docs/harness.md:2782-2807`). It also says the existing harness source and tests may be deleted rather than adapted (`.repo/pi-0.84.3/packages/agent/docs/harness.md:2809-2815`). | Explanatory implementation sequence under the document's clause-level authority rule (`.repo/pi-0.84.3/packages/agent/docs/harness.md:221`). | v0.84.3 still contains the scaffold and scaffold tests that the target sequence permits slice 1 to delete; the changelog does not declare a completed target R slice (`.repo/pi-0.84.3/packages/agent/CHANGELOG.md:39`, `.repo/pi-0.84.3/packages/agent/test/harness/agent-harness-scaffold.test.ts:56-187`). | `designed-only` | Not applicable: this item records pi's own transition path rather than an Opi current capability. | The build sequence is trajectory evidence explaining why v0.84.3 is not the final Harness v2 architecture; no Opi parity conclusion follows from slice order alone. | refined |
| External-effect guarantees | Exactly-once external effects, provider stream resumption, multiple session writers, replication, durable register history, and runtime deletion are explicit non-goals of the target design (`.repo/pi-0.84.3/packages/agent/docs/harness.md:207-214`). | Explicit non-goal. | N/A: this row records target boundaries rather than implementation maturity. | `not-assessed` | Not applicable: a pi non-goal does not create an Opi parity requirement. | No Opi parity conclusion follows; these constraints bound how the target design should be interpreted. | confirmed |
| Future Postgres retention | The partitioned-retention section describes a possible Postgres deployment and says it is a current best guess rather than a contract (`.repo/pi-0.84.3/packages/agent/docs/harness.md:2700-2710`). | Explicitly informative. | N/A: the source explicitly exempts shipping backends from this future sketch. | `not-assessed` | Not applicable: non-binding upstream future material is recorded without asserting an Opi gap. | No Opi parity conclusion follows; this is retained as design-lineage evidence only. | confirmed |
| Unresolved design questions | Repairing a missing model in an open operation, overflow heuristics, and pending-payload write amplification remain open questions (`.repo/pi-0.84.3/packages/agent/docs/harness.md:2937-2941`). | Explicit open questions. | N/A: the target document does not claim these questions are settled. | `not-assessed` | Not applicable: unresolved pi design does not define an Opi target. | No Opi parity conclusion follows until pi resolves these questions in authority-scoped design or implementation evidence. | confirmed |
| Search indexing integration | The session-search document says opt-in still needs a synchronization/indexing layer and proposes a no-op-by-default index sink so canonical writes can emit indexing events unconditionally (`.repo/pi-0.84.3/packages/agent/docs/search.md:270-276`). | Explanatory proposal; the document does not declare it normative. | The query/scanning API and SQLite FTS rebuild/trigger maintenance are implemented, but the proposed shared sink is absent: searched `.repo/pi-0.84.3/packages/agent/src/search` and `.repo/pi-0.84.3/packages/agent/src/harness` for `SearchIndexSink`, `NOOP_SEARCH_INDEX_SINK`, and index-event sink implementations (`.repo/pi-0.84.3/packages/agent/src/search/index.ts:14-31`, `.repo/pi-0.84.3/packages/agent/src/search/scanning.ts:135-176`, `.repo/pi-0.84.3/packages/session-backends/sqlite-node/src/sqlite/search-backend.ts:61-98`, `.repo/pi-0.84.3/packages/session-backends/sqlite-node/test/search.test.ts:170-188`). | `designed-only` | Session search and an index-event sink are absent: searched `crates/opi-agent/src` and `crates/opi-coding-agent/src`. | pi has current query/scanning and backend-specific FTS maintenance plus an explanatory proposal for a shared indexing sink; Opi has no current counterpart in the searched scope. This horizon delta does not assign Opi priority. | refined |

## Appendix B: method and verification

- Dimension count: 18 of 18.
- Retained delta count: 68.
- Retained outcome tally: 36 `confirmed`, 25 `refined`, 7 `added`, 0 `refuted`.
- Layer H count: 7 target-design items. Observed-maturity tally: 1 `partial`, 1 `scaffold`, 2 `designed-only`, and 3 `not-assessed`; verification tally: 4 `confirmed` and 3 `refined`.
- Independent coverage: dimensions 1-6 were measured and then verified by the worker that measured 13-18; dimensions 7-12 were verified by the worker that measured 1-6; dimensions 13-18 were verified by the worker that measured 7-12. No dimension verified its own first pass.
- Layer H coverage: all seven items received an independent follow-up verifier pass; three were refined to correct evidence or maturity labels.
- Layer H design search: inspected `.repo/pi-0.84.3/packages/agent/docs` and `.repo/pi-0.84.3/packages/coding-agent/docs` for implementation specifications, architecture/design material, and explicitly forward-looking sections; the follow-up retained `harness.md` and `search.md` as consequential target-horizon evidence.
- Currentness: opi's empty `Unreleased` section precedes release 0.8.1 (`CHANGELOG.md:8-10`). The measured pi package changelogs identify 0.84.3 as current, including coding-agent, AI, protocol, client, server, agent, TUI, and telemetry (`.repo/pi-0.84.3/packages/coding-agent/CHANGELOG.md:3`, `.repo/pi-0.84.3/packages/ai/CHANGELOG.md:3`, `.repo/pi-0.84.3/packages/protocol/CHANGELOG.md:3`).
- First-pass statements dropped or replaced during verification: opi does implement concrete SSE; opi-sandbox is not uniformly restrictive because the library exposes `NoRestriction`; pi's `AgentHarness` has implemented state/configuration accessors; pi does not ship a WebSocket/browser client; and opi's image surface includes tool-result images in addition to user input. Their narrower replacement rows are marked `refined`.
- A verifier also found stale v1 wording in `SessionFacade`/`JsonlSessionRepo` rustdoc alongside the v2 owning session module (`crates/opi-agent/src/harness.rs:142-164`, `crates/opi-agent/src/session.rs:1-21`). It is recorded here as an internal evidence inconsistency, not retained as a cross-project delta.
- Unavailable evidence: the supplied pi snapshot contains no nested `.git` directory. Revision identity was established by resolving upstream tag `v0.84.3` and byte-comparing the snapshot with that commit's tracked archive. External extension packages and downstream embedders were outside the source-tree scope; all absence claims are limited to their stated paths.
