# opi 与 pi v0.84.3——客观差异

- 测量日期：2026-08-25。
- opi 根目录：`D:\Luiz\Odradek\opi`；版本 `0.8.1`（`Cargo.toml:13`）；提交 `8944d5ad1fb23cc6a3c4e70078981e2ca46eec0b`。
- pi 根目录：`D:\Luiz\Odradek\opi\.repo\pi-0.84.3`；版本 `0.84.3`（`.repo/pi-0.84.3/packages/coding-agent/package.json:3`）；标签 `v0.84.3`；提交 `4e58f324fae8ebfa98a3d45181fb248072a2afac`。
- 已验证的 pi 远程仓库：`https://github.com/earendil-works/pi.git`。`git ls-remote` 将 `v0.84.3` 解析到所记录的提交。所提供的快照不含嵌套的 Git 元数据，因此将其文件与该提交全新检出后生成的 `git archive` 进行了逐字节比较；未发现已跟踪文件存在差异。
- 范围：完整内向比较，涵盖 `opi-realign` 中的全部 18 个维度。
- 方法：在三个有界批次中基于双方当前源码进行测量；每个保留的差异均由不同的工作者对照源码和相关变更日志检查。所有维度都经过了独立验证。
- 表述框架：Layer A 不采用阶段、基线或路线图框架；Layer H 单独记录固定目标设计证据及其成熟度。

## 阅读方式

项目段落和项目列均为有引用依据的当前事实。`Difference` 仅记录所测量源码树之间的原始差异。缺失项会列明搜索过的源码路径。Layer H 记录固定版本中的目标意图及已观察到的成熟度，不代表已交付行为，也不构成 Opi 需求。`Verification outcome` 记录独立的第二轮检查结果；本次未要求漂移分类或建议附录。

## 1. Provider 抽象与分派

**opi：** `ProviderCollection::prepare_call` 解析规范路由、验证请求、一次性解析身份验证，并在尝试开始前冻结 Provider、请求、身份验证和路由；`Provider::stream_prepared` 使用该已准备状态（`crates/opi-ai/src/provider_collection.rs:400-478`、`crates/opi-ai/src/provider_collection.rs:684-702`、`crates/opi-ai/src/provider.rs:29-44`）。

**pi：** Provider 声明身份验证和流式处理行为，而 `Models.applyAuth()` 在惰性流委托给 `provider.stream()` 之前解析并应用身份验证；模型记录同时携带 Provider 标识和 API 标识（`.repo/pi-0.84.3/packages/ai/src/models.ts:89-154`、`.repo/pi-0.84.3/packages/ai/src/models.ts:636-679`、`.repo/pi-0.84.3/packages/ai/src/types.ts:820-826`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 调用准备 | 集合在尝试前冻结路由、请求和已解析的身份验证（`crates/opi-ai/src/provider_collection.rs:400-478`）。 | `Models` 在委托给 Provider 前按请求应用身份验证（`.repo/pi-0.84.3/packages/ai/src/models.ts:636-679`）。 | opi 的准备工作归集合所有，并在各次尝试间保持冻结；pi 在 Provider 流式处理前由 `Models` 解析身份验证。 | refined |
| 路由标识 | 选择采用规范的 `provider:model`，已准备路由会记录 Provider、模型和 wire API（`crates/opi-ai/src/provider_collection.rs:400-425`、`crates/opi-ai/src/provider_collection.rs:466-475`）。 | 模型记录 `provider` 和 `api`；Provider 工厂可按 `model.api` 选择适配器（`.repo/pi-0.84.3/packages/ai/src/types.ts:820-826`、`.repo/pi-0.84.3/packages/ai/src/models.ts:756-791`）。 | opi 在执行前解析出一个规范路由；pi 可在一个 Provider 内选择不同 API 适配器。 | confirmed |
| 刷新发布 | 在将动态目录作为一个集合替换之前，会先收集所有 Provider 的结果（`crates/opi-ai/src/provider_collection.rs:552-630`）。 | 生成、取消、发布和错误均按选定 Provider 分别维护（`.repo/pi-0.84.3/packages/ai/src/models.ts:320-445`）。 | opi 的发布作用于整个集合，pi 的发布则局限于单个 Provider。 | confirmed |

## 2. 身份验证与凭据

**opi：** 凭据存储在操作系统密钥链中，其变更由 `fs4` 锁协调；doctor 会公开凭据探测状态，但不公开凭据值（`crates/opi-coding-agent/src/credential_store.rs:3-18`、`crates/opi-coding-agent/src/doctor.rs:413-453`）。

**pi：** `AuthStorage` 将数据持久化到 `auth.json`，使用 `proper-lockfile`，并默认以 `0600` 模式写入；`pi auth check` 可选择性输出已解析的凭据（`.repo/pi-0.84.3/packages/coding-agent/src/core/auth-storage.ts:2-25`、`.repo/pi-0.84.3/packages/coding-agent/src/core/auth-storage.ts:52-76`、`.repo/pi-0.84.3/packages/coding-agent/src/cli/auth-command.ts:19-45`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 持久化 | 操作系统密钥链加跨进程凭据锁（`crates/opi-coding-agent/src/credential_store.rs:3-18`）。 | 受模式限制的 `auth.json` 加文件锁（`.repo/pi-0.84.3/packages/coding-agent/src/core/auth-storage.ts:2-25`、`.repo/pi-0.84.3/packages/coding-agent/src/core/auth-storage.ts:52-76`）。 | opi 的持久化秘密后端是操作系统密钥链，pi 的则是本地 JSON 文件。 | confirmed |
| API 密钥环境回退 | 仅当密钥链条目不存在，或后端被狭义分类为不可用时，才使用已配置的环境变量；凭据类型错误和其他存储错误会直接返回（`crates/opi-coding-agent/src/credential_store.rs:1071-1074`、`crates/opi-coding-agent/src/credential_store.rs:1107-1131`）。 | 仅当没有存储任何凭据时，才查询环境身份验证；已存储的凭据类型错误和 OAuth 失败路径都不会回退（`.repo/pi-0.84.3/packages/ai/src/auth/resolve.ts:44-48`、`.repo/pi-0.84.3/packages/ai/src/auth/resolve.ts:87-109`）。 | 回退条件不同：opi 是选定密钥链条目缺失或不可用，pi 是不存在任何已存储凭据。 | refined |
| 内置 OAuth 流程 | Anthropic、OpenAI Codex 和 GitHub Copilot：3 个流程（`crates/opi-coding-agent/src/oauth.rs:2392-2427`）。 | Anthropic、OpenAI Codex、GitHub Copilot、OpenRouter、Kimi Coding、xAI 和 Radius：7 个加载器（`.repo/pi-0.84.3/packages/ai/src/auth/oauth/load.ts:26-66`）。 | 两套内置集合分别包含 3 个和 7 个流程。 | confirmed |
| 凭据检查 | Doctor 报告凭据是否存在及探测状态，但不公开秘密（`crates/opi-coding-agent/src/doctor.rs:413-453`）。 | `pi auth check --credentials` 输出已解析的凭据（`.repo/pi-0.84.3/packages/coding-agent/src/cli/auth-command.ts:19-45`、`.repo/pi-0.84.3/packages/coding-agent/src/main.ts:177-196`）。 | pi 提供一条需主动选择、会输出凭据的 CLI 路径；opi 所引用的检查路径经过脱敏。 | confirmed |

## 3. Provider 目录

**opi：** 产品组装注册 11 个固定 Provider ID，闭合的 `WireApi` 枚举包含 8 个变体（`crates/opi-coding-agent/src/provider_factory.rs:517-530`、`crates/opi-ai/src/model_info.rs:76-110`）。

**pi：** `builtinProviders()` 构造 40 个 Provider；`KnownApi` 包含 10 个标识符，而 `Api` 还接受自定义字符串（`.repo/pi-0.84.3/packages/ai/src/providers/all.ts:89-131`、`.repo/pi-0.84.3/packages/ai/src/types.ts:17-29`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 内置 Provider | 11 个固定 Provider ID（`crates/opi-coding-agent/src/provider_factory.rs:517-530`）。 | 40 个 Provider 定义（`.repo/pi-0.84.3/packages/ai/src/providers/all.ts:89-131`）。 | 枚举出的内置目录相差 29 个 Provider 定义。 | confirmed |
| API 词汇表 | 闭合的 8 变体 `WireApi` 枚举（`crates/opi-ai/src/model_info.rs:76-110`）。 | 10 个已知 API 标识符，另加任意自定义字符串（`.repo/pi-0.84.3/packages/ai/src/types.ts:17-29`）。 | opi 拒绝枚举以外的标识符；pi 的公共 API 类型允许自定义标识符。 | confirmed |
| 产品刷新触发器 | Provider 契约未记录编程 Agent 的生产刷新触发器；未发现调用：已在 `crates/opi-ai/src`、`crates/opi-agent/src` 和 `crates/opi-coding-agent/src` 中搜索 `ProviderCollection::refresh` 和 `.refresh().await` 调用点（`crates/opi-ai/src/provider.rs:17-21`）。 | `ModelRuntime.create()` 会调用刷新，除非 `refreshOnCreate` 为 false（`.repo/pi-0.84.3/packages/coding-agent/src/core/model-runtime.ts:190-216`）。 | pi 的产品创建路径会调用刷新；在所述范围内未发现对应的 opi 产品调用点。 | confirmed |

## 4. 流与传输生命周期

**opi：** 流会规范化开始、文本、思考、工具调用、完成和错误事件；Agent 循环先准备一次逻辑调用，再基于同一个冻结调用依次重试（`crates/opi-ai/src/stream.rs:325-438`、`crates/opi-agent/src/agent_loop.rs:29-39`、`crates/opi-agent/src/agent_loop.rs:270-378`）。

**pi：** 规范化流包含延迟终止原因和持久句柄；重试既发生在适配器请求建立阶段，也发生在编程会话因可重试的助手错误而继续执行时（`.repo/pi-0.84.3/packages/ai/src/types.ts:405-419`、`.repo/pi-0.84.3/packages/ai/src/types.ts:535-551`、`.repo/pi-0.84.3/packages/ai/src/utils/provider-retry.ts:98-123`、`.repo/pi-0.84.3/packages/coding-agent/src/core/agent-session.ts:2808-2860`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 延迟完成 | 缺失延迟响应原语：已在 `crates/opi-ai/src`、`crates/opi-agent/src` 和 `crates/opi-coding-agent/src` 中搜索 `DeferredHandle`、`fetchDeferred` 和 `cancelDeferred`；`StopReason` 没有延迟变体（`crates/opi-ai/src/stream.rs:13-36`）。 | `deferred` 是一种终止完成原因，并带有经身份验证的获取/取消操作（`.repo/pi-0.84.3/packages/ai/src/types.ts:405-419`、`.repo/pi-0.84.3/packages/ai/src/models.ts:706-731`）。 | 只有 pi 在其规范化模型 API 中公开延迟完成。 | confirmed |
| 重试边界 | 一次已准备的逻辑调用会为依次进行的尝试而重新打开（`crates/opi-agent/src/agent_loop.rs:270-378`、`crates/opi-ai/src/provider_collection.rs:781-821`）。 | 重试发生在全新的适配器请求周围以及编程会话继续执行过程中（`.repo/pi-0.84.3/packages/ai/src/utils/provider-retry.ts:98-123`、`.repo/pi-0.84.3/packages/coding-agent/src/core/agent-session.ts:2808-2860`）。 | opi 所引用的重试边界是冻结的逻辑调用；pi 具有适配器级和产品会话级重试边界。 | confirmed |
| Provider 中立的传输选择器 | 存在具体的 SSE 适配器（`crates/opi-ai/src/anthropic.rs:1-54`、`crates/opi-ai/src/gemini.rs:1-33`、`crates/opi-ai/src/openai_chat.rs:1-45`）；缺失带有 `websocket`、`websocket-cached` 和 `auto` 的公共文本生成选择器：已搜索 `crates/opi-ai/src`、`crates/opi-agent/src` 和 `crates/opi-coding-agent/src`。 | 请求词汇表公开 `sse`、`websocket`、`websocket-cached` 和 `auto`（`.repo/pi-0.84.3/packages/ai/src/types.ts:110`）。 | 两者都包含 SSE 行为，但只有 pi 公开所引用的 Provider 中立传输选择器。 | refined |

## 5. Agent 循环与 harness

**opi：** 公共 `agent_loop` 与有状态的 `Agent` 配对；`SessionFacade` 和产品侧 `CodingHarness` 分别提供会话封装和产品封装（`crates/opi-agent/src/agent_loop.rs:29-58`、`crates/opi-agent/src/agent.rs:539-563`、`crates/opi-agent/src/harness.rs:217-255`、`crates/opi-coding-agent/src/harness.rs:662`）。

**pi：** 公共 `agentLoop`/`agentLoopContinue` 与有状态的 `Agent` 配对；`AgentHarness` 公开已实现的状态/配置访问器，而若干运行、导航、lane 和 watch 操作返回 `HarnessNotImplemented`（`.repo/pi-0.84.3/packages/agent/src/agent-loop.ts:27-64`、`.repo/pi-0.84.3/packages/agent/src/agent.ts:167-181`、`.repo/pi-0.84.3/packages/agent/src/harness/agent-harness.ts:305-503`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 核心轮次接口 | 公共循环函数加有状态封装（`crates/opi-agent/src/agent_loop.rs:29-58`、`crates/opi-agent/src/agent.rs:539-563`）。 | 公共循环函数加有状态封装（`.repo/pi-0.84.3/packages/agent/src/agent-loop.ts:27-64`、`.repo/pi-0.84.3/packages/agent/src/agent.ts:167-181`）。 | 两者都将循环函数与事件驱动的有状态封装分离。 | confirmed |
| 名为 `AgentHarness` 的类型 | 缺失公共 `AgentHarness` 类型：已在 `crates/opi-agent/src` 和 `crates/opi-coding-agent/src` 中搜索公共 struct、trait 或 type；opi 公开 `SessionFacade` 和 `CodingHarness`（`crates/opi-agent/src/harness.rs:217-255`、`crates/opi-coding-agent/src/harness.rs:662`）。 | 状态/配置访问器已实现；提示词/运行/导航/lane/watch 操作返回 `HarnessNotImplemented`（`.repo/pi-0.84.3/packages/agent/src/harness/agent-harness.ts:305-503`）。 | 只有 pi 存在该名称的公共类型，且其操作接口的实现情况有差异，并非全部不可用。 | refined |
| 产品构造 | 交互式、runner 和 RPC 路径构建 `CodingHarness`（`crates/opi-coding-agent/src/main.rs:1243`、`crates/opi-coding-agent/src/runner.rs:271`、`crates/opi-coding-agent/src/rpc.rs:348`）。 | SDK 路径构建 `Agent`；session-server 路径构建 `AgentHarness`（`.repo/pi-0.84.3/packages/coding-agent/src/core/sdk.ts:173`、`.repo/pi-0.84.3/packages/coding-agent/src/server/create-harness.ts:153`）。 | opi 所引用的产品入口路径汇聚到一种 harness 类型；pi 保留独立的 SDK 构造和 server-harness 构造。 | confirmed |

## 6. 会话模型

**opi：** 新的持久化 JSONL 写入器要求使用 v2；v1 仍可读取，但不能重新打开进行写入。条目组成 `parent_id` 图，最新的叶条目选定活跃分支（`crates/opi-agent/src/session.rs:1-21`、`crates/opi-agent/src/session.rs:46-55`、`crates/opi-agent/src/session.rs:143-174`、`crates/opi-agent/src/session_branch.rs:30-43`）。会话搜索契约缺失：已在 `crates/opi-agent/src` 和 `crates/opi-coding-agent/src` 中搜索 `SessionSearch`、会话搜索索引和会话条目搜索 API。

**pi：** Agent 核心 JSONL 使用带命名 lane 和仓储/存储契约的 v4，而编程产品持久化的 `SessionManager` 使用 v3 和单一当前叶节点（`.repo/pi-0.84.3/packages/agent/src/harness/session/jsonl/codec.ts:89-99`、`.repo/pi-0.84.3/packages/agent/src/harness/session/types.ts:290-372`、`.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:30`、`.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:847-866`）。Agent core 还暴露异步会话搜索契约及扫描实现（`.repo/pi-0.84.3/packages/agent/src/index.ts:141`、`.repo/pi-0.84.3/packages/agent/src/search/index.ts:14-31`、`.repo/pi-0.84.3/packages/agent/src/search/scanning.ts:135-176`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 活跃格式 | 新写入器要求 v2；v1 可读但不可写（`crates/opi-agent/src/session.rs:1-21`、`crates/opi-agent/src/session.rs:46-55`、`crates/opi-agent/src/session.rs:447-488`）。 | Agent 核心 JSONL 使用 v4；编程 Agent 的 `SessionManager` 使用 v3（`.repo/pi-0.84.3/packages/agent/src/harness/session/jsonl/repo.ts:209-216`、`.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:30`）。 | pi 包含彼此独立的核心 v4 路径和编程产品 v3 路径；opi 的产品写入核心 v2 格式。 | confirmed |
| 旧版迁移 | 源文件保持不变；恢复/分叉会创建一个带父级关系的 v2 子会话，其中包含重建后的活跃分支（`crates/opi-coding-agent/src/session_cli.rs:208-216`、`crates/opi-coding-agent/src/session_cli.rs:272-313`、`crates/opi-coding-agent/src/harness.rs:2302-2306`）。 | 持久化会话在内存中从 v1 迁移到 v2，再迁移到 v3，并在加载期间重写同一文件；没有持久化时跳过重写（`.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:230-295`、`.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:895-922`）。 | opi 保留旧版源文件的原始字节并写入子会话；pi 的持久化产品路径会升级源文件。 | refined |
| 仓储契约 | `SessionRepo` 公开追加、加载和消息计数（`crates/opi-agent/src/harness.rs:142-162`）。 | 生命周期操作分布于 `SessionRepo`；lane、记录、查询、事实和统计分布于 `SessionStorage`/`SessionTree`，并提供 JSONL 仓储和内存仓储（`.repo/pi-0.84.3/packages/agent/src/harness/session/types.ts:290-372`、`.repo/pi-0.84.3/packages/agent/src/harness/session/jsonl/repo.ts:109-155`、`.repo/pi-0.84.3/packages/agent/src/harness/session/memory.ts:148-179`）。 | 所引用的 pi 契约覆盖更多生命周期、lane、查询和替代存储操作。 | refined |
| 分支表示 | `parent_id` 图加最新叶条目选定活跃分支（`crates/opi-agent/src/session.rs:143-174`、`crates/opi-agent/src/session_branch.rs:30-43`）。 | Agent 核心 v4 公开命名 lane 指针；编程 Agent v3 则另行维护一个当前叶节点（`.repo/pi-0.84.3/packages/agent/src/harness/session/types.ts:273-296`、`.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:847-866`）。 | opi 使用一条活跃父链；pi 在 v4 中公开命名 lane，同时在 v3 中保留当前叶节点产品路径。 | confirmed |
| 会话搜索 API | 缺失：已在 `crates/opi-agent/src` 和 `crates/opi-coding-agent/src` 中搜索 `SessionSearch`、会话搜索索引和会话条目搜索 API。 | Package root 导出 `SessionSearch`，其命中具有稳定的 session/entry 标识；`createScanningSessionSearch` 已实现，并由聚焦的 memory、filter、abort 与 JSONL 测试覆盖（`.repo/pi-0.84.3/packages/agent/src/index.ts:141`、`.repo/pi-0.84.3/packages/agent/src/search/index.ts:14-31`、`.repo/pi-0.84.3/packages/agent/src/search/scanning.ts:135-176`、`.repo/pi-0.84.3/packages/agent/test/harness/session/search.test.ts:57-124`）。 | 只有 pi 暴露受测的会话搜索契约及扫描实现。 | added |

## 7. Extension 与插件接口

**opi：** 核心 extension 契约可贡献工具、Provider、模型覆盖、钩子、事件、命令和状态；可执行适配器使用由清单选择的子进程 JSONL 协议（`crates/opi-agent/src/extension.rs:164-302`、`crates/opi-coding-agent/src/adapter_protocol.rs:1-4`、`crates/opi-coding-agent/src/adapter_protocol.rs:44`）。编程产品会接入若干贡献类型，但明确将 extension 工具排除在其工具注册表之外（`crates/opi-coding-agent/src/harness.rs:1490-1514`、`crates/opi-coding-agent/src/harness.rs:1603-1609`）。

**pi：** Extension 模块通过 `jiti` 在宿主进程内加载；其 API 可注册工具、命令、快捷键、标志、渲染器、Provider、小组件、页眉/页脚和编辑器组件，extension 工具会进入实时产品注册表（`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/loader.ts:490-518`、`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/types.ts:171-262`、`.repo/pi-0.84.3/packages/coding-agent/src/core/agent-session.ts:2588-2654`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 产品中的 extension 工具 | 工具是核心贡献类型，但被排除在编程产品注册之外（`crates/opi-agent/src/extension.rs:164-302`、`crates/opi-coding-agent/src/harness.rs:1603-1609`）。 | Extension 工具进入实时产品注册表（`.repo/pi-0.84.3/packages/coding-agent/src/core/agent-session.ts:2588-2654`）。 | opi 在产品层之下声明 extension 工具，并在产品组装时将其排除；pi 将其组合进产品注册表。 | refined |
| UI、CLI 和渲染器注册 | 存在自定义命令；缺失 CLI 标志、快捷键、小组件、页眉/页脚/编辑器组件和消息渲染器注册：已搜索 `crates/opi-agent/src/extension.rs`、`crates/opi-coding-agent/src/adapter_extension.rs`、`crates/opi-coding-agent/src/adapter_protocol.rs`、`crates/opi-coding-agent/src/resource.rs` 和 `crates/opi-coding-agent/src/runtime_packages.rs`。 | Extension API 注册所引用的命令、快捷键、标志、渲染器和 UI 类别（`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/types.ts:171-262`、`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/types.ts:1281-1336`）。 | pi 公开所引用的 UI/CLI/渲染类别；opi 在所述范围内公开命令，但不公开其他类别。 | refined |
| 注册时机 | 注册要求对注册表列表拥有唯一所有权，共享后再注册会失败（`crates/opi-agent/src/extension.rs:308-379`）。 | 只有成功初始化后才会提交工厂注册，已加载的 Provider 之后仍可注册或注销（`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/loader.ts:256-275`、`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/runner.ts:354-410`）。 | opi 在共享前固定注册；pi 将事务式工厂初始化与实时 Provider 变更结合。 | confirmed |
| 执行边界 | 子进程 stdin/stdout JSONL（`crates/opi-coding-agent/src/adapter_host.rs:1-15`、`crates/opi-coding-agent/src/adapter_host.rs:183-223`）。 | 通过 `jiti` 加载的宿主进程内 JavaScript/TypeScript 工厂（`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/loader.ts:490-518`）。 | 两者的 extension 执行边界分别是进程外 JSONL 和宿主进程内模块加载。 | confirmed |

## 8. 内置工具

**opi：** 八个内置工具为 `read`、`write`、`edit`、`bash`、`grep`、`find`、`ls` 和 `glob`；运行模式策略为非交互式变更增加了按类别选择启用的门控（`crates/opi-coding-agent/src/policy.rs:18-24`、`crates/opi-coding-agent/src/policy.rs:33-75`）。

**pi：** 八个内置工具为 `read`、`bash`、`powershell`、`edit`、`write`、`grep`、`find` 和 `ls`；工具选择由设置、CLI 允许/拒绝过滤器和 SDK 输入组合确定（`.repo/pi-0.84.3/packages/coding-agent/src/core/tools/index.ts:93-105`、`.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:32-35`、`.repo/pi-0.84.3/packages/coding-agent/src/core/sdk.ts:59-73`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 内置工具构成 | 八个工具，包括 `glob`（`crates/opi-coding-agent/src/policy.rs:18-24`）。 | 八个工具，包括 `powershell`（`.repo/pi-0.84.3/packages/coding-agent/src/core/tools/index.ts:93-105`）。 | 在所引集合中，`glob` 是 opi 独有的；`powershell` 是 pi 独有的。 | confirmed |
| 默认选择 | 除列入允许列表外，非交互式变更还需要按类别选择启用（`crates/opi-coding-agent/src/policy.rs:33-75`、`crates/opi-coding-agent/src/cli.rs:93-112`）。 | 工具选择使用设置、CLI 允许/拒绝过滤器和 SDK 输入（`.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:32-35`、`.repo/pi-0.84.3/packages/coding-agent/src/core/sdk.ts:59-73`）。 | opi 增加了运行模式变更门控；pi 应用通用的基于名称的过滤器。 | refined |
| 文件变更队列 | 缺失：已搜索 `crates/opi-coding-agent/src/tool`、`crates/opi-agent/src/tool.rs` 和 `crates/opi-agent/src/agent_loop.rs`。 | 对同一实际路径的写入和编辑会串行执行，而不同路径仍可并行执行（`.repo/pi-0.84.3/packages/coding-agent/src/core/tools/file-mutation-queue.ts:4-60`）。 | 只有 pi 包含所引的逐文件串行化机制。 | confirmed |
| 默认只读集合 | 五个工具：`read`、`grep`、`find`、`ls` 和 `glob`（`crates/opi-coding-agent/src/policy.rs:18-24`）。 | 四个工具：`read`、`grep`、`find` 和 `ls`（`.repo/pi-0.84.3/packages/coding-agent/src/core/tools/index.ts:164-180`）。 | opi 所引的只读集合还包括 `glob`。 | added |

## 9. 终端 UI

**opi：** `opi-tui` 是同步 Ratatui widget 库，导出面向产品的消息、编辑器、状态、工具调用、权限和信任视图；可配置的按键绑定固定为提交、中止和换行（`crates/opi-tui/src/lib.rs:1-40`、`crates/opi-tui/src/keybindings.rs:36-42`）。

**pi：** TUI package 导出通用组件以及组件、overlay 和布局根 API；其操作映射支持声明合并和 registry（`.repo/pi-0.84.3/packages/tui/src/index.ts:12-61`、`.repo/pi-0.84.3/packages/tui/src/index.ts:116-138`、`.repo/pi-0.84.3/packages/tui/src/keybindings.ts:3-69`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 组件表面 | 面向产品的 Ratatui widget（`crates/opi-tui/src/lib.rs:1-40`）。 | 通用组件以及组件、overlay 和布局根 API（`.repo/pi-0.84.3/packages/tui/src/index.ts:12-61`、`.repo/pi-0.84.3/packages/tui/src/tui.ts:84-188`）。 | 所引导出项在 opi 中强调产品视图，在 pi 中强调通用组件/布局契约。 | refined |
| 按键绑定 | 固定的提交、中止和换行字段（`crates/opi-tui/src/keybindings.rs:36-42`、`crates/opi-tui/src/keybindings.rs:162-202`）。 | 由 registry 管理、可通过声明合并扩展的操作映射（`.repo/pi-0.84.3/packages/tui/src/keybindings.ts:3-69`、`.repo/pi-0.84.3/packages/tui/src/keybindings.ts:231-307`）。 | 固定的三字段配置与可扩展的命名操作 registry。 | confirmed |
| 终端图像 | 暴露 Kitty、iTerm2、Sixel 和回退模式；检测过程选择 Kitty/iTerm2/回退模式，且 Sixel 编码器为空（`crates/opi-tui/src/terminal_image.rs:7-14`、`crates/opi-tui/src/terminal_image.rs:61-129`）。 | 支持 Kitty 和 iTerm2；Kitty 采用分块，而 iTerm2 使用单个 OSC payload（`.repo/pi-0.84.3/packages/tui/src/terminal-image.ts:6`、`.repo/pi-0.84.3/packages/tui/src/terminal-image.ts:176-220`、`.repo/pi-0.84.3/packages/tui/src/terminal-image.ts:243-269`）。 | 协议词汇和编码器行为不同；只有 pi 的 Kitty 路径采用分块。 | refined |
| Extension UI 注入 | 缺失：已在 `crates/opi-agent/src`、`crates/opi-tui/src` 和 `crates/opi-coding-agent/src` 中搜索 extension widget、header、footer、editor 和 renderer 注册。 | Extension 可安装 widget、header/footer/editor 组件和消息 renderer（`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/types.ts:171-262`、`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/types.ts:1325-1336`）。 | 只有 pi 暴露所引的 extension UI 注入 API。 | confirmed |

## 10. Skills 与提示模板

**opi：** `SKILL.md` 发现会检查层根目录及其直接子目录，并使用简单的 frontmatter 解析器；提示片段采用 `FRAGMENT.md` 目录，并支持已声明的命名 `{{argument}}` 展开（`crates/opi-coding-agent/src/skill.rs:108-210`、`crates/opi-coding-agent/src/skill.rs:320-372`、`crates/opi-coding-agent/src/prompt_fragment.rs:1-45`）。

**pi：** Skill 加载是递归的、感知 ignore 规则的，并基于 YAML frontmatter；Skill 会进入系统提示，斜杠调用会在提交前展开 Skill 或提示模板内容（`.repo/pi-0.84.3/packages/agent/src/harness/skills.ts:50-175`、`.repo/pi-0.84.3/packages/coding-agent/src/core/system-prompt.ts:63-66`、`.repo/pi-0.84.3/packages/coding-agent/src/core/agent-session.ts:1120-1174`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 运行时 registry 使用 | harness 将发现的 Skill/片段转换为资源元数据；运行时 registry 构建、正文格式化和调用展开均缺失：已搜索 `crates/opi-coding-agent/src/harness.rs`、`crates/opi-coding-agent/src/skill.rs`、`crates/opi-coding-agent/src/prompt_fragment.rs`，以及整个产品中对其 registry API 的引用（`crates/opi-coding-agent/src/harness.rs:4163-4192`）。 | Skill 会进入系统提示，斜杠调用会在提交前展开（`.repo/pi-0.84.3/packages/coding-agent/src/core/system-prompt.ts:63-66`、`.repo/pi-0.84.3/packages/coding-agent/src/core/agent-session.ts:1120-1174`）。 | opi 所引的产品路径记录元数据；pi 所引的产品路径发出并展开内容。 | confirmed |
| 发现与解析 | 根目录/直接子目录扫描，使用简单的 frontmatter 解析器（`crates/opi-coding-agent/src/skill.rs:108-210`、`crates/opi-coding-agent/src/skill.rs:320-372`）。 | 递归、感知 ignore 规则的 YAML frontmatter 加载（`.repo/pi-0.84.3/packages/agent/src/harness/skills.ts:50-175`、`.repo/pi-0.84.3/packages/coding-agent/src/core/skills.ts:168-274`）。 | 单目录标量解析与具有 ignore 处理的递归 YAML 解析。 | confirmed |
| 模板替换 | 已声明的命名 `{{argument}}` 值（`crates/opi-coding-agent/src/prompt_fragment.rs:1-45`、`crates/opi-coding-agent/src/prompt_fragment.rs:493-540`）。 | `$1`、`$@`、`$ARGUMENTS`、切片和默认值（`.repo/pi-0.84.3/packages/coding-agent/src/core/prompt-templates.ts:58-70`、`.repo/pi-0.84.3/packages/coding-agent/src/core/prompt-templates.ts:266-281`）。 | 命名 manifest 参数与位置参数/全部参数替换。 | confirmed |
| 代码位置 | Skill 和片段机制位于 `opi-coding-agent` 中（`crates/opi-coding-agent/src/skill.rs:1-22`、`crates/opi-coding-agent/src/prompt_fragment.rs:1-22`）。 | 可复用的 Agent harness Skill 支持单独集成到编程产品中（`.repo/pi-0.84.3/packages/agent/src/harness/skills.ts:38-175`、`.repo/pi-0.84.3/packages/coding-agent/src/core/agent-session.ts:1120-1174`）。 | 产品 crate 内的代码位置与 Agent harness 内的代码位置加产品集成。 | confirmed |
| 模板资源布局 | 发现包含 `FRAGMENT.md` 的目录（`crates/opi-coding-agent/src/prompt_fragment.rs:9-22`、`crates/opi-coding-agent/src/prompt_fragment.rs:404-455`）。 | 非递归发现单个 `.md` 提示模板文件（`.repo/pi-0.84.3/packages/coding-agent/src/core/prompt-templates.ts:9-14`、`.repo/pi-0.84.3/packages/coding-agent/src/core/prompt-templates.ts:136-176`）。 | 在 opi 中，模板打包单元是目录；在 pi 中则是 Markdown 文件。 | added |

## 11. CLI 模式

**opi：** CLI 暴露默认交互模式，以及非交互、NDJSON 和 stdin/stdout JSONL RPC 模式，并提供模型、补全、doctor 和会话子命令（`crates/opi-coding-agent/src/cli.rs:87-206`、`crates/opi-coding-agent/src/cli.rs:208-257`、`crates/opi-coding-agent/src/cli.rs:297-318`）。

**pi：** CLI 暴露默认交互模式，以及打印模式和文本、JSON 或 RPC 输出模式；会话输入涵盖继续、恢复选择、显式会话、分叉、无会话和自定义会话目录（`.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:11-58`、`.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:121-165`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 运行时模式 | 交互、非交互、NDJSON 和 RPC JSONL（`crates/opi-coding-agent/src/cli.rs:87-206`）。 | 交互模式，以及具有文本、JSON 和 RPC 输出的打印模式（`.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:11-58`、`.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:157-165`）。 | 两者均暴露交互、非交互、JSON 事件和 RPC 类别，但 flag 结构不同。 | confirmed |
| Doctor 与补全 | 顶层 doctor 和补全生成（`crates/opi-coding-agent/src/cli.rs:255-257`、`crates/opi-coding-agent/src/cli.rs:297-318`）。 | 缺失：已搜索 `.repo/pi-0.84.3/packages/coding-agent/src/cli.ts`、`.repo/pi-0.84.3/packages/coding-agent/src/cli`、`.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts`、`.repo/pi-0.84.3/packages/coding-agent/src/core/package-manager.ts` 和 `.repo/pi-0.84.3/packages/coding-agent/src/bun/cli.ts`。 | 只有 opi 暴露所引的这两个运维命令。 | confirmed |
| 会话 CLI | 恢复/分叉，以及列表、删除和 Markdown/JSON 导出（`crates/opi-coding-agent/src/cli.rs:208-253`）。 | 恢复/分叉，以及继续/最新会话、选择器行为、无会话、自定义会话目录和 HTML 导出（`.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:11-58`、`.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:283-313`）。 | 两者共有的恢复/分叉表面分别由不同的会话选择、生命周期和导出操作扩展。 | refined |
| RPC 命令联合类型 | 规范联合类型有 12 个变体，包括 extension 命令、跟踪和退出（`crates/opi-agent/src/sdk.rs:48-166`）。 | `RpcCommand` 有 32 个变体，包括状态查询、模型循环切换、队列模式、Bash、会话树查询和命令目录（`.repo/pi-0.84.3/packages/coding-agent/src/modes/rpc/rpc-types.ts:20-73`）。 | 测得的命令族和联合类型大小不同。 | refined |
| RPC 提示媒体 | `prompt` 仅接受文本（`crates/opi-agent/src/sdk.rs:59-64`）。 | RPC 提示接受文本和图像附件（`.repo/pi-0.84.3/packages/coding-agent/src/modes/rpc/rpc-types.ts:20-25`）。 | 只有 pi 的本地 RPC 提示携带图像附件。 | added |

## 12. 打包与更新

**opi：** Package 命令可以添加、移除、列出、诊断、启用和禁用本地/Git package，其 manifest 可贡献 adapter、extension、Skill、片段和主题；锁定记录和激活记录会绑定来源及可执行材料（`crates/opi-coding-agent/src/cli.rs:320-363`、`crates/opi-coding-agent/src/package_store.rs:75-150`、`crates/opi-coding-agent/src/package_store.rs:287-314`）。

**pi：** Package 命令可以安装、移除、更新和列出 npm/Git/本地 package，并另有配置 TUI；manifest 可贡献 extension、Skill、提示和主题（`.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:46`、`.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:267-290`、`.repo/pi-0.84.3/packages/coding-agent/src/core/package-manager.ts:80-84`、`.repo/pi-0.84.3/packages/coding-agent/src/core/settings-manager.ts:80-88`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| Package 命令 | 添加、移除、列出、doctor、启用和禁用；启用/禁用控制可执行贡献的信任与激活（`crates/opi-coding-agent/src/cli.rs:320-363`、`crates/opi-coding-agent/src/package_activation.rs:517-589`）。 | 安装、移除、更新和列出，另有单独的资源配置 TUI（`.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:46`、`.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:267-290`）。 | opi 暴露诊断和可执行贡献激活命令；pi 暴露更新和单独的资源配置。 | refined |
| 更新与自更新 | 缺失：已搜索 `crates/opi-coding-agent/src/cli.rs`、`crates/opi-coding-agent/src/package_cli.rs`、`crates/opi-coding-agent/src/package_store.rs` 和 `crates/opi-coding-agent/src/package_resolver.rs`。 | 更新目标涵盖自身、extension/package 来源、模型和组合更新（`.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:336-357`、`.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:1006-1051`）。 | 只有 pi 暴露所引的更新目标。 | confirmed |
| 来源与资源 | 本地/Git 来源，包含 adapter、extension、Skill、片段和主题（`crates/opi-coding-agent/src/package_store.rs:75-150`、`crates/opi-coding-agent/src/package_discovery.rs:193-232`）。 | npm/Git/本地来源，包含 extension、Skill、提示和主题（`.repo/pi-0.84.3/packages/coding-agent/src/core/package-manager.ts:80-84`、`.repo/pi-0.84.3/packages/coding-agent/src/core/settings-manager.ts:80-88`）。 | opi 包括可执行 adapter 声明和片段；pi 包括 npm 分发和提示。 | confirmed |
| 完整性与信任 | 锁定记录会记录来源、Git commit、manifest 哈希和可执行材料；启用和激活会重新验证，并在发生漂移时使信任失效；Git 缓存替换采用分阶段执行并支持回滚（`crates/opi-coding-agent/src/package_store.rs:287-347`、`crates/opi-coding-agent/src/package_activation.rs:517-589`、`crates/opi-coding-agent/src/package_activation.rs:618-745`）。 | 项目信任对项目本地加载和写入进行门控；安装器管理的自更新会暂存制品、运行 `npm ci --ignore-scripts`、对版本进行冒烟测试并激活该版本（`.repo/pi-0.84.3/packages/coding-agent/src/core/project-trust.ts:24-95`、`.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:81-123`、`.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts:171-220`）。 | opi 所引的控制绑定贡献激活和 Git 缓存替换；pi 所引的控制绑定项目资源和受管理的自更新。 | refined |

## 13. 导出与分享

**opi：** 会话导出会写入 Markdown 或 JSON，并选择活动分支或树中跨所有分支的全部消息条目；独立 flag 可省略工具结果输出或 assistant 思考，并可选择摘要、详细或不脱敏（`crates/opi-coding-agent/src/cli.rs:48-55`、`crates/opi-coding-agent/src/session_cli.rs:481-550`）。

**pi：** JSONL 导出将当前分支序列化为线性父链，而 HTML 导出会序列化所有条目并包含当前叶节点；`/share` 会上传组织内可见的 Radius 制品，或创建私有 GitHub gist（`.repo/pi-0.84.3/packages/coding-agent/src/core/session-export.ts:6-40`、`.repo/pi-0.84.3/packages/coding-agent/src/core/export-html/index.ts:251-270`、`.repo/pi-0.84.3/packages/coding-agent/src/modes/interactive/session-share.ts:91-196`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 导出格式与树范围 | Markdown 或 JSON；活动分支，或按文件顺序排列的跨分支全部消息条目（`crates/opi-coding-agent/src/session_cli.rs:402-409`、`crates/opi-coding-agent/src/session_cli.rs:481-515`）。 | 当前分支的 JSONL，或全部条目加当前叶节点的 HTML（`.repo/pi-0.84.3/packages/coding-agent/src/core/session-export.ts:6-40`、`.repo/pi-0.84.3/packages/coding-agent/src/core/export-html/index.ts:251-270`）。 | 格式不同，并且 opi 的完整树输出包含消息条目，而不是 pi 的完整条目集合。 | refined |
| 披露控制 | 独立的工具输出/思考排除选项，以及摘要、详细或不脱敏选项（`crates/opi-coding-agent/src/cli.rs:242-253`、`crates/opi-coding-agent/src/session_cli.rs:518-550`）。 | 相应参数缺失：已搜索 `.repo/pi-0.84.3/packages/coding-agent/src/core/session-export.ts`、`.repo/pi-0.84.3/packages/coding-agent/src/core/export-html/index.ts` 和 `.repo/pi-0.84.3/packages/coding-agent/src/modes/interactive/session-share.ts`；分享序列化会直接附加系统提示和活动工具元数据（`.repo/pi-0.84.3/packages/coding-agent/src/modes/interactive/session-share.ts:24-42`）。 | 只有 opi 暴露所引的导出时排除和脱敏控制。 | confirmed |
| 发布 | 会话上传/分享处理程序缺失：已在 `crates/opi-coding-agent/src` 和 `crates/opi-agent/src` 中搜索 share、Radius、gist 创建、上传和发布实现。 | 配置后，`/share` 会上传 Radius NDJSON；否则调用 `gh gist create --public=false`；所选 Radius 路径失败后不会回退到 gist（`.repo/pi-0.84.3/packages/coding-agent/src/modes/interactive/session-share.ts:91-196`）。 | 只有 pi 包含产品内会话发布路径。 | refined |

## 14. 权限与沙箱

**opi：** 每次已注册工具的执行都要经过强制 authorizer 边界，缺少 authorizer 会得到受控拒绝；项目信任 resolver 采用拒绝优先的聚合方式（`crates/opi-agent/src/authority.rs:1-11`、`crates/opi-agent/src/agent_loop.rs:1785-1818`、`crates/opi-coding-agent/src/project_trust.rs:460-495`）。`opi-sandbox` CLI 会在不支持约束时拒绝执行，而库也暴露一个显式的不受限制 `NoRestriction` 接缝（`crates/opi-sandbox/src/cli.rs:507-518`、`crates/opi-sandbox/src/lib.rs:20-33`）。

**pi：** 已验证的工具调用会执行，除非可选的工具执行前 hook 将其阻止；当存在 extension handler 时，产品会将该 hook 绑定到它们。项目信任 extension 使用第一个明确结果，沙箱则作为示例 extension 提供（`.repo/pi-0.84.3/packages/agent/src/agent-loop.ts:615-700`、`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/runner.ts:203-222`、`.repo/pi-0.84.3/packages/coding-agent/examples/extensions/sandbox/index.ts:201-225`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 工具授权 | 缺少 authorizer 会在执行前拒绝（`crates/opi-agent/src/authority.rs:1-11`、`crates/opi-agent/src/agent_loop.rs:1785-1818`）。 | 调用会执行，除非可选的工具执行前 hook 将其阻止；确认门控是一个示例 extension（`.repo/pi-0.84.3/packages/agent/src/agent-loop.ts:615-700`、`.repo/pi-0.84.3/packages/coding-agent/examples/extensions/permission-gate.ts:1-33`）。 | opi 中的强制运行时授权与 pi 中的可选 extension 拦截。 | confirmed |
| 项目信任仲裁 | `Deny` 是终结性的；`Trust` 在后续 resolver 运行完毕前保持暂定状态（`crates/opi-coding-agent/src/project_trust.rs:460-495`）。 | 第一个返回 yes/no 的 handler 胜出；未决定则继续向后处理（`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/runner.ts:203-222`）。 | 项目信任 resolver 规则分别是拒绝优先的聚合与采用第一个明确结果。 | refined |
| 沙箱立场 | 原生约束不受支持时，面向用户的 CLI 以 125 退出；库也暴露显式的不受限制 `NoRestriction`（`crates/opi-sandbox/src/platform/mod.rs:1-11`、`crates/opi-sandbox/src/cli.rs:507-518`、`crates/opi-sandbox/src/lib.rs:20-33`）。 | 沙箱示例在禁用或未初始化时委托给本地 Bash，包括不受支持的平台和初始化失败（`.repo/pi-0.84.3/packages/coding-agent/examples/extensions/sandbox/index.ts:201-225`、`.repo/pi-0.84.3/packages/coding-agent/examples/extensions/sandbox/index.ts:234-284`）。 | opi 提供独立的限制产品，分别具有受约束路径和显式不受限制路径；pi 提供一个会回退到本地执行的可选示例。 | refined |

## 15. MCP

**opi：** 核心/产品源代码中不存在 MCP 协议客户端、服务器或运行时；仓库内的一个示例基于夹具数据对 MCP 风格的命令进行建模，Rust 适配器则实现在集成测试中（`examples/mcp-adapter/README.md:7-28`、`crates/opi-coding-agent/tests/mcp_adapter_example.rs:16-20`）。

**pi：** 编程 Agent 将 extension 描述为 MCP 集成途径，但受测 package 中不存在 MCP 协议客户端、服务器、运行时或示例实现（`.repo/pi-0.84.3/packages/coding-agent/README.md:394`、`.repo/pi-0.84.3/packages/coding-agent/README.md:498`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 运行时实现 | 缺失：已在 `crates/opi-ai/src`、`crates/opi-agent/src`、`crates/opi-coding-agent/src`、`crates/opi-protocol/src` 和 `Cargo.toml` 中搜索 MCP 协议/运行时/依赖项符号。 | 缺失：已在 `.repo/pi-0.84.3/packages/ai/src`、`.repo/pi-0.84.3/packages/agent/src`、`.repo/pi-0.84.3/packages/coding-agent/src`、`.repo/pi-0.84.3/packages/protocol/src`、`.repo/pi-0.84.3/packages/client/src`、`.repo/pi-0.84.3/packages/server/src` 及其清单中搜索。 | 两个受测核心/产品代码树均不包含 MCP 运行时实现。 | confirmed |
| 仓库内适配器材料 | 仅有夹具的 MCP 风格清单，适配器代码位于集成测试中（`examples/mcp-adapter/README.md:7-28`、`examples/mcp-adapter/README.md:40-42`、`crates/opi-coding-agent/tests/mcp_adapter_example.rs:273-305`）。 | 缺失：已在 `.repo/pi-0.84.3/packages/ai/examples`、`.repo/pi-0.84.3/packages/agent/examples` 和 `.repo/pi-0.84.3/packages/coding-agent/examples` 中搜索 MCP 示例。 | 只有 opi 包含所引用的仓库内夹具适配器材料。 | refined |

## 16. 图像生成

**opi：** 该产品支持用户图像输入和通用工具结果图像内容，但受测源代码中不存在专用的图像生成模型、Provider、请求或路由 API（`crates/opi-coding-agent/src/image.rs:1-8`、`crates/opi-coding-agent/src/image.rs:22-57`、`crates/opi-ai/src/message.rs:91-127`）。

**pi：** `pi-ai` 暴露 `generateImages()`、`ImagesModels`、由 Provider 解析的身份验证、内置 OpenRouter 图像 Provider，以及生成的图像模型目录（`.repo/pi-0.84.3/packages/ai/src/images.ts:14-20`、`.repo/pi-0.84.3/packages/ai/src/images-models.ts:45-88`、`.repo/pi-0.84.3/packages/ai/src/providers/openrouter-images.ts:7-21`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 库生成 API | 存在用户图像输入和通用工具结果图像；专用生成 API 缺失：已在 `crates/opi-ai/src`、`crates/opi-agent/src`、`crates/opi-coding-agent/src` 和 `Cargo.toml` 中搜索图像生成模型、Provider、请求和路由（`crates/opi-coding-agent/src/image.rs:1-8`、`crates/opi-ai/src/message.rs:91-127`）。 | `generateImages()` 以及 `ImagesProvider` 和 `ImagesModels`（`.repo/pi-0.84.3/packages/ai/src/images.ts:14-20`、`.repo/pi-0.84.3/packages/ai/src/images-models.ts:8-88`）。 | 只有 pi 暴露专用的库生成 API。 | refined |
| 内置生成 Provider | 缺失：已在 `crates/opi-ai/src` 中搜索图像生成 Provider 和模型目录 API。 | OpenRouter 适配器以及生成的目录（`.repo/pi-0.84.3/packages/ai/src/providers/openrouter-images.ts:7-21`、`.repo/pi-0.84.3/packages/ai/src/image-models.generated.ts:6-22`）。 | 只有 pi 包含内置图像生成 Provider。 | confirmed |
| 编程产品命令/工具 | 缺失：已在 `crates/opi-coding-agent/src` 中搜索图像生成命令/工具注册。 | 缺失：已在 `.repo/pi-0.84.3/packages/coding-agent/src` 中搜索 `generateImages`、`ImagesModels`、`ImagesProvider` 和生成命令/工具注册。 | 两个编程产品均未注册内置图像生成命令或工具。 | confirmed |
| extension 对生成库的访问 | 缺失：已在 `crates/opi-ai/src`、`crates/opi-agent/src` 和 `crates/opi-coding-agent/src` 中搜索暴露给 extension 的图像生成库或捆绑生成 Provider 模块。 | extension 加载器暴露捆绑的 `pi-ai/compat` 和 `pi-ai/providers/all` 模块（`.repo/pi-0.84.3/packages/coding-agent/src/core/extensions/loader.ts:59-72`）。 | 即使产品没有内置生成命令/工具，pi extension 仍可使用生成库。 | added |

## 17. 诊断与可观测性

**opi：** 共享的结构化诊断携带稳定代码并明确执行公开脱敏；`opi doctor` 在六个范围内进行本地、无网络的检查，而选择启用的跟踪捕获会写入每次运行的证据 JSONL 及一份清单（`crates/opi-agent/src/diagnostic.rs:70-112`、`crates/opi-coding-agent/src/doctor.rs:3-17`、`crates/opi-coding-agent/src/evidence.rs:52-64`）。

**pi：** 诊断覆盖参数解析、服务创建、设置和资源。遥测 package 定义回调 span、类型化 schema 以及无操作/内存适配器；受测运行时源代码中没有持久遥测导出器（`.repo/pi-0.84.3/packages/coding-agent/src/core/diagnostics.ts:1-15`、`.repo/pi-0.84.3/packages/telemetry/src/index.ts:14-24`、`.repo/pi-0.84.3/packages/telemetry/src/memory.ts:188-218`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 运维健康检查命令 | `opi doctor` 在不进行网络调用的情况下覆盖配置、Provider、package、会话、TUI 和 RPC（`crates/opi-coding-agent/src/doctor.rs:3-17`、`crates/opi-coding-agent/src/doctor.rs:59-84`）。 | 缺少统一的 doctor 命令：已搜索 `.repo/pi-0.84.3/packages/coding-agent/src/main.ts`、`.repo/pi-0.84.3/packages/coding-agent/src/cli` 和 `.repo/pi-0.84.3/packages/coding-agent/src/package-manager-cli.ts`。 | 只有 opi 包含统一的健康检查命令。 | confirmed |
| 诊断契约 | 严重程度、稳定代码、来源、消息、详细信息、操作，以及完全脱敏的公开 payload（`crates/opi-agent/src/diagnostic.rs:70-112`、`crates/opi-agent/src/diagnostic.rs:144-163`）。 | 参数、服务、设置和资源诊断使用范围更窄的运行时/资源记录；所引用的文件中不存在共享的诊断脱敏方法（`.repo/pi-0.84.3/packages/coding-agent/src/cli/args.ts:53-58`、`.repo/pi-0.84.3/packages/coding-agent/src/core/diagnostics.ts:1-15`）。 | 诊断 schema 和公开边界处理方式不同。 | refined |
| 持久证据与遥测 span | 选择启用的文件 sink 会追加写入 `evidence.jsonl`，并为每次运行持久发布 `manifest.json`（`crates/opi-coding-agent/src/evidence.rs:52-64`、`crates/opi-coding-agent/src/evidence.rs:166-215`、`crates/opi-coding-agent/src/evidence.rs:300-391`）。 | 回调 span、类型化 schema 和无操作/内存适配器；span 辅助函数及持久/文件/OTLP 导出器的运行时调用点缺失：已搜索 `.repo/pi-0.84.3/packages/telemetry/src`、`.repo/pi-0.84.3/packages/agent/src` 和 `.repo/pi-0.84.3/packages/coding-agent/src`（`.repo/pi-0.84.3/packages/telemetry/src/index.ts:14-24`、`.repo/pi-0.84.3/packages/telemetry/src/memory.ts:188-218`）。 | opi 提供持久运行证据；pi 提供遥测契约和参考适配器，但在受测范围内没有持久导出器。 | refined |
| 安装/更新版本 ping | 缺失：已在 `crates/opi-coding-agent/src` 和 `crates/opi-agent/src` 中搜索安装/更新遥测、版本 ping 及版本 ping 调用点。 | 在全新安装或检测到更新后，交互模式会发送可选择退出的安装/更新版本 ping；离线模式和禁用安装遥测时会跳过（`.repo/pi-0.84.3/packages/coding-agent/src/modes/interactive/interactive-mode.ts:1263-1300`、`.repo/pi-0.84.3/packages/coding-agent/src/core/telemetry.ts:8-13`）。 | 只有 pi 包含受测产品版本 ping。 | added |

## 18. Wire protocol 与嵌入

**opi：** `CodingHarness` 是进程内产品封装器；独立的 schema-v3 stdin/stdout JSONL RPC 控制编程 Agent，而 `opi-protocol` 定义的是单次执行命令后端 JSONL wire，而非远程会话服务（`crates/opi-coding-agent/src/harness.rs:655-703`、`crates/opi-coding-agent/src/rpc.rs:1-36`、`crates/opi-protocol/src/lib.rs:1-17`）。

**pi：** `createAgentSession()` 是进程内产品 API，编程产品具有独立的本地 JSONL RPC，而实验性的 protocol/client/server package 定义了一个带长度前缀的 CBOR 多会话栈及字节传输客户端抽象（`.repo/pi-0.84.3/packages/coding-agent/src/core/sdk.ts:173-188`、`.repo/pi-0.84.3/packages/coding-agent/src/modes/rpc/rpc-types.ts:1-73`、`.repo/pi-0.84.3/packages/protocol/src/schemas.ts:260-325`、`.repo/pi-0.84.3/packages/client/src/transport.ts:1-18`）。

| 项目 | opi | pi | 差异 | 验证结果 |
|---|---|---|---|---|
| 进程内嵌入 | `CodingHarness` 暴露 builder 和 prompt 操作（`crates/opi-coding-agent/src/harness.rs:655-703`、`crates/opi-coding-agent/src/harness.rs:1214-1224`、`crates/opi-coding-agent/src/harness.rs:2828-2869`）。 | `createAgentSession()` 构造一个 `AgentSession`（`.repo/pi-0.84.3/packages/coding-agent/src/core/sdk.ts:39-75`、`.repo/pi-0.84.3/packages/coding-agent/src/core/sdk.ts:173-188`）。 | 两者都通过产品级会话 API 进行嵌入，但使用的类型不同。 | confirmed |
| 本地 Agent RPC | Schema-v3 stdin/stdout JSONL 包含继续、extension 命令、跟踪和退出（`crates/opi-coding-agent/src/rpc.rs:1-36`、`crates/opi-agent/src/sdk.rs:37-130`）。 | Stdin/stdout JSONL 包含携带图像的 prompt、新建会话、模型轮换、Bash、HTML 导出、分支/树和记录查询（`.repo/pi-0.84.3/packages/coding-agent/src/modes/rpc/rpc-types.ts:1-73`）。 | 两个产品均暴露本地 JSONL 控制面，但命令联合类型不同。 | confirmed |
| 远程多会话 wire | 远程产品会话客户端/服务器缺失：已搜索 `crates/opi-agent/src`、`crates/opi-coding-agent/src` 和 `crates/opi-protocol/src`；`opi-protocol` 转而负责单次命令执行（`crates/opi-protocol/src/lib.rs:1-17`、`crates/opi-protocol/src/execution/v1/mod.rs:1-23`）。 | Protocol v1 使用经过验证的带长度前缀 CBOR、按会话寻址的命令，以及管理多个活动会话的服务器（`.repo/pi-0.84.3/packages/protocol/src/schemas.ts:260-325`、`.repo/pi-0.84.3/packages/protocol/src/codec.ts:60-85`、`.repo/pi-0.84.3/packages/server/src/sessions.ts:38-115`）。 | 只有 pi 包含受测的远程多会话产品栈。 | refined |
| 客户端传输实现 | 远程会话字节传输接缝缺失：已在 `crates/opi-agent/src`、`crates/opi-coding-agent/src` 和 `crates/opi-protocol/src` 中搜索 `ByteTransport`、CBOR、WebSocket 和会话 socket 监听器。 | `ByteTransport` 是抽象的；随附的实现是 Node Unix 域 socket 传输。WebSocket/浏览器实现缺失：已搜索 `.repo/pi-0.84.3/packages/protocol/src`、`.repo/pi-0.84.3/packages/client/src` 和 `.repo/pi-0.84.3/packages/server/src`（`.repo/pi-0.84.3/packages/client/src/transport.ts:1-18`、`.repo/pi-0.84.3/packages/client/src/unix.ts:1-23`）。 | pi 暴露运行时中立的字节接缝和一个 Unix socket 适配器，而非随附的 WebSocket/浏览器客户端；在所述范围内，opi 两者都未暴露。 | refined |
| 第三种命令联合类型 | Opi 的受测命令联合类型是本地编程 Agent RPC 和单次执行协议（`crates/opi-agent/src/sdk.rs:37-130`、`crates/opi-protocol/src/execution/v1/mod.rs:1-23`）。 | 远程协议具有独立的 9 命令联合类型：列出、创建、附加、分离、prompt、引导、中止、设置模型和设置思考（`.repo/pi-0.84.3/packages/protocol/src/schemas.ts:291-325`）。 | pi 的远程协议增加了第三个更窄的控制面，与两个产品的本地 RPC 联合类型均不同。 | added |

## Layer H：目标设计视野

固定的 pi 源码树包含 `packages/agent/docs/harness.md`，其标题为 `AgentHarness — implementation specification`。该文档说明，只有其中的 `must` / `must not` 条款具有规范性，其余文字属于解释（`.repo/pi-0.84.3/packages/agent/docs/harness.md:1`、`.repo/pi-0.84.3/packages/agent/docs/harness.md:221`）。源码树还包含 `packages/agent/docs/search.md`，其最后一段提出后续 indexing seam，但没有声明该提议具有规范性（`.repo/pi-0.84.3/packages/agent/docs/search.md:270-276`）。下表保留这些权威边界，不把完整设计视为 pi 已交付行为，也不把它视为 Opi 需求。

### H5-H6. Agent harness 与持久会话设计

| 设计项 | pi 目标设计 | 设计权威/状态 | pi 当前证据 | 观察到的成熟度 | opi 当前证据 | 客观视野差异 | 验证结果 |
|---|---|---|---|---|---|---|---|
| 持久会话系统模型 | 会话由不可变 entry tree、可变 facts、具名 lanes 和只追加 usage ledger 组成；每条 lane 拥有自己的 leaf、配置、队列以及至多一个 operation（`.repo/pi-0.84.3/packages/agent/docs/harness.md:88-105`）。 | 解释性系统模型；只有显式 `must` / `must not` 条款被文档自行声明为规范性内容（`.repo/pi-0.84.3/packages/agent/docs/harness.md:221`）。 | Agent core v4 暴露 lane/storage/repo 契约及已实现的 lane/operation 委托，并有 JSONL 聚焦测试；编程产品仍单独持久化 v3 current-leaf 模型（`.repo/pi-0.84.3/packages/agent/src/harness/session/types.ts:273-372`、`.repo/pi-0.84.3/packages/agent/src/harness/session/session.ts:190-219`、`.repo/pi-0.84.3/packages/agent/test/harness/session/jsonl.test.ts:273-322`、`.repo/pi-0.84.3/packages/agent/CHANGELOG.md:30-40`、`.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:30`、`.repo/pi-0.84.3/packages/coding-agent/src/core/session-manager.ts:847-866`）。 | `partial` | Opi 当前暴露 parent-entry 图、一个选定的活动分支、`SessionRepo` 与 `SessionFacade`；在 `crates/opi-agent/src` 和 `crates/opi-coding-agent/src` 中未搜索到具名 lane 声明（`crates/opi-agent/src/session.rs:143-174`、`crates/opi-agent/src/session_branch.rs:30-43`、`crates/opi-agent/src/harness.rs:142-255`）。 | pi 的目标把 tree、facts、具名 lanes、usage 与持久 operation 收敛为一个会话模型；所引的 Opi 模型目前暴露 parent graph、repository 和 facade，但没有这一完整的具名 lane 契约。 | refined |
| Harness 职责与持久 operation | Harness 的目标职责是驱动 lane prompt、模型/工具步骤、队列、压缩、导航和恢复；它还拥有 registry、hook、event、配置及 operation 的完整持久状态（`.repo/pi-0.84.3/packages/agent/docs/harness.md:101-110`）。 | 文档条款级权威规则下的解释性目标模型（`.repo/pi-0.84.3/packages/agent/docs/harness.md:221`）。 | v0.84.3 changelog 将公开 v2 界面称为 compile-complete scaffold；聚焦测试显示配置访问器可用，而 prompt、压缩、导航、恢复、队列、lane、watch、hook 和 event 路径均以 `HarnessNotImplemented` 拒绝（`.repo/pi-0.84.3/packages/agent/CHANGELOG.md:39`、`.repo/pi-0.84.3/packages/agent/test/harness/agent-harness-scaffold.test.ts:84-187`）。 | `scaffold` | Opi 将当前 loop、持久会话 facade 与产品策略分别放在 `Agent`、`SessionFacade` 和 `CodingHarness` 中；在所搜索的 agent 与 coding-agent 源码中不存在名为 `AgentHarness` 的公开类型（`crates/opi-agent/src/agent.rs:539-563`、`crates/opi-agent/src/harness.rs:217-255`、`crates/opi-coding-agent/src/harness.rs:655-703`）。 | pi 为 Opi 当前分布在 runtime、session 与产品 wrapper 中的职责描述了一个未来 harness 边界。当前 pi scaffold 尚不足以为该目标建立行为等价性。 | confirmed |
| 从 scaffold 过渡的构建顺序 | 实现规范将 storage/search 工作与顺序执行的 R1-R12 runtime track 分开；R1 允许公开 operation 仍未实现，R12 则移除剩余 scaffold 代码（`.repo/pi-0.84.3/packages/agent/docs/harness.md:2782-2807`）。文档还说明，现有 harness 源码和测试可以删除，无须适配（`.repo/pi-0.84.3/packages/agent/docs/harness.md:2809-2815`）。 | 文档条款级权威规则下的解释性实现顺序（`.repo/pi-0.84.3/packages/agent/docs/harness.md:221`）。 | v0.84.3 仍保留目标顺序允许在 slice 1 删除的 scaffold 和 scaffold 测试；changelog 没有声明已完成某个目标 R slice（`.repo/pi-0.84.3/packages/agent/CHANGELOG.md:39`、`.repo/pi-0.84.3/packages/agent/test/harness/agent-harness-scaffold.test.ts:56-187`）。 | `designed-only` | 不适用：此项记录 pi 自身的过渡路径，而非 Opi 当前能力。 | 该构建顺序解释了为何 v0.84.3 不是 Harness v2 的最终架构；不能仅从 slice 顺序推导 Opi 等价性结论。 | refined |
| 外部 effect 保证 | Exactly-once 外部 effect、provider stream 恢复、多会话 writer、复制、持久 register 历史以及 runtime 删除都是目标设计明确列出的非目标（`.repo/pi-0.84.3/packages/agent/docs/harness.md:207-214`）。 | 明确的非目标。 | 不适用：此行记录目标边界，而非实现成熟度。 | `not-assessed` | 不适用：pi 的非目标不会创建 Opi 等价性要求。 | 不得据此得出 Opi 等价性结论；这些约束限定了应如何解释目标设计。 | confirmed |
| 未来 Postgres retention | 分区 retention 章节描述一个可能的 Postgres 部署，并说明它只是当前最佳猜测而非契约（`.repo/pi-0.84.3/packages/agent/docs/harness.md:2700-2710`）。 | 明确标为 informative。 | 不适用：来源明确说明交付 backend 不受该未来构想约束。 | `not-assessed` | 不适用：记录上游非约束性未来材料时，不据此声称 Opi 存在缺口。 | 不得据此得出 Opi 等价性结论；这里仅将其保留为设计沿革证据。 | confirmed |
| 未解决的设计问题 | 打开 operation 中缺失模型的修复方式、overflow 启发式以及 pending-payload 写放大仍是开放问题（`.repo/pi-0.84.3/packages/agent/docs/harness.md:2937-2941`）。 | 明确的开放问题。 | 不适用：目标文档并未声称这些问题已经解决。 | `not-assessed` | 不适用：pi 尚未解决的设计不会定义 Opi 目标。 | 在 pi 通过权威边界明确的设计或实现证据解决这些问题前，不得据此得出 Opi 等价性结论。 | confirmed |
| 搜索索引集成 | 会话搜索文档说明 opt-in 仍需要同步/indexing 层，并提出一个默认 no-op 的 index sink，使规范写入点可以无条件发出 indexing 事件（`.repo/pi-0.84.3/packages/agent/docs/search.md:270-276`）。 | 解释性提议；文档没有声明其具有规范性。 | Query/scanning API 与 SQLite FTS 重建/trigger 维护已经实现，但所提议的共享 sink 缺失：已在 `.repo/pi-0.84.3/packages/agent/src/search` 和 `.repo/pi-0.84.3/packages/agent/src/harness` 中搜索 `SearchIndexSink`、`NOOP_SEARCH_INDEX_SINK` 及 index-event sink 实现（`.repo/pi-0.84.3/packages/agent/src/search/index.ts:14-31`、`.repo/pi-0.84.3/packages/agent/src/search/scanning.ts:135-176`、`.repo/pi-0.84.3/packages/session-backends/sqlite-node/src/sqlite/search-backend.ts:61-98`、`.repo/pi-0.84.3/packages/session-backends/sqlite-node/test/search.test.ts:170-188`）。 | `designed-only` | 会话搜索与 index-event sink 均缺失：已搜索 `crates/opi-agent/src` 和 `crates/opi-coding-agent/src`。 | pi 具有当前 query/scanning、backend-specific FTS 维护以及一个共享 indexing sink 的解释性提议；Opi 在所搜索范围内没有当前对应物。该视野差异不分配 Opi 优先级。 | refined |

## 附录 B：方法与验证

- 维度数：18/18。
- 保留的差异数：68。
- 保留的结果统计：36 `confirmed`、25 `refined`、7 `added`、0 `refuted`。
- Layer H 数量：7 个目标设计项。观察到的成熟度统计：1 个 `partial`、1 个 `scaffold`、2 个 `designed-only`、3 个 `not-assessed`；验证结果统计：4 个 `confirmed`、3 个 `refined`。
- 独立覆盖：维度 1-6 在测量后，由负责测量 13-18 的工作者验证；维度 7-12 由负责测量 1-6 的工作者验证；维度 13-18 由负责测量 7-12 的工作者验证。没有任何维度由执行其第一轮测量的工作者验证。
- Layer H 覆盖：全部 7 项均经过独立的后续验证；其中 3 项经修正以改正证据或成熟度标签。
- Layer H 设计搜索：检查了 `.repo/pi-0.84.3/packages/agent/docs` 和 `.repo/pi-0.84.3/packages/coding-agent/docs` 中的实现规范、架构/设计材料及明确面向未来的章节；后续补充将 `harness.md` 与 `search.md` 保留为重要的目标视野证据。
- 时效性：opi 的空 `Unreleased` 章节位于版本 0.8.1 之前（`CHANGELOG.md:8-10`）。受测 pi package 的变更日志将 0.84.3 标识为当前版本，包括 coding-agent、AI、protocol、client、server、agent、TUI 和 telemetry（`.repo/pi-0.84.3/packages/coding-agent/CHANGELOG.md:3`、`.repo/pi-0.84.3/packages/ai/CHANGELOG.md:3`、`.repo/pi-0.84.3/packages/protocol/CHANGELOG.md:3`）。
- 验证期间删除或替换的第一轮陈述：opi 的确包含 SSE 的具体实现；opi-sandbox 并非一概施加限制，因为库暴露 `NoRestriction`；pi 的 `AgentHarness` 具有已实现的状态/配置访问器；pi 不提供 WebSocket/浏览器客户端；此外，opi 的图像界面除用户输入外还包括工具结果图像。其范围更窄的替代行标记为 `refined`。
- 一名验证者还在 `SessionFacade`/`JsonlSessionRepo` rustdoc 中发现了陈旧的 v1 措辞，与拥有会话的 v2 模块并存（`crates/opi-agent/src/harness.rs:142-164`、`crates/opi-agent/src/session.rs:1-21`）。此问题在这里记录为内部证据不一致，不保留为跨项目差异。
- 不可用的证据：所提供的 pi 快照不包含嵌套的 `.git` 目录。通过解析上游标签 `v0.84.3` 并将快照与该提交中已跟踪文件的归档进行逐字节比较，确定了修订版本身份。外部 extension package 和下游嵌入方超出源代码树范围；所有缺失声明均仅限于其所述路径。
